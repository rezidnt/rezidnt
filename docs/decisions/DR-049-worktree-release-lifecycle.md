> Index: [§20 of the plan](../rezidnt-architecture.md#20-decision-records) · plan §7 (run substrate — `RepoSubstrate` lifecycle) · §5/§6 (pure-fold derived state — I3) · **opens** the slice [DR-047](DR-047-registry-convergence-landed.md) §Decision 6 named and settles its semantics · **completes** the allocate→use→release lifecycle [DR-007](DR-007-release-worktree.md) ratified · **dissolves** the trade DR-047 §Decision 5 declined to make · carries the queued warden `/subject` from DR-047 §Decision 4 · invariants I3, I5, I6 load-bearing

# Decision Record DR-049 — Worktree release lifecycle: released at merge, the fold's status field split, failed trees survive for triage

**Date:** 2026-07-25
**Status:** ACCEPTED (ratified under the standing autonomy grant, 2026-07-25 — routine in substance: it opens the slice DR-047 already named, settles DEFAULT-level semantics, amends derived-state shape only, and ~~mints no subject, invariant, or trait change~~ — **CORRECTED 2026-07-25: the struck clause is preserved above verbatim for audit. Its "subject" and "invariant" limbs are TRUE; its "or trait change" limb was FALSE of the merged diff, in the same way and for the same reason as the §Amends ledger below. See that line and [DR-052](DR-052-worktree-release-ledger-correction.md) §Decision 1 for the accurate five-item ledger.** Flagged rather than assumed, so the owner can overturn it knowingly. **The two settlements most likely to warrant re-opening are Decision 2** (the fold split — it changes the board's rendered shape and every test that asserts `status`) **and Decision 3** (explicit-only release of failed trees — it accepts unbounded accumulation until an operator acts).)
**Amends:** [DR-047](DR-047-registry-convergence-landed.md) §Decision 6 — the named slice is **OPENED and its four undecided questions SETTLED** below. DR-047 §Decision 5's declined trade ("releasing at merge trades one derived-state regression for another") is **DISSOLVED, not chosen between**: Decision 2 removes the clobber that made it a trade. [DR-007](DR-007-release-worktree.md) is **unchanged in shape** — the three-method trait stands; this record makes its lifecycle complete in production for the first time. The §5/§6 fold's derived worktree entry is amended (Decision 2) — derived state, rebuildable from the log, not fabric vocabulary. ~~Mints NO invariant, NO subject (`worktree.released` v1 already exists — verified, `spec/ontology.md` §payloads), NO trait method, NO new dep.~~ **STRUCK as false of the merged diff and replaced — see the DR-052 note directly below.** **CARRIES one queued warden `/subject`** (DR-047 §Decision 4). Adds a §20 index row.

> **AMENDED by [DR-052](DR-052-worktree-release-ledger-correction.md) (owner-ACCEPTED 2026-07-25).** §Decision 1 strikes the ledger clause above as false of what merged (lane commit `a63e580`, merged at `da17699`) and supplies the accurate disclosure. **Still true, unchanged:** NO invariant; NO subject — none added, renamed, or versioned (`worktree.released` v1 already existed; `v` stays 1 everywhere the lane touched); NO new dep (no `Cargo.toml` changed in the merge). **This is NOT the same as "the ontology was untouched"** — the lane touched `spec/ontology.md` in THREE recorded warden entries: the queued DR-047 §Decision 4 emitter-cell/`source` pass, the queued `status = "merged"` prose correction paid in the same change-set as the split's code and fixtures, and — the only one of the three that mints anything — the additive-optional payload FIELD at item (e) below. *(This sentence has now carried a false claim twice: first "prose/emitter pass, nothing added or renamed," then an exhaustive count of two. The exhaustive form is what keeps failing; the count is stated here against the `spec/ontology.md` changelog and should be re-checked there rather than trusted from this record.)* **False, corrected — this record DID mint five things**, every one of them already authorized *in substance* by Decision 2/3 below and each ratified by DR-052 under a house precedent: **(a)** one DEFAULTED trait method `McpSubstrate::release_worktree` (`crates/rezidnt-mcp/src/lib.rs`) — not an I4 violation, because `McpSubstrate` is the MCP core's daemon seam and not one of the four I4-bound §7 traits (precedent [DR-011](DR-011-permit-pdp-config-seam.md) §Decision 1); **(b)** one mutating MCP tool `release_worktree` plus its `ReleaseWorktreeArgs` schemars type (`crates/rezidnt-types/src/mcp.rs`; precedent [DR-044](DR-044-live-lead-sub-fanout.md)); **(c)** one additive refusal code `worktree.unknown` (precedent [DR-045](DR-045-fan-out-lead-only.md)); **(d)** a breaking `board_view` response-shape change — `WorktreeRow` drops `status`, gains `lifecycle` + `outcome` — which amends [DR-039](DR-039-board-view-mcp-resource.md) §Decision 3 and is now flagged there; **(e)** one additive-optional PAYLOAD FIELD `worktree?: string` on the ratified `gate.failed` v1 subject (`spec/ontology.md:79` table cell and `:330` payload block both attribute it to this lane; emitted in `bins/rezidentd/src/gates.rs`, folded in `crates/rezidnt-state/src/lib.rs` under a "DR-049 criterion (c)" comment; lane commit `df61f89`, ontology entry `4bab765`) — additive, `v` stays 1, existing fixtures stay valid, and it carries its own DR-012 declared-vs-absent value-semantics ruling (present IFF the gate ran against an allocated tree, absent on the pre-spawn `vet` refusal, never synthesized to `""`). A field is a mint the house ledger enumerates explicitly, and criterion (c) below could not be met without it. The defect was an **incomplete ledger, not unauthorized work**. DR-052 further ESCALATES this record's §Consequences risk (1) — see the pointer there — and settles two `release_worktree` door policies out of rustdoc into decision text (a dual operator-badge-OR-agent-macaroon door; reuse of the verb `"merge"`). This record's Context and Decision text below are UNCHANGED.

## Context

Every premise below was checked against the tree before drafting (DR-047 §Decisions 2 and 8: a brief is a hypothesis until verified). (i) `release_worktree` has **no production caller** — `bins/` references are comments and test mocks only; every allocation therefore leaks a `notify` watcher plus debounce task for the daemon's lifetime, trees stay on disk, registry entries never close (DR-047 risk-register ADD 1). (ii) The fold collapses lifecycle and outcome into **one `status` String** (`crates/rezidnt-state/src/lib.rs:847` sets `"released"`, `:866` sets `"merged"`), so a release fact after a merge fact would clobber `"merged"` — this is exactly why DR-047 §Decision 5 declined to release at merge. (iii) `RunTaskContext` (`bins/rezidentd/src/runs.rs:1615`) carries the worktree as a `PathBuf` only; the daemon cannot call the `WorktreeId`-keyed release verb without threading the id. (iv) The adapter's release verb is **already built and tested**: it drops the watcher (closing the mpsc, which ends the debounce loop), removes the tree, closes and persists the registry entry, and emits `worktree.released` `{path, branch?}`. The slice's work is callers and the fold, not the verb.

**Strongest counterarguments, recorded.** Against Decision 1: a merged tree might hold post-merge forensic value. It loses to I3 — the merged diff is pinned in CAS and the log; a tree whose content the log can rebuild is not a record, and retaining it retains its watcher. Against Decision 3: explicit-only release means failed trees accumulate until someone acts; a TTL would bound it. The TTL loses because a timer is nondeterministic policy — wall-clock reaping is not replayable from the log and belongs, if ever, to a later DR. The accumulation is accepted and named in the risk register.

## Decision

1. **A merged worktree is released at merge.** After `diff.merged`, the run task calls `release_worktree`. The golden path completes DR-007's lifecycle; trees do not accumulate for successful runs. *(Alternative — retain for inspection — lost to I3, above.)*
2. **The fold's collapsed field is split**: the derived worktree entry gains `lifecycle` (`allocated` → `released`) and `outcome` (`merged` | `failed` | `abandoned` | absent), replacing the single `status`. `worktree.released` sets lifecycle only; `diff.merged` sets outcome only; neither can clobber the other, so the trade DR-047 flagged dissolves. The board's worktrees table and its golden fixture move with the shape. *(Alternative — an ordering guard ignoring released-after-merged — lost: it picks a regression to keep instead of removing the collision, and makes the fold order-sensitive.)*
3. **A failed run's tree survives for triage**, retained until an **explicit** release closes it — operator or lead calls the release verb, exposed MCP-first (I5) on the write-capable operator surface, never the read-only board (DR-031). v1 is explicit-only: no TTL, no timer. *(Alternative — auto-reap on TTL — lost as nondeterministic policy; a later DR may add it.)*
4. **Release drops the runtime resources** — the notify watcher and debounce task die with the release, not the daemon. Verified already implemented in the adapter (`drop(_watcher)` closes the mpsc and ends the loop); the slice owes the production call paths, not the mechanism.
5. **`WorktreeId` is threaded through `RunTaskContext`** — mechanical work owed in this slice; the context today carries only the path.
6. **QUEUED, riding with this slice, not done here:** the warden `/subject` from DR-047 §Decision 4 (the `diff.ready` emitter cell and the `source` attribution) — it moves with golden fixtures, which this slice touches anyway. Any further new or changed subject this slice discovers goes through `/subject`; none is expected — `worktree.released` v1 already exists.

**Acceptance criteria (done = `/vet` + `/debrief`, nothing else counts).** (a) A golden-path run ends with `worktree.released` on the log after `diff.merged`; the tree is gone from disk; the registry entry is closed; the watcher and debounce task are demonstrably dropped (asserted structurally host-side; behaviorally — no `diff.ready` from post-release writes — WSL-side). (b) The fold for that worktree shows `lifecycle = released` **and** `outcome = merged`. (c) A failed run's tree demonstrably survives — on disk, registry entry open, `lifecycle = allocated`, `outcome = failed` — and a manual release then closes it with `outcome = failed` preserved. **Exit demo:** one take — golden run merges and its tree vanishes; a failed run's tree persists; one explicit release call closes it; the board shows both states truthfully.

## Consequences

- **Roadmap.** DR-047 §Consequences ADDS risk (1) — the per-allocation watcher/registry leak — retires when this lands. The DR-047 §Decision 4 warden pass stops being deferred. Phase 3's terminal-substrate arc is untouched.
- **Risk register.** ADDS: (1) *failed trees accumulate until explicitly released* — unbounded by design in v1, bounded only by operator discipline. **ESCALATED by [DR-052](DR-052-worktree-release-ledger-correction.md) §Decision 3 (owner-ACCEPTED 2026-07-25): bounded by operator discipline only PRE-restart; post-restart, bounded by NOTHING.** `Daemon::allocations` is an in-memory `HashMap` with process lifetime (`bins/rezidentd/src/runs.rs:223`), so after a daemon restart a retained failed tree is no longer reachable by the MCP release door at all — the only door that could close it. It refuses LOUDLY, with `worktree.unknown`, never silently (I6-consistent). Accepted as a NAMED risk, not fixed; persisting or reaping `allocations` across a restart is deferred to a future record. This is **not** a gap in acceptance criterion (c) above, which asserts a failed tree survives and a manual release closes it and says nothing about surviving a restart. (2) *fold shape migration* — every consumer of `status` (board render, golden fixture, `s2_worktrees`/`s4_gates` assertions) changes shape in one slice; a missed consumer is a silent-wrong of exactly the class this arc produced eleven of. (3) The watcher-drop *behavioral* assertion is WSL-only (`ReadDirectoryChangesW` blindness, per DR-047); host `/vet` sees only the structural leg.
- **Test/criterion honesty, plain words.** Existing fold tests asserting the single `status` string are **rewritten to the split shape** — a criterion form change, disclosed; the replacements assert strictly more (two independent fields where one was). Nothing else is weakened; no verifier bar moves.
- **Deferred, named.** TTL/auto-reap policy for abandoned failed trees; DR-046's carry-overs unchanged.

~~Amendments to this record require DR-050.~~ **CORRECTED 2026-07-25: the reservation was spent without being used — [DR-050](DR-050-trials-slice-a-closeout.md) explicitly declined to amend this record ("No amendment to DR-049; this record shares its file but not its decisions"), and the amendment that did land is [DR-052](DR-052-worktree-release-ledger-correction.md). Further amendments to this record require DR-053.**
