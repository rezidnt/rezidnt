# rezidnt — subject taxonomy

**Taxonomy version:** v0 · **This file is the canonical copy.** `docs/rezidnt-architecture.md` Appendix B is the excerpt; when they diverge, this file wins and the appendix is stale.
**Provenance:** bootstrapped 2026-07-16 from architecture doc v0.2 — Appendix B, plus the DR-001 amendments (native harness telemetry subjects; sole-allocator worktree model).
**Custody:** this file is edited only through `/subject` inside a warden-gated ontology session; direct edits are blocked by the ontology-gate hook. Every change appends one line to the changelog at the bottom. Any change that amends a BINDING item routes through `/dr` first.

## Grammar and change discipline (BINDING)

- Subjects are dot-namespaced: `noun.verb[.qualifier]`.
- Past tense for facts (`worktree.allocated`, `gate.passed`); present tense for state deltas (`agent.status.changed`).
- Subjects are **never renamed** — deprecation only. A deprecated subject's name stays reserved forever.
- Payloads evolve **additively**. A breaking payload change mints `v+1` for that subject, and every live reducer must handle all live versions.
- Payloads ride the Event envelope (doc §5): `v: u16` is the per-subject payload schema version; payloads are JSON, hard-capped at 32 KiB (I2). Larger content becomes a CAS ref (`CasRef { hash, bytes, mime }`), never inline bytes.
- Taxonomy v0 starts every subject at `v = 1`.

**Grammar note (grandfathered):** six v0 subjects keep their Appendix B forms verbatim even though they do not strictly parse as `noun.verb`: `worktree.conflict`, `gate.inconclusive`, `diff.ready`, `daemon.warning`, `daemon.error`, `agent.message`. They are canonical and will never be renamed; strict `noun.verb[.qualifier]` applies to all new subjects from v0 forward.

## Subjects

### workspace

| Subject | v | Emitter | Semantics |
|---|---|---|---|
| `workspace.opened` | 1 | daemon (`rezidnt open` materialization) | A workspace was materialized from a project spec and is live. |
| `workspace.closed` | 1 | daemon | A workspace was shut down and is no longer live. |
| `workspace.spec.applied` | 1 | daemon | A project spec (`rezidnt.toml` shape, doc §13) was applied to a workspace — layout intent, agents, and gate bindings configured. |

### worktree

rezidnt is the **sole allocator** of worktrees (DR-001). `worktree.observed` and `worktree.conflict` are retained only to guard against out-of-band human git activity; the two-allocator reconciliation problem is deleted, not solved (DR-001, trait changes).

| Subject | v | Emitter | Semantics |
|---|---|---|---|
| `worktree.allocated` | 1 | git adapter (RepoSubstrate) | rezidnt allocated a worktree; registered under its canonicalized path with branch and allocator recorded. |
| `worktree.observed` | 1 | git adapter (FS watcher) | A worktree not allocated by rezidnt was observed on disk — out-of-band human git activity guard (DR-001). |
| `worktree.conflict` | 1 | git adapter (worktree registry) | A second claim landed on an already-registered canonicalized path; emitted instead of silently double-tracking — out-of-band human git activity guard (DR-001). |
| `worktree.released` | 1 | git adapter (RepoSubstrate) | A worktree was released and its registry entry closed. |

### session

Terminal-substrate lifecycle. DR-001 removed TerminalSubstrate from Phases 1–2 and reserves it as the Phase 3 seam; these subjects are retained (not deprecated) and their emitter arrives with Phase 3.

| Subject | v | Emitter | Semantics |
|---|---|---|---|
| `session.created` | 1 | terminal substrate (Phase 3 seam) | A terminal session came into existence. |
| `session.attached` | 1 | terminal substrate (Phase 3 seam) | A client attached to a session. The byte stream itself is out-of-band (I2); this is the lifecycle fact. |

### pane

| Subject | v | Emitter | Semantics |
|---|---|---|---|
| `pane.spawned` | 1 | terminal substrate (Phase 3 seam) | A pane was spawned within a session. |
| `pane.exited` | 1 | terminal substrate (Phase 3 seam) | A pane's process exited. |

### agent

Lifecycle facts come from `rezidnt-run` (the ProcessSubstrate, DR-001). Harness telemetry subjects (`agent.tool.invoked`, `agent.message`) were added by DR-001 for the native claude-code adapter: stream-json events map onto the fabric as typed telemetry instead of terminal-scraping heuristics.

| Subject | v | Emitter | Semantics |
|---|---|---|---|
| `agent.spawned` | 1 | rezidnt-run (AgentSubstrate impl) | An agent run was spawned — environment scrubbed, badge injected at spawn (DR-001). |
| `agent.status.changed` | 1 | rezidnt-run / harness adapter | State delta: the run's status transitioned. For claude-code, mapped from stream-json telemetry (DR-001). |
| `agent.blocked` | 1 | rezidnt-run + gate engine | The run became blocked at a gate; `gate why` / `gate_explain` answers with the failing verifier and evidence (I6). |
| `agent.completed` | 1 | rezidnt-run (reaper) | The run finished; exit status recorded. |
| `agent.signaled` | 1 | rezidnt-run (reaper) | A signal was delivered to the run (TERM→KILL escalation with grace). |
| `agent.tool.invoked` | 1 | native harness adapters (DR-001) | Harness telemetry: the agent invoked a tool. Mapped from claude-code stream-json tool-call events. **Added by DR-001.** |
| `agent.message` | 1 | native harness adapters (DR-001) | Harness telemetry: an assistant message emitted by the agent. Mapped from claude-code stream-json message events. **Added by DR-001.** |

### gate

Verdicts are `pass | fail | inconclusive` — never a bare boolean; `inconclusive` is never coerced to `pass` (I6, doc §8). Evidence blobs go to the CAS; gate events carry refs only.

| Subject | v | Emitter | Semantics |
|---|---|---|---|
| `gate.entered` | 1 | gate engine (rezidnt-gate) | A run entered a named gate (`vet`, `pre_merge`, `post_run`/debrief). |
| `gate.passed` | 1 | gate engine | Every verifier on the gate passed. Payload carries evidence CAS refs. |
| `gate.failed` | 1 | gate engine | A verifier failed. Payload carries the failing verifier and its evidence CAS refs. |
| `gate.inconclusive` | 1 | gate engine | A verifier was inconclusive (timeout, nonzero exit, malformed output). Routed to a human; never coerced to pass. |
| `gate.explained` | 1 | gate engine | An interrogation (`gate why` / `gate_explain`) was answered: failing verifier, evidence refs, exact inputs. |

### artifact

| Subject | v | Emitter | Semantics | Payload sketch |
|---|---|---|---|---|
| `artifact.captured` | 1 | any capturing component (run capture, gate evidence, git adapter) | Bytes were persisted to the CAS; the fabric carries the ref only (I2). | `{ref: CasRef, provenance}` — ratified below; the v0 sketch's top-level `mime`/`bytes` ride inside the ref |

### diff

| Subject | v | Emitter | Semantics |
|---|---|---|---|
| `diff.ready` | 1 | git adapter (notify watcher, debounced 250 ms) | A diff summary for a worktree is ready as a CAS ref. S2 acceptance: within 1 s of write, post-debounce. |
| `diff.merged` | 1 | git adapter (RepoSubstrate, git CLI mutations) | A verified diff was merged into the target branch and the worktree lifecycle closed. Emitted only after the `pre_merge` gate `gate.passed` verdict (golden-path exit). Distinct from `merge.completed`: `diff.merged` is the worktree-lifecycle fact keyed on `{run, worktree, diff}` that the S4 reducer folds to `status = "merged"`; `merge.completed` is the v0 merge-mutation fact. |

### merge

| Subject | v | Emitter | Semantics |
|---|---|---|---|
| `merge.completed` | 1 | git adapter (git CLI mutations) | A diff was merged. |
| `merge.rejected` | 1 | git adapter | A merge attempt was rejected. |

### adapter

| Subject | v | Emitter | Semantics |
|---|---|---|---|
| `adapter.health.changed` | 1 | supervisor (rezidnt-supervise) | State delta: an adapter's health transitioned (`Starting → Healthy → Degraded → Faulted`, doc §7). The crash-loop breaker parks an adapter in `Faulted` visibly — never a silent retry storm. |

### daemon

WARN and above from `tracing` are mirrored onto the fabric as `daemon.*` events so the system's own misbehavior is queryable with the same tools as everything else (doc §14).

| Subject | v | Emitter | Semantics |
|---|---|---|---|
| `daemon.started` | 1 | daemon core | The daemon started. |
| `daemon.warning` | 1 | daemon core | A WARN-level condition, mirrored from tracing onto the fabric. |
| `daemon.error` | 1 | daemon core | An ERROR-level condition, mirrored from tracing onto the fabric. |

### badge

**Operator badge (S3 note):** doc §12 badges are per-`AgentRun` capability tokens; S3 additionally mints one daemon-lifetime **operator badge** for local human clients, announced via the 0600 MCP lockfile (`{pid, port, url, badge}`) — possession of the file is possession of the capability. This is a §12/DEFAULT security-layer reading, pinned by the S3 board; the lockfile shape itself is discovery metadata, **not fabric surface**, and is documented at doc level (scribe scope). The concept is blessed here only insofar as `badge.*` semantics must eventually accommodate a badge scoped to the daemon lifetime rather than a run. DR-005 (owner-ratified 2026-07-17) settled the coupled badge questions: the operator badge is blessed as a distinct daemon-lifetime badge class (DEFAULT), and the `badge.issued` emit-or-drop question is **decided — drop the emitter** (attribution rides `agent.spawned.badge_id`; see the `badge.issued`/`badge.revoked` rows below).

**§12 badge rule (out of ontology scope — recorded for cross-reference only):** DR-005 also narrows the §12 rule to "a badge is required on every **state-mutating** call" (spawn, open, merge), with interrogation (`gate_explain`, `gate why`) and `tail_events` treated as **read-class/unbadged** so a blocked agent can always read *why* it was blocked (I6). That rule lives in doc §12, not in this taxonomy — the ontology carries subjects and payload shapes, not the call-badging policy — so no ontology entry is minted for it; it is noted here purely so the badge section and DR-005 do not read as divergent.

| Subject | v | Emitter | Semantics |
|---|---|---|---|
| `badge.issued` | 1 | **none in v1 (DR-005)** | Reserved subject: a per-run capability badge is minted (256-bit token scoped to `{workspace, verb set, expiry}`, doc §12, injected at spawn per DR-001) — but this subject has **NO emitter in v1**. Issuance is attributable via `agent.spawned.badge_id` (DR-005; `bins/rezidentd/src/runs.rs`), so no separate `badge.issued` fact is emitted. Subject stays reserved (never renamed); the emitter is DEFAULT-droppable and cheap to revisit if a run-independent delegation use case appears. |
| `badge.revoked` | 1 | **none in v1 (DR-005)** | Reserved subject, consistent with `badge.issued`: **NO emitter in v1** (no revocation path exists as built). Revocation would mean mutating calls bearing the badge are refused thereafter. Reserved (never renamed); an emitter arrives only if/when revocation is built. |

### integrity

Log/replay integrity is its own axis, distinct from gate verdicts (`gate.*`) and daemon lifecycle (`daemon.*`). DR-006 (owner-ratified 2026-07-17) chose a **dedicated subject** for replay divergence over reusing the broad `daemon.error` bucket AND over polluting the gate vocabulary — the integrity-of-log signal (recorded ≠ replayed) must be precisely queryable, and integrity-of-log ≠ gate verdict. This new top-level noun is the ratified intent, not a folding under `gate.*`.

| Subject | v | Emitter | Semantics |
|---|---|---|---|
| `integrity.alarm` | 1 | daemon (single writer; DR-006 routes the `debrief` append through the daemon's sole log writer, I3) | A recorded verdict on the log diverged from the verdict re-derived on replay (`debrief` replay over log + CAS). Recorded ≠ replayed means verifier nondeterminism (a verifier bug) or an altered log (§12); raised, named, never silently reconciled. Lands a **durable fact** on the log (DR-006) — strictly additive to the CLI `debrief --json` report (`report.alarms[]`, exit 3) which stays correct. Folded by `rezidnt-state` into `agent_runs[run].integrity_alarms`, so it is **not** a dead-letter subject. |

### permit

The **pre-hoc authorization axis** (DR-008 permit-engine pivot; scope extended by DR-009). rezidnt is the in-daemon Policy Decision Point (PDP); the harness `PreToolUse` hook is the Policy Enforcement Point (PEP). A `permit`-verifier is a verifier on the new `permit` gate lifecycle point — **the gate engine *is* the policy engine** (DR-008); so a permit decision is a gate verdict in disguise, but on a **distinct axis** (the "may" axis) from the `gate.*` verdict facts (the "did/proved" axis), keyed on the **action request**, not the gate-run.

The verdict contract maps to authorization with **zero new vocabulary**, honoring I6 — verdict is carried by the **subject itself** (as `gate.*` does), never a bare boolean, and `inconclusive` is **never coerced** (DR-008; design §4):

| Verdict | Permit subject | Meaning |
|---|---|---|
| `pass` | `permit.granted` | **allow** |
| `fail` | `permit.denied` | **deny** |
| `inconclusive` | `permit.escalated` | **escalate to a human** (routed to a client, never auto-resolved) |

There is deliberately **no `permit.allowed` synonym** and no subject that coerces an inconclusive to allow (I6). All four subjects fold into materialized state (no consumer-less subjects — DR-006 precedent): a per-run permit **ledger** (request → decision, correlated) plus per-**session accumulators** (running risk score, cumulative spend, tool-use-vs-intent) that the later contextual/stateful C1/C7 permit-verifiers read (design §7; intel memo 001 C6/C7 sizing note). This session mints the subjects + reducer scaffolding only; the verifiers themselves are SP0–SP4 work.

| Subject | v | Emitter | Semantics |
|---|---|---|---|
| `permit.requested` | 1 | harness PEP via `request_permission` (MCP tool / socket, I5); recorded by the daemon PDP | An agent (via the harness PEP) asked to perform an action. Carries the run, a small inline action/target descriptor, and a CAS ref for any large action context (args/diffs/file bytes) — never inline (I2). The `correlation` opens the request→decision chain each decision fact points back at. |
| `permit.granted` | 1 | permit gate engine (PDP; DR-008) | Decision = **allow** (`pass`). Carries `policy_ref` (the deciding permit-verifier/policy) and `evidence_ref` (CAS) so `gate why` / `gate_explain` can explain the grant (I6 interrogability). |
| `permit.denied` | 1 | permit gate engine (PDP; DR-008) | Decision = **deny** (`fail`). Carries the deciding `policy_ref` + `evidence_ref` (CAS) and a short `reason` so a blocked agent can always read *why* (I6). |
| `permit.escalated` | 1 | permit gate engine (PDP; DR-008) | Decision = **inconclusive → routed to a human** (`ask`). Never coerced to allow (I6, DR-008 §4). Carries `policy_ref` + `evidence_ref` (CAS) so the human sees the deciding policy and evidence. |
| `permit.delegated` | 1 | daemon PDP, when a lead agent's badge is attenuated for a sub-agent spawn (DR-017 §Decision 2; SP4b macaroon-attenuated delegation) | A capability was **narrowed and handed off**: a lead agent's macaroon badge was attenuated to mint a child badge for a sub-spawn. The attenuation is a **real `Macaroon::attenuate` at the daemon boundary** (DR-018 §Decision (b)) — the daemon calls `attenuate`, not `mint`, so a narrowing caveat **is** appended and the running MAC sig **is** re-keyed without the root key (offline-of-the-root-key: the running sig, not the root key, is the next hop's key — the delegation property, design §4). What is **deferred to a later SP4 sub-slice** (DR-018 §Decision (b)) is the *holder-offline process boundary* — a lead-agent process performing the attenuation itself with no daemon involvement; in SP4b's shipped scope the single daemon holds the base badge and attenuates on the sub-spawn path, so this fact must not be read as holder-offline delegation. A **new event in time** on the `permit.*` "may" axis (contrast the SP4a spawn-time `agent.spawned.role?` field) — it earns a row because the capability chain is a durable fact that must replay (I3). Carries the parent/child `badge_id`s (loggable, never the token) and the added caveats. Folds onto the run's dossier so the delegation chain is queryable/replayable. |

### run

The **run-intent axis** (DR-010, ACCEPTED 2026-07-18; design `docs/design/intent-lock.md`). A run's *initiating intent* and the intent-derived least-privilege tool set the future `intent-lock` permit-verifier (SP-intent) enforces. Keyed on the `run` RunId ULID — the same key discipline `agent.*`/`permit.*` fold on — so the intent folds onto the run's dossier (`AgentRunState`), not a new entity. This is a **distinct axis** from `agent.spawned.allowed_tools?`: that records the *composed harness allowlist* (what the agent was configured with — a governance-composition fact); `run.intent.declared.allowed_tools` records the *intent-derived least-privilege set* the verifier checks a requested tool against (DR-010 §4). A distinct axis warrants a distinct subject (DR-006 precedent: `integrity.*` not folded into `gate.*`). The `run` noun is new; the run's *lifecycle* facts remain grandfathered under `agent.*`, and `run.intent.declared` mints the run-intent axis rather than fracturing it — the subject folds onto the existing `AgentRunState` keyed on `run`, so the two nouns describe one entity, one dossier. This session mints the subject + its folding reducer only; the `intent-lock` verifier is SP-intent (oracle-first, next slice).

| Subject | v | Emitter | Semantics |
|---|---|---|---|
| `run.intent.declared` | 1 | the run/spawn intent-declaration path (SP-intent; project spec `[gates.permit]` / agent spec, design §3(a)) — **minted by SP-intent, not this session** | A run declared its initiating intent and the intent-scoped tool set the `intent-lock` verifier enforces (least-privilege in time). The intent text is a CAS ref (never inline; I2); the allowed-tool set is a short string list. Folds to a rebuild-stable per-run intent state (`AgentRunState.intent`) the verifier reads as a pinned input (I3; DR-010 §4). |

### action

The **post-action metering axis** (DR-021, ACCEPTED 2026-07-20; C1 live spend-cap build). A per-**action** metering fact carrying the **measured** actual resource cost of a single action *after it ran*. This is the honest source (I3) for the C1 spend-cap fold that DR-021 §Decision B2 moved OFF the pre-action `permit.*` decision: a permit decision fires *before* the action and folds NO spend; measured actuals ride this fact instead, so the folded `cumulative_spend_usd` is *measured incurred* spend, never a pre-action estimate or a phantom charge on a denied action. Keyed on the `run` RunId ULID — the same key discipline `agent.*`/`permit.*`/`run.*` fold on — so it folds onto the run's dossier (`AgentRunState`), not a new entity. The `action` noun is **new**, minted per the DR-006/DR-008/DR-010 "distinct axis → distinct noun" precedent (integrity/permit/run were minted rather than folded into an existing noun). This session mints the subject + payload schema + reducer-arm obligation; the fold implementation and the DR-021 §8/§9 PDP-injection wiring are the implementer's C1 work (oracle-first), NOT written here.

| Subject | v | Emitter | Semantics |
|---|---|---|---|
| `action.metered` | 1 | the harness PEP / run substrate post-action metering path (C1; DR-021 §Decision A→option 3) — **emitter minted by the C1 build, not this session** | A single action completed and its **measured** actual resource cost was recorded, *after the action ran* (post-action attribution, DR-021 B2). Carries measured dollars spent and token counts at per-**action** grain — distinct from `agent.completed.cost`, which is the per-**run** terminal total (too coarse for a per-action spend-cap fold). Folds its measured `spend_delta_usd` into `AgentRunState.permit_accumulators.cumulative_spend_usd` (the fold-source that moved off `permit.*`; DR-021 §reducer). Scalar $ + token counts, no bulk payload (I2). |

### egress

The **egress-mediation axis** (DR-029, ACCEPTED 2026-07-21; the paired warden mint DR-029 §Decision 6 required, replacing the DR-025/026/028 PLACEHOLDER strings). This noun carries the C3 network-confinement facts a governed run produces: the composed **run-posture** fact (confined + how the network is confined) and the per-connection **denied** fact. The sandbox posture is a **field** on the posture fact (`sandbox: "available" | "unavailable"`), NOT a parallel `sandbox.*` subject: `compose.rs::compose_degrade` decides ONE composed state (the product of C3a's sandbox availability and c3bc's egress availability), so the single decision rides a single fact — splitting it across `sandbox.*` + `egress.*` would fracture one decision into two subjects. This mirrors how `pep?`/`role?` (spawn-time postures) ride as fields on `agent.spawned` rather than minting their own subjects.

Keyed on the `run` RunId ULID — the same key discipline `agent.*`/`permit.*`/`run.*`/`action.*` fold on — so these facts fold onto the run's dossier (`AgentRunState`), NOT a new entity. The `egress` noun is **new**, minted per the DR-006/DR-008/DR-010/DR-021 "distinct axis → distinct noun" precedent. The degrade contract is **CLOSED and loud** (DR-026 §Decision, I6): an unavailable backend NEVER opens unmediated egress and NEVER injects — it keeps the sealed netns and announces `egress.unavailable`; an unsandboxed spawn declares egress un-enforceable rather than silently claiming mediation it lacks (the overclaim the threat model forbids). This session mints the subjects + payload schemas + the reducer-arm obligation; the emitter swap (replacing the placeholders in `compose.rs`/`egress.rs`/`runs.rs`) and the fold are the C3 implementer's `c3-egress-fold` work (oracle-first), NOT written here.

| Subject | v | Emitter | Semantics |
|---|---|---|---|
| `egress.mediated` | 1 | `rezidnt-run` composed spawn path (`compose.rs::degrade_fact`, `ComposedDegrade::Mediated`) — **emitter swap is C3 implementer scope** | The composed run is confined AND egress-mediated: pasta-outer sealed the userspace netns and bwrap confined the filesystem on the SAME shared netns, so the run's sole route out is the folded proxy (DR-028 §Decision 1). The **enforcing** posture, not a degrade — its subject carries no `*.unavailable` marker. Replaces the `sandbox.mediated` placeholder. |
| `egress.unavailable` | 1 | `rezidnt-run` composed spawn path (`compose.rs::degrade_fact`, `ComposedDegrade::ConfinedClosed` AND `ComposedDegrade::Unsandboxed`) — **emitter swap is C3 implementer scope** | The composed run is NOT mediating egress, and the fact is loud about which floor it landed on (`sandbox` field distinguishes the two): **confined + CLOSED** (`sandbox="available"`) — the sandbox held but the egress backend was down, so the sealed netns is KEPT with no network and nothing injected (DR-026 CLOSED degrade); or **unsandboxed** (`sandbox="unavailable"`) — the sandbox itself was down, so there is no sealed netns for egress to be the sole route out of and mediation is un-enforceable (the load-bearing sandbox-first dependency, DR-028 §Decision 4). Either way `egress_enforceable=false` and `injected=false` — no silent claim of mediation. Replaces BOTH the `egress.unavailable` and `sandbox.unavailable` placeholders (one subject; the `sandbox` field carries the distinction). |
| `egress.denied` | 1 | the egress dataplane / run-loop mediation decision (`egress.rs::Mediation::Deny`; the `EgressScope` `fail` verdict at the run-loop level, DR-026 crit 3) — **emitter swap is C3 implementer scope** | An outbound connection to an **off-allowlist** destination was denied-and-logged (a durable, replayable enforcement decision — the `fail` half of the mediation verdict). A per-**connection** event in time (many per run), distinct from the once-per-run `egress.mediated`/`egress.unavailable` posture fact. Carries the denied `dest` and the deciding `policy_ref` so `gate_explain` can answer *why denied* (I6). |

### credential

The **brokered-credential axis** (DR-029, ACCEPTED 2026-07-21; paired mint, DR-029 §Decision 6, replacing the DR-026 PLACEHOLDER). A distinct noun from `egress` because a brokered secret is its own entity — a `secret_ref` — whose lifecycle (injected upstream / dropped-because-unresolvable) is a distinct axis, and whose **leak-discipline is a property of this noun specifically**: every `credential.*` fact carries ONLY the `secret_ref` (a label/hash), the destination, and a `policy_ref` anchor — **NEVER the secret value** (I2/I3; DR-026 crit 5). The value is redacted at the type level in `rezidnt_run::egress::BrokeredSecret` (`Debug`/`Display` print `"<redacted>"`; the value is reachable only through the single audited `expose()` on the upstream-write path), so a value literally cannot ride these facts: the fact constructors take a `&BrokeredSecret` and lift only `secret_ref()`, never `expose()`.

Keyed on the `run` RunId ULID (same key discipline as `egress.*`/`permit.*`) — folds onto the run's dossier (`AgentRunState`), not a new entity. The `credential` noun is **new**, minted per the same distinct-axis-→-distinct-noun precedent. This session mints the subjects + payload schemas + the reducer-arm obligation; the emitter swap (`egress.rs::injected_fact` + the drop path DR-029 §Decision 2 introduces) and the fold are C3 implementer scope, NOT written here.

| Subject | v | Emitter | Semantics |
|---|---|---|---|
| `credential.injected` | 1 | the egress dataplane upstream-injection point (`egress.rs::injected_fact`, post-TLS-termination on the plaintext the agent never sees) — **emitter swap is C3 implementer scope** | A brokered secret was injected into the UPSTREAM request on an approved mediated egress (DR-026 crit 4). The PDP records THAT a secret was injected and WHICH (its `secret_ref`) + WHERE (`dest`) + under which policy (`policy_ref`) — **never the value** (the by-reference contract; the value is exposed only at this injection point, never on the fabric). Replaces the `credential.injected` placeholder. |
| `credential.dropped` | 1 | the daemon-side fold / `SecretSource` resolution path (DR-029 §Decision 2 — `secret_ref` the source cannot resolve) — **emitter minted by the C3 implementer, not this session** | A `secret_ref` declared in the `[egress.secrets]` map could NOT be resolved by the `SecretSource`, so that host→secret mapping was DROPPED with a loud fact — never a fake/empty secret, never an injection of the empty string (the honest floor, DR-029 §Decision 2). The run still proceeds mediated for hosts whose secrets resolved. A `credential.*` variant (not an `egress.*` degrade) because the dropped thing is a **credential** identified by its `secret_ref`; the fact carries the unresolvable `secret_ref` + the `dest` it was mapped to + a `reason` so *why uninjected* is interrogable (I6). Carries no value (there is none — that is the point). |

## Payload schemas — v1 baselines

Ratified per-subject payload shapes. A field marked `?` is optional and may be absent; readers tolerate unknown fields (additive evolution, doc §5). Types map JSON onto the rezidnt-types Rust shapes; `CasRef` is `{hash: blake3 hex string, bytes: u64, mime: string}`. Subjects not listed here have no ratified payload schema yet — their shape is proposal-stage until a warden session ratifies it. These baselines define v = 1; a breaking change to any of them mints v+1 per the change discipline above.

### S1 set (ratified 2026-07-16)

**`workspace.opened` v1** — the envelope `workspace` id is the entity key (reducers fold on it); the payload carries human-facing identity.
- `name: string` — workspace name from the project spec.
- `root: string` — canonicalized absolute path of the workspace root.

**`workspace.spec.applied` v1**
- `spec_ref?: CasRef` — the applied spec file as persisted to the CAS; optional because an open may apply an inline/default spec with no persisted blob.
- `agents: [string]` — spec agent names configured by the applied spec.

**`worktree.allocated` v1**
- `path: string` — canonicalized worktree path; exists on disk at emission time.
- `branch?: string` — branch checked out in the worktree, when one was requested.
- `allocator: "rezidnt"` — sole-allocator model (DR-001). The value `"human"` is reserved for out-of-band observation and is never emitted by rezidnt on this subject.

**`agent.spawned` v1**
- `run: string` — RunId ULID; the key every `agent.*` fact carries.
- `agent: string` — spec agent name.
- `harness: string` — harness identifier (e.g. `claude-code`).
- `harness_version?: string` — as probed at spawn; version-gated per adapter.
- `pid?: u32` — OS process id when known at emission.
- `badge_id: string` — loggable badge identifier. NEVER the badge token (doc §12); the token exists only in the spawned environment. For a macaroon (agent) badge, this id is derived from the running MAC sig — `hex(blake3(sig)[..8])` (DR-018 §Decision (a)), not `blake3(identifier)[..8]`; see the `permit.delegated` id fields for the rationale (a true `attenuate` preserves the identifier, so a sig-derived id is what distinguishes parent from child edge ids). This is distinct from the DR-005 daemon-lifetime **operator** badge (§ badge / doc §12), which is not a macaroon and whose id derivation DR-018 does not touch.
- `idempotency_key?: string` — caller-supplied spawn idempotency key, recorded on the fact so the key→run map is log-derivable (I3; S3-T1 remediation, pre-S4). Present iff the spawning call supplied one: MCP `spawn_agent` requires a key (`SpawnAgentArgs.idempotency_key`), other spawn paths carry none — optional is the honest shape, never synthesized for keyless spawns. Constraints: non-empty, ≤ 256 bytes UTF-8 (DEFAULT cap; a key is a short opaque token, trivially inside I2). Opaque to the daemon beyond byte equality. Dedup scope is per workspace — the envelope `workspace` id paired with the key: a spawn request bearing a key already recorded on an `agent.spawned` fact for that workspace answers with that fact's `run` and emits nothing new, including across daemon restart. Emitter obligation: a keyed spawn fact MUST set the envelope `workspace` field, or the rebuilt map has no scope. **Added 2026-07-17 (additive; v stays 1).**
- `bare?: bool` — whether the spawn was governed as a *bare* agent (no interactive/permission-prompt affordances): the enforcement decision the `vet` gate's `bare-mode` verifier checked, recorded on the fact so the governance posture is log-derivable (DR-001: enforcement decisions recorded in events; I3). Optional/additive: present on governed spawns that ran through `vet`; absent on legacy or ungoverned spawn paths (never synthesized — absence is honest, not `false`). **Added 2026-07-17 (S4; additive; v stays 1).**
- `allowed_tools?: [string]` — the composed allow-list of tool names the agent was spawned with, as enforced pre-spawn — the permission composition the `vet` gate's `allowed-tools` verifier checked (DR-001: allowedTools recorded in events; I3). Records what was granted, for log-derivable attribution of what the agent *could* do vs. `agent.tool.invoked` facts for what it *did*. Optional/additive: present on governed spawns; absent when no tool policy was composed. A short list of short strings — trivially inside I2; a pathological policy stays a CAS concern only if it ever exceeded the envelope cap (it does not for tool-name lists). **Added 2026-07-17 (S4; additive; v stays 1).**
- `pep?: "enforced"` — **enforcement-mode visibility (DR-014 §Decision 5, Design §6).** Records whether this run had the permit **Policy Enforcement Point** (the `rezidnt permit-hook` `PreToolUse` hook) wired at spawn, so `gate_explain` can honestly distinguish a *mid-run-PEP-enforced* run from an *edge-gated-only* run (I4 degradation honesty). The value is fixed at spawn — opt-in is keyed on the spec declaring a `[gates.permit]` gate (DR-014 §Decision 2; Design §3), and the enforcement mode **never changes after spawn**, so this is a spawn-time property (a field), not a lifecycle event of its own. **Value semantics (DR-012 declared-vs-absent discipline):** `"enforced"` — present iff `SpawnPlan::for_claude_code` wired the PEP (the run's spec declared a `[gates.permit]` gate); mid-run tool calls are intercepted and asked of the daemon PDP. **Absent** — the run was **edge-gated-only**: no `[gates.permit]` gate, or a legacy/ungoverned spawn path that wired no hook; it still gets pre-spawn `vet` + post-hoc `debrief` evidence but **no mid-run interception**. Absence is the honest representation of "no PEP wired" — never synthesized to a `false` / `"unenforced"` value (exact mirror of the `bare?` / `allowed_tools?` absent-is-honest discipline; DR-012 keeps declared-empty distinct from absent, but here there is no "declared-empty" state — the PEP is wired or it is not, so a single present value + honest absence is the correct shape). An **enum string, not a bool**, so a future degraded/partial enforcement mode (e.g. hook present but socket unreachable at spawn) can arrive additively as a new value without a `v+1` (same additive-string discipline as `agent.status.changed` / `gate.inconclusive.reason`); in v1 the only value any emitter produces is `"enforced"`. Independent optional — does not collide with `bare?` / `allowed_tools?` / `harness_version?` / `idempotency_key?` / `badge_id`. **Added 2026-07-18 (DR-014; additive; v stays 1).**
- `role?: string` — **RBAC role — permit input axis (DR-016 §Decision 2, Design §4).** The agent's authorization role, taken verbatim from `AgentSpec.role` (`role: Option<String>`, DR-016 §Decision 2), recorded on the spawn fact so permit policies can key decisions on **role + workspace + action** (permit-engine §7). Injected into `decide_permit`'s content-pinned per-run params (DR-011 §2) as a **new input axis** both native and exec permit verifiers read — no new verifier machinery, a new input field on the existing SP1–SP3 dispatch. Like `pep?`, this is a **spawn-time property of the run** (the role is fixed by the spec at spawn and rides the run's derived state), not a lifecycle event of its own — a field, not a subject. **Value semantics (DR-012 declared-vs-absent discipline):** an **opaque string the policy interprets** (rezidnt mints no role vocabulary — DR-016 does not design the role taxonomy); present iff `AgentSpec.role` declared one. **Absent** — the spec declared **no role**; the run has no RBAC role and a role-keyed policy sees no role to match. Absence is the honest representation of "no role declared" — **never synthesized to a default** like `"contributor"` (exact mirror of the `bare?` / `allowed_tools?` / `pep?` absent-is-honest discipline; DR-012 declared-vs-absent — a role-less agent is not a `"contributor"` agent). A short scalar (a role name is a short opaque token, trivially inside I2 — no CAS ref, no accumulator). Independent optional — does not collide with `pep?` (enforcement *mode*) / `bare?` / `allowed_tools?` (composed tool allowlist) / `harness_version?` / `idempotency_key?` / `badge_id`; `role?` records *who the agent is authorized as*, a distinct axis from all of them. **Added 2026-07-19 (DR-016 SP4a; additive; v stays 1).**
  - Note on `harness_version`: already ratified above as `harness_version?: string` (S1 baseline). The `vet` gate's `pinned-version` verifier reads/enforces it; no new field is minted for version pinning — the existing optional field carries the recorded decision. Cross-referenced here so the governed-spawn set (`bare`, `harness_version`, `allowed_tools`, `pep`, `role`) reads together.
- Interaction check (recorded 2026-07-17; extended 2026-07-18 for `pep`, 2026-07-19 for `role`): the governed-spawn fields (`bare?`, `allowed_tools?`, `harness_version?`, `pep?`, `role?`) are mutually independent optionals and do not collide with `idempotency_key?` (dedup key) or `badge_id` (attribution). The `badge.issued` emit-or-drop and `badge_id`-on-mutation-facts deferrals are untouched and not foreclosed — no field added here occupies a name or scope reserved for those decisions. `pep?` records a *spawn-time enforcement property* (was the PEP wired), a distinct axis from `allowed_tools?` (what tools were composed) — the two answer different `gate_explain` questions and never coincide. `role?` records the *RBAC role the run is authorized as* (a permit input axis, DR-016) — distinct again from `pep?` (enforcement *mode*, not identity) and from `allowed_tools?` (a *tool* allowlist, not a *role*); a policy reads `role` + workspace + action, not the tool set.

**`agent.status.changed` v1** — state delta.
- `run: string`
- `from: string`, `to: string` — run-status vocabulary: `spawning | running | completed | failed | signaled`.

**`agent.completed` v1** — dossier accounting (DR-001).
- `run: string`
- `status: "success" | "error"` — the harness result outcome. This is a distinct vocabulary from the run-status values of `agent.status.changed` (`completed`/`failed`); reducers must not conflate the two.
- `cost: {total_usd: f64, input_tokens: u64, output_tokens: u64}`
- `num_turns: u64`
- `duration_ms: u64`
- `session_id?: string` — harness session id, captured for run checkpointing (`--resume`, DR-001).

**`agent.signaled` v1**
- `run: string`
- `signal: string` — the delivered signal name (e.g. `SIGTERM`, `SIGKILL`).
- `escalation?: "term" | "kill"` — present when the signal came from the reaper's TERM→KILL escalation path, recording the stage; absent for out-of-band signals.

**`agent.tool.invoked` v1** — harness telemetry (DR-001).
- `run: string`
- `tool: string` — tool name as reported by the harness.
- `input_summary?: string` — truncated human-readable summary of the tool input; bulk input goes to the CAS and rides `artifact.captured`, never inline (I2).

**`agent.message` v1** — harness telemetry (DR-001). Carries exactly one of `text` / `ref`.
- `run: string`
- `role: "assistant"` — fixed in v1; other roles arrive additively.
- `text?: string` — inline only when ≤ 8 KiB (DEFAULT cap; keeps envelope headroom under the 32 KiB I2 hard cap).
- `ref?: CasRef` — bulk message body persisted to the CAS.

**`artifact.captured` v1**
- `ref: CasRef` — carries `hash`, `bytes`, `mime`; the v0 sketch's top-level `mime`/`bytes` live inside the ref (no duplicated fields).
- `provenance: {run?: string, kind: string, chunk?: u64}` — `run` when the artifact belongs to an agent run; `kind` names the capture class (e.g. `capture-chunk`, `diff`, `gate-evidence`); `chunk` is the 0-based ordinal within the run's capture stream, present iff `kind = "capture-chunk"`. DR-001 chunked run output rides this subject via ref-only manifest facts (`rezidnt-run` `ManifestEntry {run, chunk, ref}`); whether high-rate capture chunks deserve a dedicated subject is an open question flagged for `/dr` — not minted here.

### S2 set (ratified 2026-07-17)

**`worktree.observed` v1** — out-of-band guard fact (DR-001); registers the tree so re-observation stays silent.
- `path: string` — canonicalized path of the observed tree; the registry key it occupies.
- `allocator: "human"` — fixed in v1: observation is by definition out-of-band. The value `"rezidnt"` is never emitted on this subject (exact mirror of the `worktree.allocated` reservation).
- `branch?: string` — branch checked out in the observed tree when the watcher can read one; absent for detached human trees. Additive; family coherence with `worktree.allocated`.

**`worktree.conflict` v1** — emitted instead of double-tracking. **Emission is at-least-once, not exactly-once (honest wording, S2 re-debrief T2).** The adapter emits the conflict fact and *then* persists the `conflicted` dedup mark (`crates/rezidnt-adapters/git/src/lib.rs`: in `observe`/`allocate`/`discover` the sequence is set-flag → `emit("worktree.conflict")` → `persist_registry`). In the crash-free path this is exactly-once: the persisted mark suppresses re-emission on the next collision, forever. But a crash *between* the emit and the registry persist loses the mark, so on restart the next claim on the same path re-emits — hence at-least-once across a crash. Reducer obligation (I3): each logged fact is folded, and a downstream consumer treats `worktree.conflict` as idempotent per contested path (dedup on `path`), because the log is the source of truth and may honestly carry a duplicate the crash produced. This describes the code's real semantics; it is a wording correction, not a design change.
- `path: string` — the contested **canonicalized registry key** (the path as registered, not the colliding spelling).
- `claimed_path?: string` — the colliding claim as observed, pre-canonicalization, present when it differs from `path`. Triage evidence: names the spelling that collided without a registry read.
- `holder?: "rezidnt" | "human"` — allocator recorded on the standing registry entry at collision time, making the fact self-contained for triage. (Named `holder`, not `allocator`: on this subject "allocator" would be ambiguous between the entry's holder and the out-of-band claimant.)

**`worktree.released` v1**
- `path: string` — canonicalized registry key; byte-identical to the spelling the allocation minted (both are canonicalized, so the strings agree — the release fact closes exactly the entry the allocated fact opened).
- `branch?: string` — branch the worktree carried, when it had one. Additive; lets a consumer of the release fact alone know what went away, family coherence with `worktree.allocated`.

**`diff.ready` v1** — I2 contract: the diff summary is a CAS ref, never inline diff bytes.
- `worktree: string` — canonicalized path of the worktree the diff concerns (the same registry key `worktree.allocated` minted).
- `diff: CasRef` — the diff summary persisted to the CAS; resolvable at emission time. `mime` is `text/x-diff` (DEFAULT, not load-bearing); a real tree change yields a non-empty summary (`bytes > 0`).

### S3 set (ratified 2026-07-17)

Strict supersets of the S3 oracle pins (`rezidnt-mcp/tests/gate_explain.rs`, `bins/rezidentd/tests/mcp_http.rs`, `spec/fixtures/s3_gate_forced_failure.jsonl`, `s3_gate_inconclusive.jsonl`). The verdict is carried by the **subject itself** (`gate.failed` → `fail`) — never a bare boolean field, and `inconclusive` is never coerced (I6). The full verifier engine is S4; these baselines cover exactly what S3's forced-failure / `gate_explain` slice emits and interrogates, and S4 extends them additively (e.g. network-opt-in recording, `cost_ms`). `gate.passed` has no S3 emitter or pin and its baseline is deliberately **not** ratified here — S4 scope.

**`gate.entered` v1** — the envelope `correlation` groups the gate run (doc §5); `causation` on subsequent verdict facts points back at this fact.
- `run: string` — RunId ULID; the key every gate fact carries.
- `gate: string` — the named policy point (`vet`, `pre_merge`, `post_run`); a string, not a closed enum — gate defs are named policy points (doc §8).

**`gate.failed` v1**
- `run: string`, `gate: string` — as `gate.entered`.
- `verifier: string` — the **failing** verifier's name (doc §8; exactly what `gate_explain` must return).
- `evidence: [CasRef]` — evidence blobs live in the CAS; the fact carries refs only (I2, doc §8 BINDING).
- `inputs: object` — the exact verifier input document, recorded **verbatim** (doc §8 stdin contract: `{gate, refs, params, timeout_ms}`; `refs` values are `cas:blake3:<hex>` strings — inputs pinned by content hash, per the determinism BINDING). Deliberately an opaque-but-recorded object, not a decomposed schema: `gate_explain` returns it byte-for-byte from the log (I6 interrogability), and the S4 engine widens the document additively without a payload break here.

**`gate.inconclusive` v1** — same shape as `gate.failed` plus `reason`; routed to a human, never coerced to `pass` (I6).
- `run`, `gate`, `verifier`, `evidence: [CasRef]`, `inputs` — exactly as `gate.failed`.
- `reason: string` — v1 vocabulary from the §8 causes: `timeout | nonzero_exit | malformed_output | could_not_run`; new causes arrive additively as strings.
  - `could_not_run` — the verifier program could not be executed (spawn/exec failure, e.g. argv[0] points at a nonexistent binary) so **nothing ran**; distinct from `malformed_output`, which is a program that **did** run but emitted unparseable output. The verdict stays `Inconclusive` — a verifier that cannot run is never coerced to `pass` (I6). Wire form is the snake_case string `"could_not_run"` (pinned by `crates/rezidnt-gate/tests/exec_could_not_run.rs`). **Added 2026-07-17 (additive string under the existing "new causes arrive additively" rule; `gate.inconclusive` stays `v = 1`).**

**`gate.explained` v1** — interrogations are facts too. The explanation content (verifier, evidence, inputs) is **derived** from the verdict fact already on the log (I3) and is not duplicated into this payload.
- `run: string` — the interrogated run; the pinned minimum is this field alone.
- `gate?: string`, `verdict?: "pass" | "fail" | "inconclusive"` — optional self-contained triage context; when present, `verdict` is the recorded verdict verbatim (never coerced, I6).

### S4 set (ratified 2026-07-17)

Strict supersets of the S4 oracle pins: `crates/rezidnt-gate/tests/`, `crates/rezidnt-state/tests/s4_gates.rs`, `bins/rezidentd/tests/{vet_gate.rs,golden_path.rs}`, and the S4 fixtures `spec/fixtures/s4_{verified_run,vet_refusal,replay_verified,replay_divergence_alarm}*`. Additive-only; every subject stays `v = 1`. The `inputs` document is recorded **verbatim** per the §8 stdin contract and is measured at ~182 bytes for the largest S4 pin (a `pre_merge` diff-scope record with a glob `allow` list); a full 3-verifier `gate.passed` payload is ~838 bytes — three orders of magnitude under the 32 KiB I2 hard cap. Tool-name lists and glob params are short strings; larger content is already a CAS ref by the §8 contract, so no S4 payload approaches the cap.

**`gate.passed` v1** — the S3 baseline deliberately left this unratified ("until an emitter exists"); the S4 engine is that emitter. Now ratified.
- `run: string`, `gate: string` — as `gate.entered`.
- `verifiers: [VerifierRecord]` — one record **per verifier** that ran on the gate, in execution order. Replay re-executes each verifier against its own recorded inputs, and the slice exit requires per-verifier recorded cost — so the evidence is carried per verifier, not flattened on the event. Each `VerifierRecord` is:
  - `verifier: string` — the verifier's name (§8).
  - `cost_ms: u64` — wall-clock cost of this verifier's execution (the §8 stdout `cost_ms`); the "recorded cost" the golden-path exit asserts.
  - `evidence: [CasRef]` — this verifier's evidence blobs as CAS refs (I2); empty when the verifier emitted none. The reducer flattens all records' evidence hashes in order into `GateState.evidence`.
  - `inputs: object` — the exact per-verifier §8 stdin document, recorded verbatim (`{gate, refs, params, timeout_ms}`; `refs` values are `cas:blake3:<hex>` — inputs pinned by content hash, determinism BINDING). Same opaque-but-recorded discipline as `gate.failed.inputs`; widened additively by the engine without a payload break.
- **Asymmetry with `gate.failed` v1 (rationale, ratified):** `gate.failed` carries exactly ONE verifier record (flat: `verifier`, `evidence`, `inputs` at payload top level) — the *first* verifier to fail short-circuits the gate, so there is a single failing verifier and no further verifiers ran. `gate.passed` carries ALL verifier records because a pass means every verifier ran to completion and each contributes replayable evidence + recorded cost. The shapes are deliberately different (single-failing vs. full-pass-evidence), NOT a nested-vs-flat accident: the pins fix both (`vet_gate.rs` asserts a `verifiers` array on passed and a top-level `verifier` on failed; `s4_gates.rs` folds both). This asymmetry is intentional and load-bearing; it is not a candidate for later "harmonization" without a `v+1` on both subjects.

**`diff.merged` v1** — NEW subject (golden-path merge fact); closes the worktree lifecycle after a verified `pre_merge` pass.
- `run: string` — RunId ULID; ties the merge to the agent run (the golden-path exit asserts `diff.merged.payload.run == run_id`).
- `worktree: string` — canonicalized worktree path; the same registry key `worktree.allocated`/`diff.ready` minted. The reducer folds `status = "merged"` here, inserting the entry even if never allocated (the log is truth, I3 — `s4_gates.rs::diff_merged_marks_the_worktree`).
- `diff: CasRef` — the merged diff summary as a CAS ref (I2); the reducer pins `last_diff = Some(diff.hash)`.
- **New-subject checklist (recorded):** *grammar* — `diff.merged` parses `noun.verb`, past tense for a fact (conformant; unlike grandfathered `diff.ready`). *family coherence* — sits in the `diff` section with `diff.ready`; shares the `worktree` + `diff: CasRef` shape of `diff.ready` and adds `run` (the merge is attributable to a run; a diff-summary is not). *emitter* — git adapter (RepoSubstrate), the S4 golden-path emitter. *reducer consumer exists* — yes: `rezidnt-state` S4 reducer scaffold folds it (`s4_gates.rs` test `diff_merged_marks_the_worktree`; `s4_verified_run.expected.json` counts it). *not a duplicate of `merge.completed`* — `merge.completed` (v0, `merge` section) is the raw merge-mutation fact; `diff.merged` is the worktree-lifecycle-closing fact keyed on the run and folded by the S4 reducer. They coexist; `diff.merged` is not a rename or synonym (distinct key set, distinct consumer).
- **COLLISION with the taxonomy drift guard (reported, not silently resolved):** adding `diff.merged` as a new subject table row makes `spec/ontology.md` declare 34 subjects where `rezidnt_types::taxonomy::SUBJECTS_V0` declares 33. The committed test `crates/rezidnt-types/tests/taxonomy_drift.rs` asserts the two match exactly (same set, same table order) and runs under `/vet` (`cargo test --workspace`). Ratifying `diff.merged` therefore REQUIRES a one-line companion edit — insert `"diff.merged",` into `SUBJECTS_V0` (in the `// diff` group, immediately after `"diff.ready"`, to preserve ontology-table order). Per the warden protocol step 4 this const lives in `rezidnt-types` and is normally edited in the same session; this session's work order scoped edits to `spec/ontology.md` ONLY, so the companion edit is **flagged for the implementer as a same-commit obligation** rather than applied here. Until it lands, `/vet` will be red on `taxonomy_drift.rs`. This is a scope collision between the session constraint and the drift coupling, not an ontology-vs-pin content collision — the pin (the reducer/fixtures) and the ontology agree on `diff.merged`.

**`agent.spawned` v1 — governed-spawn additive fields** (`bare?`, `allowed_tools?`; `harness_version?` pre-existing) are documented inline in the `agent.spawned` S1 baseline above (kept with the rest of that subject's fields). Additive; v stays 1.

### DR-014 set (ratified 2026-07-18 — enforcement-mode visibility)

DR-014 (ACCEPTED, owner, 2026-07-18) §Decision 5 committed the enforcement-visibility signal as required warden work but explicitly left the taxonomy design to this session: *a field on `agent.spawned` or a new `permit.enforcement.declared` subject + reducer (no consumer-less subjects, DR-006).* This session **chose the field** — `pep?: "enforced"` on `agent.spawned`, documented inline in the S1 baseline above. Additive; `agent.spawned` stays `v = 1`.

**Field-vs-subject decision (recorded against DR-006):**
- *Necessity / not a new lifecycle fact* — the signal is a **spawn-time property of an existing fact**, not a new lifecycle event. Opt-in is fixed at spawn by `[gates.permit]` presence and **never changes after spawn** (DR-014 §Decision 2; Design §3/§6). A subject earns its own row when a new *event in time* occurs; here nothing new happens after spawn — the enforcement mode is decided exactly when `agent.spawned` is emitted. This is the direct `bare?` / `allowed_tools?` precedent (S4): a spawn-time enforcement property recorded as a field on the run's existing spawn fact, not a separate subject.
- *Why NOT `permit.enforcement.declared` (DR-006 test)* — a new subject would need its own folding consumer (DR-006: no consumer-less subjects) folding onto the SAME `AgentRunState` keyed on the SAME `run` ULID that `agent.spawned` already folds — i.e. a second subject describing one entity's one spawn moment, redundant with the fact already on the log. A `permit.*` subject would also misplace the signal on the *action-request* "may" axis (`permit.requested`/`granted`/`denied`/`escalated` are keyed on the request, DR-008), whereas enforcement-mode is keyed on the *run's spawn* — the `agent.*` axis. The `run.intent.declared` / `integrity.alarm` "distinct axis → distinct subject" precedent does **not** apply: those minted new subjects because a genuinely new fact occurred (an intent declaration; a replay divergence) on a distinct key/axis; enforcement-mode is neither — it is a property of the spawn fact itself.
- *Consumer obligation (a field is not consumer-less either)* — DR-006's "no consumer-less subjects" rule, read for a field, means **the reducer must surface the field** so `gate_explain` can read it. The folding consumer is the existing `agent.spawned` reducer arm in `rezidnt-state` (`crates/rezidnt-state/src/lib.rs`, the `"agent.spawned"` match arm, currently ~line 329 — it folds `run → status = "spawning"` but does **not** yet read `bare?`/`allowed_tools?`/`pep?`). The implementer's follow-on obligation (criterion 6, oracle-first) is to extend that arm to fold `pep?` onto a new `AgentRunState` field (e.g. `pep_enforced: bool` / `enforcement_mode: Option<String>`, `#[serde(default)]` so pre-DR-014 golden fixtures parse and compare equal unchanged, I3 rebuild-stability), which the MCP `gate_explain` read path (`crates/rezidnt-mcp/src/lib.rs`) then surfaces to answer *mid-run-enforced vs. edge-gated-only*. Naming the reducer here keeps the field from being surfaced-nowhere; wiring it is implementer scope.
- *I2* — a single short enum string (`"enforced"`, ≤ 8 bytes), trivially inside the 32 KiB cap; no CAS ref, no accumulator.
- *Drift guard — subject list UNCHANGED* — this is a **field on an existing subject, not a new subject**, so `spec/ontology.md` still declares 40 subject **table rows** and `rezidnt_types::taxonomy::SUBJECTS_V0` still declares 40. `crates/rezidnt-types/tests/taxonomy_drift.rs` parses only `(subject, v)` table rows (prose/field bullets are ignored) and `agent.spawned` stays `v = 1`, so **no `SUBJECTS_V0` / ontology-hash companion edit is required** (unlike the S4 `diff.merged`, DR-006 `integrity.alarm`, DR-008 `permit.*`, and DR-010 `run.intent.declared` sessions, which each added a table row and therefore a const entry). The drift guard stays green with the ontology edit alone.

### DR-016 set (ratified 2026-07-19 — RBAC role, permit input axis)

DR-016 (ACCEPTED, owner, 2026-07-19) §Decision 2 committed the SP4a roles scope: `role: Option<String>` on `AgentSpec`, recorded on `agent.spawned` as an additive field, injected into `decide_permit`'s content-pinned per-run params (DR-011 §2) as a new permit input axis both native and exec permit verifiers read. DR-016 §Decision 2 names the warden `/subject` as required SP4a work **explicitly** ("This requires a warden `/subject`") but does **not** design the role taxonomy — the field is opaque; the policy interprets it. This session **chose the field** — `role?: string` on `agent.spawned`, documented inline in the S1 baseline above. Additive; `agent.spawned` stays `v = 1`. A **direct precedent-match to the `pep?` field** (DR-014 set above): a spawn-time property of the run, recorded on the existing spawn fact, folded by the same reducer arm, surfaced through the same read path.

**Field-vs-subject decision (recorded against DR-006):**
- *Necessity / not a new lifecycle fact* — the role is a **spawn-time property of an existing fact**, not a new lifecycle event. It is fixed by `AgentSpec.role` at spawn and **never changes after spawn** (DR-016 §Decision 2 — the role rides the run's spawn-time params); nothing new happens *in time* after the spawn that a distinct subject would record. A subject earns its own row when a new *event* occurs; here the RBAC role is decided exactly when `agent.spawned` is emitted. This is the direct `bare?` / `allowed_tools?` / `pep?` precedent: a spawn-time governance property recorded as a field on the run's existing spawn fact, not a separate subject. Not a duplicate or synonym of any existing field — `role?` is *who the agent is authorized as* (an RBAC identity a policy keys on), distinct from `allowed_tools?` (the *tool* allowlist), `pep?` (the enforcement *mode*), and `badge_id` (attribution).
- *Why NOT a new subject (DR-006 test)* — a new subject (e.g. a `permit.role.declared`, or a field-bearing `agent.role.assigned`) would need its own folding consumer (DR-006: no consumer-less subjects) folding onto the SAME `AgentRunState` keyed on the SAME `run` ULID that `agent.spawned` already folds — a second subject describing one entity's one spawn moment, redundant with the fact already on the log. A `permit.*` subject would additionally misplace the signal on the *action-request* "may" axis (`permit.requested`/`granted`/`denied`/`escalated` are keyed on the request, DR-008), whereas the role is keyed on the *run's spawn* — the `agent.*` axis; the role is a permit **input** the verifier reads, not a permit **decision** fact. The `run.intent.declared` / `integrity.alarm` "distinct axis → distinct subject" precedent does **not** apply: those minted new subjects because a genuinely new fact occurred (an intent declaration; a replay divergence) on a distinct key/axis; the role is neither a new event nor on a new key — it is a property of the spawn fact itself. (Contrast the deliberate mirror-image call: `run.intent.declared` is a *subject* because the intent-derived least-privilege set is a genuinely new fact distinct from the composed allowlist, DR-010 §4 — whereas `role` is a *field* because it is fixed at spawn and folds onto the same spawn moment.)
- *Consumer obligation (a field is not consumer-less either)* — DR-006's "no consumer-less subjects" rule, read for a field, means **the reducer must surface the field** so `decide_permit` (and `gate_explain`) can read it. The folding consumer is the existing `agent.spawned` reducer arm in `rezidnt-state` (`crates/rezidnt-state/src/lib.rs`, the `"agent.spawned"` match arm, currently ~line 350 — it folds `run → status = "spawning"` and already reads `pep?`, folding it onto `AgentRunState.pep`; it does **not** yet read `role?`). The implementer's follow-on obligation (SP4a, oracle-first) is to extend that same arm to fold `role?` onto a new `#[serde(default)]` `AgentRunState` field (e.g. `role: Option<String>`, `#[serde(default)]` so pre-DR-016 golden fixtures parse and compare equal unchanged, I3 rebuild-stability — the exact pattern `AgentRunState.pep` / `AgentRunState.intent` already use), which `decide_permit` then reads to inject the role into the permit input params (DR-016 §Decision 2). Naming the reducer here keeps the field from being surfaced-nowhere; wiring it is implementer SP4a scope.
- *I2* — a role name is a short opaque scalar string, trivially inside the 32 KiB cap; no CAS ref, no accumulator (same sizing as `bare?`/`allowed_tools?`/`pep?`).
- *Drift guard — subject list UNCHANGED* — this is a **field on an existing subject, not a new subject**, so `spec/ontology.md` still declares 40 subject **table rows** and `rezidnt_types::taxonomy::SUBJECTS_V0` still declares 40. `crates/rezidnt-types/tests/taxonomy_drift.rs` parses only `(subject, v)` table rows (prose/field bullets are ignored) and `agent.spawned` stays `v = 1`, so **no `SUBJECTS_V0` / ontology-hash companion edit is required** (identical to the DR-014 `pep?` session; unlike the S4/DR-006/DR-008/DR-010 sessions which each added a table row and therefore a const entry). The drift guard stays green with the ontology edit alone.

### Carried subjects — v1 baselines (ratified 2026-07-17)

Grandfathered non-`noun.verb` subjects whose payload shape was carried unratified across earlier slices. Ratified here from the **actual current emitters** (not invented), closing carried items.

**`daemon.warning` v1** — WARN-level `tracing` conditions mirrored onto the fabric (doc §14) so the daemon's own misbehavior is queryable with the same tools as everything else. Shape read from the sole live emitter, `bins/rezidentd/src/runs.rs::warn_open_failed` (all warning paths — open refusal, post-ack materialization failure, and per-agent launch failure — funnel through it).
- `what: string` — the discriminator naming the warning class. **Vocabulary in use (v1):** `"open-failed"` — the only value any current emitter produces. New classes arrive additively as strings (same discipline as the `gate.inconclusive.reason` vocab); a breaking change to the discriminator's meaning mints `v+1`.
- `error: string` — a short human-readable detail message (for `open-failed`, the spec-parse refusal message or the formatted materialization/launch error). I2: this is a short diagnostic string, trivially inside the 32 KiB cap; if a warning class ever needs to carry a large detail blob it rides a `CasRef` field (never inline bytes), not an inflated `error`. The daemon `SourceId` is `"daemon"` and the fact carries its own `correlation` (warnings are not scoped to another chain unless a caller sets `causation`).
- **Grammar note:** `daemon.warning` is one of the six grandfathered v0 subjects (does not strictly parse as past-tense `noun.verb`); canonical and never renamed. Ratifying its payload does not change that status. `daemon.error` (its ERROR-level sibling) has the same emitter family but **no live emitter today** — its payload baseline is deliberately left unratified until an emitter exists (same discipline the S3 set applied to `gate.passed`).

### DR-006 set (ratified 2026-07-17)

Strict superset of the DR-006 oracle pins: the FOLD board `crates/rezidnt-state/tests/dr006_integrity_alarms.rs` (reducer folds to `agent_runs[run].integrity_alarms`, deduped by `(run, gate, verifier)`, deterministic order, rebuild-safe) and the daemon-routed EMIT board `bins/rezidentd/tests/golden_path.rs` (`dr006_*`: durable fact on the log via the daemon single writer, rebuild-visible, deduped). Additive-only; the subject is minted at `v = 1`. The payload mirrors `rezidnt_gate::IntegrityAlarm` (`crates/rezidnt-gate/src/lib.rs`).

**`integrity.alarm` v1** — NEW subject (DR-006); the durable log fact of a replay divergence. Emitter: the daemon (single writer — DR-006 routes the `debrief` append through the daemon's sole log writer; the CLI keeps its direct read for the `report.alarms[]` output, I3).
- `run: string` — RunId ULID; the run whose recorded verdict diverged on replay. The reducer folds the alarm onto this run's dossier, minting the run entry if absent (the log is truth, I3 — an alarm needs no prior `agent.spawned`/`gate.*` fact, mirroring how gate facts fold without a spawn).
- `gate: string` — the gate name; matches the diverging fact's `gate` (`gate.failed`/`gate.passed`/`gate.inconclusive`), and is part of the dedup key.
- `verifier: string` — the diverging verifier's name (§8); the second component of the dedup key. Named on the fact so triage needs no re-derivation.
- `recorded: "pass" | "fail" | "inconclusive"` — the verdict on the log for this verifier. Verbatim from the recorded `gate.*` fact; never coerced (I6).
- `replayed: "pass" | "fail" | "inconclusive"` — the verdict re-derived from inputs + CAS on replay. Verbatim; `recorded ≠ replayed` is the definition of the alarm and the two are **never reconciled** (§8). (Note: `rezidnt_gate::ReplayedVerdict.replayed` is `Option<Verdict>` for the general replay report — a not-re-executed record; but an *alarm* only exists when both verdicts are concrete and disagree, so on this subject `replayed` is a concrete verdict string, matching `IntegrityAlarm.replayed: Verdict`.)
- **Dedup semantics (at-least-once, deduped by `(run, gate, verifier)`):** emission is **at-least-once, not exactly-once** on the wire — same honesty as `worktree.conflict`. The daemon dedups at APPEND by reading existing `integrity.alarm` facts off the log (log-derived, I3 — never a side table) and appending nothing for an already-alarmed `(run, gate, verifier)`; a re-run of `debrief` over an already-alarmed divergence is a no-op append. The reducer dedups again at FOLD: duplicate facts on the log collapse to ONE queryable record per `(run, gate, verifier)`, so the log may honestly carry a duplicate (e.g. a crash between the log read and the append) while derived state shows the divergence once. Log stays append-only and is the source of truth; derived state is deduped.
- **I2:** all fields are short strings (a ULID, two names, two verdict words) — a full alarm payload is ~120 bytes, three orders of magnitude under the 32 KiB hard cap. Evidence is **not** re-inlined here: it stays CAS-ref'd on the originating `gate.failed`/`gate.passed` fact; the alarm carries only the divergence identity and the two verdicts.
- **New-subject checklist (recorded):** *necessity* — genuinely new lifecycle fact (log-integrity divergence), not a payload field on an existing subject; has a folding consumer (not dead-letter); not a synonym of `gate.failed` (that carries a verdict, this carries a divergence between two). *grammar* — `integrity.alarm` parses strictly as `noun.verb` (`integrity` a new top-level noun, `alarm` the fact of an alarm raised); **not grandfathered** — it is a new subject and conforms, unlike the six v0 grandfathered forms. *family coherence* — new top-level noun `integrity`, justified: log/replay integrity is a distinct axis from gate verdicts and daemon lifecycle; DR-006 explicitly chose a dedicated noun over both the `daemon.error` bucket and `gate.*` vocabulary. *emitter exists* — the daemon single writer (DR-006). *reducer consumer exists* — yes: `rezidnt-state` folds to `agent_runs[run].integrity_alarms` (`IntegrityAlarmRecord`; board `dr006_integrity_alarms.rs`). *not a duplicate of `daemon.error`* — DR-006 rejected that bucket precisely so the integrity signal is queryable and the gate vocabulary stays clean.
- **COLLISION with the taxonomy drift guard (reported, not silently resolved):** adding `integrity.alarm` as a new subject table row makes `spec/ontology.md` declare 35 subjects where `rezidnt_types::taxonomy::SUBJECTS_V0` declares 34. The committed test `crates/rezidnt-types/tests/taxonomy_drift.rs` asserts the two match exactly (same set, same table order) and runs under `/vet`. Ratifying `integrity.alarm` therefore REQUIRES a one-line companion edit — insert `"integrity.alarm",` into `SUBJECTS_V0` in ontology-table order: at the **end of the list, after `"badge.revoked"`**, under a new `// integrity` group comment (the `integrity` section is the last section in this file, after `badge`). Per warden protocol step 4 this const lives in `rezidnt-types` and is normally edited in the same session; this work order scoped edits to `spec/ontology.md` ONLY, so the companion edit is **flagged for the main thread as a same-commit obligation** rather than applied here. Until it lands, `/vet` is red on `taxonomy_drift.rs`. This is a scope collision between the session constraint and the drift coupling, not an ontology-vs-pin content collision — the pins (reducer + golden fixtures) and the ontology agree on `integrity.alarm` and its shape.

### permit set (ratified 2026-07-17 — DR-008 / DR-009)

The pre-hoc authorization subjects. Minted at `v = 1`. Verdict is carried by the **subject** (`permit.granted`/`permit.denied`/`permit.escalated`), never a bare boolean; `inconclusive → permit.escalated` is never coerced (I6, DR-008 §4). Large action context (command args, file bytes, diffs) is **always a CAS ref, never inline** (I2). Payloads are sized to **support contextual/stateful downstream decisions** (intel memo 001 C6/C7): decision facts carry `policy_ref` + `evidence_ref` so the interrogation (`gate why` / `gate_explain`) and the per-session accumulators (risk/spend/intent) can be derived from the log alone (I3). This is subject + reducer scaffolding; the C1/C7 verifiers are SP0–SP4 work, not minted here.

Correlation model: `permit.requested` opens the chain via the envelope `correlation` (doc §5); each decision fact carries `causation` = the request event id and repeats the request's `correlation`, so the ledger folds request→decision from the envelope without a payload back-pointer (mirrors the `gate.entered`→verdict correlation model in the S3 set). A `request_id` field is additionally carried on the payload as the explicit, log-derivable ledger key (the request's own ULID text) so the ledger rebuilds even if a decision arrives with the envelope back-pointers absent — honest key, never synthesized.

**`permit.requested` v1** — the request fact. Small descriptor inline; large context is a CAS ref (I2).
- `run: string` — RunId ULID; the key every `permit.*` fact carries (the agent run asking to act). Ties a decision back to its run for the per-session accumulators.
- `request_id: string` — this request's own ULID text; the ledger key the decision facts reference. Log-derivable, never synthesized.
- `action: string` — the action kind, a short closed-ish vocabulary string (v1: `"tool.invoke"`; new kinds arrive additively as strings, same discipline as `gate.inconclusive.reason`). What the agent wants to do.
- `target: object` — a **small inline descriptor** of the action target: a `tool?: string` (tool name, e.g. `"Bash"`), and other short scalar hints. Bounded to short strings — a descriptor, not the payload. Any large context (command args, file contents, diffs) does **not** live here.
- `context_ref?: CasRef` — the large action context (full argv, file bytes, diff) persisted to the CAS; carried as a ref, never inline (I2). Optional: absent when the action has no bulk context (a descriptor-only request).
- `badge_id?: string` — loggable badge identifier of the caller whose capability authorizes the eventual mutation (doc §12; DR-005). NEVER the badge token. Optional/additive: absent on unbadged/read-class request paths; present when the request rode a badge (the request's *result* authorizes a mutation, so the badge is the caller's identity — design §5).

**`permit.granted` v1** — decision = allow (`pass`). Interrogable: the deciding policy + evidence are on the fact (I6).
- `run: string` — as `permit.requested`; the run the decision is for (drives the per-session accumulators).
- `request_id: string` — the `permit.requested.request_id` this decision answers; the ledger correlation key.
- `policy_ref: CasRef` — the deciding permit-verifier/policy, content-addressed: which policy allowed this action, so `gate why` / `gate_explain` returns it (I6). A CAS ref (the policy document may be a Cedar/Rego file or a native verifier's recorded params — bytes go to the CAS, never inline; I2).
- `evidence_ref?: CasRef` — the decision evidence (the verifier's recorded findings/inputs) persisted to the CAS; ref only (I2, mirrors `gate.*` evidence). Optional: absent when an allow produced no evidence blob (a trivially-permitted action).
- `cost_ms?: u64` — wall-clock cost of the decision (the §8 stdout `cost_ms`); additive, absent when not measured. Supports the latency risk-tracking of design §10.2. **Intentionally recorded-only (SP5 close-out):** carried on the decision fact for per-decision latency/audit fidelity — unlike `risk_delta?`, it has **no folding accumulator** (`rezidnt-state` deliberately does not sum it into `PermitAccumulators`; there is no `cumulative_cost_ms`). Reading it back is a per-fact, log-derivable read (I3), never a materialized running total. A future accumulator, if ever wanted, would be its own change (a new `PermitAccumulators` field + reducer arm) — not a payload break (`v` stays 1; the field is unchanged). `permit.denied`/`permit.escalated` inherit this via their "exactly as `permit.granted`" `cost_ms?` line.
- Accumulator inputs (additive, all optional — sized per intel memo 001 C6/C7 so the reducer can fold running per-session state without a `v+1`):
  - `risk_delta?: f64` — the incremental risk this action contributes to the running per-session risk score (C6 contextual policy). **This is the C6 fold source (DR-024, 2026-07-20).** DR-024 confirms `risk_delta?`'s placement on `permit.granted` as correct, not incidental (Q2): risk is deterministically COMPUTED pre-action from the request axis (`tool`/`paths`/`role`) by the `RiskCap` native, so it legitimately rides the permit fact — NOT the dishonest-if-denied pre-action *estimate* that spend was (why `spend_delta_usd?` was retired, DR-021 B2). PRODUCED only here: the emit site stamps it into the existing `DecisionDeltas` carrier via the shared deterministic `risk_score(request_axis, table)` pure fn that `RiskCap::verify` also calls for its soft/hard verdict, so the stamped delta and the verdict's evidenced score cannot diverge (contract-free producer seam — `VerifierOutput`/`PermitOutcome` UNCHANGED, DR-024 Q5→option-iii). FOLDED GRANTED-only (DR-024 Q3): the reducer sums it into `PermitAccumulators.risk_score` on the `"granted"` arm ONLY — a denied/escalated action never ran, so it folds ZERO risk (the I3 honesty property, mirror of DR-021 B2 spend). Same grain/shape as the retired `spend_delta_usd?` and the recorded-only `cost_ms?` (a small optional scalar); unlike `cost_ms?` it HAS a folding accumulator. Absent when not scored.
  - **`spend_delta_usd?` RETIRED as the C1 fold source (DR-021 B2, 2026-07-20):** the incremental-spend field is **removed** from this payload. Under DR-021 §Decision B2 the pre-action permit decision folds NO spend (a pre-action estimate on a granted/denied fact is the dishonest-if-summed accumulator DR-021 rejected on I3 grounds); the **measured** spend now rides the post-action `action.metered` subject (see the DR-021 set below), whose reducer arm folds `cumulative_spend_usd`. The field was never emitted by a live producer (the C1 path was inert — `DecisionDeltas::default()` hard-coded, DR-021 §Context), so its removal is not a breaking payload change and `permit.granted` stays `v = 1`. The reducer arm that read it (`crates/rezidnt-state/src/lib.rs:725-726`) moves to `action.metered` in the same commit (implementer obligation — DR-021 set §reducer).

**`permit.denied` v1** — decision = deny (`fail`). Same shape as `permit.granted` plus a `reason` so a blocked agent can always read *why* (I6, DR-005 read-class-interrogation rule).
- `run`, `request_id`, `policy_ref: CasRef`, `evidence_ref?: CasRef`, `cost_ms?` — exactly as `permit.granted`.
- `reason: string` — short human-readable denial reason (e.g. `"path outside allowed scope"`); the deny detail the interrogation surfaces. I2: a short diagnostic string; large detail rides `evidence_ref`, never inflated inline.
- **`risk_delta?` RETIRED from this payload (DR-024 Q3/Q5, 2026-07-20)** — removed. DR-024 makes `risk_delta` PRODUCED only on `permit.granted` (Q5: the emit site stamps it there via the shared scorer) and FOLDED only on the granted reducer arm (Q3: GRANTED-only fold). A denied action never ran, so folding its risk is a phantom charge — the exact I3 dishonesty DR-021 B2 refused for spend (and the sharpest case for it, exactly as denied was for `spend_delta_usd?`). The field is therefore DEAD on `permit.denied` — never produced, never folded — and is retired, mirroring the `spend_delta_usd?` retire above. (The earlier "a denied sensitive attempt is itself risk signal" rationale is superseded by DR-024: a denied attempt is real signal, but it is surfaced via the `permit.denied` fact + its `reason`/`policy_ref`/`evidence_ref` and the `denied` counter — NOT by charging phantom risk into the running `risk_score` the next decision reads.) The field was never emitted by a live producer (the C6 path was inert until DR-024 wired it), so removal is not a breaking payload change and `permit.denied` stays `v = 1`.
- **`spend_delta_usd?` RETIRED as the C1 fold source (DR-021 B2, 2026-07-20)** — removed from this payload for the same reason as `permit.granted` (the pre-action decision folds NO spend; measured spend rides `action.metered`). A **denied** action is the sharpest case for B2's honesty: a denied action never runs and must contribute ZERO to `cumulative_spend_usd` (DR-021 acceptance-criteria #5 — no phantom charge on a denied action). The field was never emitted by a live producer; its removal is not a breaking change and `permit.denied` stays `v = 1`.

**`permit.escalated` v1** — decision = inconclusive → routed to a human (`ask`); never coerced to allow (I6, DR-008 §4).
- `run`, `request_id`, `policy_ref: CasRef`, `evidence_ref?: CasRef`, `cost_ms?` — exactly as `permit.granted`.
- `reason: string` — why the decision was inconclusive / requires a human (e.g. `"cumulative spend crossed soft cap"`, `"no policy matched"`); v1 is a free string, additive vocabulary, mirroring `gate.inconclusive.reason`. Never a coercion to allow.
- **`risk_delta?` RETIRED from this payload (DR-024 Q3/Q5, 2026-07-20)** — removed, for the same reason as `permit.denied`. DR-024 folds `risk_delta` GRANTED-only (Q3) and produces it only on `permit.granted` (Q5). An escalated action is routed to a human and has NOT run at decision time, so a pre-action risk delta folded here would charge the run for danger it never incurred — the phantom charge B2 rejected for spend, and precisely why `spend_delta_usd?` was also retired from this same fact (the one decision fact where the action provably has not run). The field is DEAD on `permit.escalated` — never produced, never folded — and is retired. (The earlier "folded when the human resolves it" rationale is superseded: DR-024 does not fold on escalation; if the human resolves to allow and the action runs, its risk is assessed and stamped on the resulting `permit.granted` fact and folded there.) The field was never emitted by a live producer; removal is not a breaking payload change and `permit.escalated` stays `v = 1`.
- **`spend_delta_usd?` RETIRED as the C1 fold source (DR-021 B2, 2026-07-20) — warden coherence extend beyond the literal `granted`/`denied` retire, flagged:** the C1 spend fold source is now `action.metered` **exclusively**, so no `permit.*` fact — escalated included — sources it. An escalated action has not yet run at decision time (it is routed to a human), so a pre-action spend delta here is the same pre-action-estimate honesty problem B2 rejected; the measured spend, if the human resolves to allow and the action runs, rides `action.metered` post-action. DR-021 §Consequences names the retire on `permit.granted`/`permit.denied` explicitly and this session's work order scoped the retire to those two; retiring it on `permit.escalated` as well is the **coherent completion** (leaving it would keep a dead pre-action spend field on the one decision fact where the action provably has not run) and is called out here so the warden judgment is auditable rather than silent. The field was never emitted by a live producer; removal is not a breaking change and `permit.escalated` stays `v = 1`. `risk_delta?` is unaffected (C6).

**New-subject checklist (recorded — four subjects):** *necessity* — four genuinely new lifecycle facts on the pre-hoc "may" axis (DR-008), each with a folding consumer (permit ledger + per-session accumulators — not dead-letter); not payload fields on an existing subject (the `gate.*` family is keyed on the gate-run + carries a verdict; `permit.*` is keyed on the action request + carries an authorization decision + `policy_ref`/`evidence_ref`); no synonym of `gate.passed` (a gate-run outcome ≠ an authorization decision on a single action). Three decision subjects (not one with a verdict field) matches the house pattern of carrying the verdict **in the subject** (`gate.passed`/`gate.failed`/`gate.inconclusive`), I6. *grammar* — all four parse strictly as past-tense `noun.verb` (`permit` a new top-level noun; `requested`/`granted`/`denied`/`escalated` past-tense facts) — **not grandfathered**, they conform. *family coherence* — new top-level noun `permit` = the pre-hoc authorization axis, justified as distinct from `gate.*` verdicts (post-hoc/gate-run) and `daemon.*`/`integrity.*` (DR-008 makes `permit` a distinct gate lifecycle point; same precedent as DR-006 minting `integrity` as its own noun rather than folding into `gate.*`/`daemon.error`). *emitter exists* — harness PEP via `request_permission` for `permit.requested`; the permit gate engine (PDP) for the three decisions (DR-008 §3/§5). *reducer consumer exists* — yes: `rezidnt-state` folds a per-run permit ledger + per-session accumulators (`permit_ledger`, `permit_accumulators`; scaffolded this session). *verdict mapping* — `granted=allow(pass)`, `denied=deny(fail)`, `escalated=ask-a-human(inconclusive)`; no allow-coercion subject (I6, DR-008 §4). *authority* — DR-008 (ACCEPTED) authorizes the four subjects by name; DR-009 (ACCEPTED) sizes the contextual payload; both already ratify the BINDING scope, so no `/dr` is pending. *I2* — action/target is a small inline descriptor; all bulk context (`context_ref`), the policy (`policy_ref`), and evidence (`evidence_ref`) are CAS refs — a decision payload is short scalars + three refs, ~a few hundred bytes, far under the 32 KiB cap.

**COLLISION with the taxonomy drift guard (companion edit — applied same-session):** adding four `permit.*` rows makes `spec/ontology.md` declare 39 subjects vs. `rezidnt_types::taxonomy::SUBJECTS_V0`'s prior 35. `crates/rezidnt-types/tests/taxonomy_drift.rs` asserts the two match exactly (same set, ontology-table order) under `/vet`. Unlike the S4/DR-006 sessions (which flagged the const edit for a later commit because they were scoped to `spec/ontology.md` only), **this session's work order mandates the same-session companion edit** (taxonomy-drift precedent), so `SUBJECTS_V0` gains the four `permit.*` entries under a new `// permit` group at the **end of the list, after `"integrity.alarm"`** (matching this section's position as the last subjects section), applied in the same session as this ratification.

### S4 open questions — decided or tracked

- **(a) exec-verifier network opt-in — where recorded (DECIDED, no new field):** §8 says "no network by default … unless the gate def opts in, recorded in the event." The opt-in is a property of the *verifier's inputs*, so it rides the already-verbatim `inputs` object on `gate.passed`/`gate.failed`/`gate.inconclusive` — i.e. `inputs.params` (the §8 `params` the verifier receives) carries the opt-in flag when the gate def sets it. No new top-level payload field is minted: the recorded-verbatim `inputs` document already satisfies "recorded in the event," and adding a parallel top-level field would duplicate state the §8 stdin contract owns. When the S4 engine begins emitting the flag, it appears inside `inputs.params` additively (the `inputs` object is explicitly "widened additively by the engine without a payload break"). The S4 pins carry `params: {}` / `params: {allow: […]}` with no network opt-in exercised, so this decision foreclosed nothing the pins fixed. If a future gate needs the flag surfaced OUT of the opaque `inputs` for indexing/query, that is a `v+1` proposal with a consuming reducer — tracked, not minted.
- **(b) debrief replay divergence — integrity alarm as a log FACT? (RESOLVED by DR-006, 2026-07-17):** the S4 pins (`golden_path.rs::cli_debrief_divergence_raises_integrity_alarm`) fixed the alarm as a **CLI-surface report** (`report.alarms[]` on `debrief --json`, exit 3) — a derived read over log + CAS, emitting nothing. This question — "should replay-divergence integrity alarms be durable log facts, and under which subject" — was routed to `/dr` and **owner-ratified as DR-006 (ACCEPTED)**: divergence lands a **durable fact on the log** via a **new dedicated subject** `integrity.alarm` (chosen over reusing `daemon.error` and over polluting `gate.*`), with a **folding reducer** (`agent_runs[run].integrity_alarms`, no consumer-less subject), the append routed through the daemon's single writer (I3). The subject is minted above — see the **DR-006 set** `integrity.alarm` v1 baseline. The CLI-report-only behavior the earlier pin fixed remains correct: the durable fact is strictly additive to it (exit stays 3). No longer open; DR-006's `integrity.alarm` `/subject` (this ratification) is the closing action. Any amendment to this outcome requires DR-008 (per DR-006's amendment clause).

### run-intent set (ratified 2026-07-18 — DR-010)

The run-intent subject. Minted at `v = 1`. The intent text is **always a CAS ref, never inline** (I2); the allowed-tool set is a short string list (trivially inside I2). The payload is content-pinned so the `intent-lock` verifier reads it as a deterministic, replayable input — never a live re-derivation (DR-010 §3, design §3: the determinism BINDING forbids a live LLM call inside the verifier). This is subject + reducer scaffolding only; the `intent-lock` verifier is SP-intent (oracle-first, next slice).

**`run.intent.declared` v1** — the durable per-run intent fact; folds to a rebuild-stable per-run intent state (`AgentRunState.intent`) the verifier reads (no consumer-less subjects — DR-006 precedent).
- `run: string` — RunId ULID; the key the per-run intent state folds on (the same key discipline every `agent.*`/`permit.*` fact carries). Ties the intent to its run so `AgentRunState.intent` is the pinned input the verifier resolves.
- `intent_ref: CasRef` — the initiating task/prompt text, persisted to the CAS; carried as a ref, never inline (I2). `CasRef` is `{hash: blake3 hex string, bytes: u64, mime: string}`. The interrogable "what was this run for" the `gate_explain` evidence names alongside the off-task tool (design §4).
- `allowed_tools: [string]` — the intent-scoped tool set the `intent-lock` verifier enforces (in-set → allow; off-task → escalate, or deny under the `on_off_task = deny` knob; DR-010 §3). A short list of short strings — trivially inside I2 (mirrors `agent.spawned.allowed_tools?` sizing). **Distinct from `agent.spawned.allowed_tools?`**: that field records the *composed harness allowlist* (what the agent was configured with — a governance-composition fact); this records the *intent-derived least-privilege set* the verifier checks a requested tool against (DR-010 §4, load-bearing). The two axes fold onto different state (`agent.spawned.allowed_tools` is not folded to a dedicated field; `run.intent.declared` folds to `AgentRunState.intent`), so this is a distinct subject, not a synonym.
- **New-subject checklist (recorded):**
  - *necessity* — a genuinely new lifecycle fact (a run's declared intent + its least-privilege tool set), not a payload field on an existing subject: the distinctness from `agent.spawned.allowed_tools?` is load-bearing (DR-010 §4) — a distinct axis (intent-derived enforcement target vs. configured capability), so a distinct subject, not a field. Has a folding consumer (`AgentRunState.intent`) — not dead-letter. Not a synonym of any existing subject.
  - *grammar* — `run.intent.declared` parses as the established three-segment house form `noun.qualifier.verb` (`run` noun · `intent` qualifier · `declared` past-tense verb), the exact shape of `workspace.spec.applied`, `agent.status.changed`, `agent.tool.invoked`, `adapter.health.changed`. Past tense for a fact (conformant; **not grandfathered**). The `run` noun is NEW.
  - *family coherence (the load-bearing call, recorded honestly)* — the run's *lifecycle* facts (`spawned`/`status.changed`/`completed`/`signaled`/`blocked`) are grandfathered under the `agent.*` noun and are NOT renamed (never rename — deprecation only). `run.intent.declared` mints a NEW noun `run` for the run-**intent** axis, justified as DR-006/DR-008 precedent (a distinct axis → a distinct noun: `integrity`, `permit` were minted rather than folded into `gate.*`). This does not fracture the entity: the subject keys on the `run` RunId ULID every AgentRun fact already carries and folds onto the SAME `AgentRunState` dossier, so `agent.*` and `run.intent.*` describe one entity through one derived record. The noun `run.intent.declared` was ratified BY NAME in DR-010 §4 (ACCEPTED, owner, 2026-07-18); confirming it (over renaming to `agent.intent.declared`) stays inside DR-010's grant — a rename would override a same-day owner-ratified name, i.e. touch a BINDING-adjacent decision beyond DR-010, which the work order routes to `/dr`. Confirmed as ratified.
  - *emitter exists (named; minted by SP-intent, not this session)* — the run/spawn intent-declaration path (project spec `[gates.permit]` / agent spec, design §3(a)) declares the intent and records this fact. The emitter is SP-intent implementation scope; naming it here keeps this from being a dead subject — the folding **consumer** is what THIS session builds.
  - *reducer consumer exists* — yes: `rezidnt-state` folds `run.intent.declared` into `AgentRunState.intent: Option<IntentState { allowed_tools, intent_ref }>` (this session; scaffold test `crates/rezidnt-state/tests/run_intent.rs`). A keyless fact (missing `run`) folds counters-only / no-op, never panics (I3 honesty — the permit-reducer discipline). Rebuild-stable: survives `fold(log) == snapshot` / incremental == fold-from-zero (`reducer_props.rs` folds every `SUBJECTS_V0` subject, `run.intent.declared` now among them).
  - *authority* — DR-010 (ACCEPTED, owner, 2026-07-18) §4 authorizes exactly this subject by name and shape; no BINDING item is amended beyond DR-010's grant, so no `/dr` is pending.
  - *I2* — `run` is a ULID, `allowed_tools` a short string list, and the intent text is a `CasRef` (out-of-band) — the payload is small scalars + one ref, orders of magnitude under the 32 KiB hard cap. A pathological intent stays a CAS concern; the tool-name list never approaches the cap (same reasoning as `agent.spawned.allowed_tools?`).
- **COLLISION with the taxonomy drift guard (companion edit — applied same-session, per the work order / DR-009 precedent):** adding the `run.intent.declared` table row makes `spec/ontology.md` declare 40 subjects vs. `rezidnt_types::taxonomy::SUBJECTS_V0`'s prior 39. `crates/rezidnt-types/tests/taxonomy_drift.rs` asserts the two match exactly (same set, ontology-table order) under `/vet`. Unlike the S4/DR-006 sessions (which flagged the const edit for a later commit), **this session's work order mandates the same-session companion edit** (the DR-009/SP5 taxonomy-drift precedent), so `SUBJECTS_V0` gains `"run.intent.declared"` under a new `// run` group at the **end of the list, after `"permit.escalated"`** (matching this section's position as the last subject section), applied in the same session as this ratification. Not a pin↔ontology content collision — the reducer + scaffold test agree on `run.intent.declared` and its shape.

### DR-017 set (ratified 2026-07-19 — SP4b macaroon-attenuated delegation)

DR-017 (ACCEPTED, owner, 2026-07-19) §Decision 2 committed a **new `permit.delegated` subject + folding reducer** by name — a delegation is a new event in time on the `permit.*` axis (a capability was narrowed and handed off via badge attenuation), **explicitly chosen over a field on `agent.spawned`** (contrast the SP4a `role?`, which IS a spawn-time field — DR-017 §Decision 2, design §7/§10.2). Minted at `v = 1`. Design basis `docs/design/permit-macaroon-delegation-sp4b.md` §4 (the caveat shape) / §7 (the delegation fact). This session mints the **subject + payload schema + reducer obligation**; the folding reducer implementation and the macaroon crypto are the implementer's SP4b work (oracle-first), NOT written here.

**`permit.delegated` v1** — the durable capability-chain fact: a lead agent's badge was attenuated (a narrowing caveat appended, re-keyed offline — no root key, the delegation property, design §4) to mint a child badge for a sub-agent spawn. Folds onto the run's dossier so the capability chain is queryable/replayable (I3; no consumer-less subjects — DR-006 precedent).
- `run: string` — RunId ULID; the key every `permit.*`/`agent.*` fact carries — the run under which the delegation happened (the lead agent's run). Ties the delegation to the run's dossier the reducer folds onto.
- `parent_badge_id: string` — loggable badge identifier of the **parent** (lead-agent) badge that was attenuated. `hex(blake3(sig)[..8])` — an 8-byte hex prefix of `blake3` over the badge's **running MAC sig** (DR-018 §Decision (a), amending the DR-005 `blake3(identifier)[..8]` convention for macaroon badges): the running sig re-keys per appended caveat, so a *true* same-identifier `attenuate` yields a parent/child that differ by their caveat chains rather than their identifier, giving distinct edge ids while preserving the offline-delegation property. We hash `blake3(sig)` rather than the raw sig bytes so no bytes of the MAC itself land on the fabric. **Shape unchanged** — an 8-byte hex prefix, loggable; only the pre-image changes. **NEVER the badge token** (the macaroon exists only under `REZIDNT_BADGE` in the spawned environment; the token is never on the fabric, I2/§12).
- `child_badge_id: string` — loggable badge identifier of the **child** (sub-agent) badge minted by the attenuation. Same `hex(blake3(sig)[..8])` sig-derived form (DR-018 §Decision (a)); never the token. Because the child's running sig is re-keyed by the appended narrowing caveat, its `badge_id` differs from the parent's even under the shared identifier a faithful `attenuate` preserves — so `parent_badge_id`/`child_badge_id` name the two distinct ends of the capability edge and the chain is queryable from the log alone without resolving any token.
- `added_caveats: [Caveat]` — the narrowing caveats appended at this attenuation step (the predicates that make the child strictly ⊆ the parent — the monotonicity invariant, DR-017 §Decision 3 / design §5). Each `Caveat` is a **small, deterministically-serialized, tagged first-party predicate** (design §4 — the daemon evaluates them at verify; no third-party discharge). A short list of small predicates (a few per delegation), trivially inline under the 32 KiB cap (I2 — a macaroon is an identifier + a few caveats, never CAS). The tagged shape below is **content-stable and canonically serialized** so verify/replay are deterministic (I6 — the same discipline the keyed-MAC chain requires; a reordered or edited caveat breaks the MAC). A `Caveat` is **exactly one of** (discriminated by `kind`; the four first-party predicate kinds design §4 names):
  - `{ kind: "workspace", workspace: string }` — restricts the badge to a single workspace (`workspace` is a WorkspaceId ULID text). The child may act only within this workspace.
  - `{ kind: "verb", verbs: [string] }` — restricts the badge to a set of verbs (the state-mutating call kinds the §12 door checks, e.g. `spawn` / `open` / `merge`). `verbs` is the narrowed allowed set — a short string list; monotonic narrowing means the child's set is a subset of the parent's.
  - `{ kind: "expiry", not_after: string }` — the badge is invalid after `not_after` (an RFC3339 UTC timestamp string). **Evaluated at verify time against a passed-in timestamp from the request context — never an ambient `now()` inside the verifier** (DR-017 §Decision 3 / design §5: replayable, I6). A short scalar.
  - `{ kind: "role", role: string }` — pins the badge to an RBAC role (an opaque role string the policy interprets — same opaque-role discipline as `agent.spawned.role?`, DR-016; rezidnt mints no role vocabulary). A short scalar.
  - New caveat kinds arrive **additively** as new `kind` strings (same discipline as `gate.inconclusive.reason` / `agent.status.changed`); a breaking change to an existing kind's meaning mints `v+1`.
- **New-subject checklist (recorded):**
  - *necessity* — a genuinely new lifecycle fact (a capability narrowed + handed off), not a payload field on an existing subject. DR-017 §Decision 2 **explicitly chose a subject over a field**: a delegation is a new *event in time* (attenuation happens after spawn, when a lead agent spawns a sub-agent), unlike the SP4a `role?` / DR-014 `pep?` which are fixed *at spawn* and correctly fields on `agent.spawned`. Has a folding consumer (the run dossier — see reducer obligation below) — not dead-letter. Not a synonym of the `permit.granted`/`.denied`/`.escalated` decision facts: those are PDP *authorization decisions* keyed on an action `request_id` ("did the daemon allow THIS action"); `permit.delegated` is a *capability-chain* fact keyed on the `run` ("a badge was narrowed into a child badge") — distinct meaning, distinct key, distinct payload (badge edge + caveats, no `policy_ref`/verdict).
  - *grammar* — `permit.delegated` parses strictly as past-tense `noun.verb` (`permit` the existing "may"-axis noun; `delegated` a past-tense fact). Conformant — **not grandfathered**. No existing subject is renamed (never rename — deprecation only); this is a pure addition to the existing `permit` noun.
  - *family coherence* — sits under the existing `permit` noun (the pre-hoc authorization "may" axis, DR-008), which is exactly right: a delegation is a capability (a "may") being narrowed and handed off. It does **not** mint a new noun (unlike `integrity`/`run`) because the axis already exists; it extends `permit` from *decisions on a request* to *the capability chain those decisions run against*. Distinct from the four `permit.*` decision subjects in key (run vs. request_id) and payload (badge edge + caveats vs. verdict + `policy_ref`), so it is an addition to the family, not a synonym within it.
  - *emitter exists* — the daemon PDP, when a lead agent's badge is attenuated for a sub-agent spawn (DR-017 §Decision 2 — the PDP is the sole minter/verifier; attenuation is recorded as this fact). The macaroon crypto (`check_badge` verify + attenuate) is SP4b implementer scope; naming the emitter here keeps this from being a dead subject.
  - *reducer consumer exists (obligation — NOT built this session)* — DR-017 §Decision 2 **requires a folding reducer** (no consumer-less subjects — DR-006 precedent). The delegation folds onto the run's dossier consistent with how every other `permit.*` subject folds: keyed on the payload `run`, onto `AgentRunState` in `rezidnt-state` (`crates/rezidnt-state/src/lib.rs` — the same `run`-keyed dossier the permit ledger `AgentRunState::permit_ledger` and accumulators fold onto). The implementer's SP4b follow-on (oracle-first) is to fold `permit.delegated` onto a new `#[serde(default)]` `AgentRunState` field capturing the capability chain (e.g. `delegations: Vec<DelegationRecord { parent_badge_id, child_badge_id, added_caveats }>`, deterministic order, rebuild-stable — the exact `#[serde(default)]` pattern `integrity_alarms` / `permit_ledger` / `intent` already use so every pre-DR-017 golden fixture parses and compares equal unchanged, I3). A keyless fact (missing `run`) folds counters-only / no-op, never panics (the established permit-reducer discipline). Naming the fold target here keeps the subject from being surfaced-nowhere; **wiring it is implementer SP4b scope, not this session.**
  - *authority* — DR-017 (ACCEPTED, owner, 2026-07-19) §Decision 2 authorizes exactly this subject and payload shape by name (`{parent_badge_id, child_badge_id, added_caveats, run}`); no BINDING item is amended beyond DR-017's grant (the monotonicity invariant and the §12 badge-door flip are DR-017's own ratifications), so no `/dr` is pending.
  - *I2* — a macaroon is an identifier + a few caveats (design §2/§8); the fact carries two short `badge_id`s (8-byte blake3 prefixes, hex) + a short list of small tagged caveats — a few hundred bytes at most, orders of magnitude under the 32 KiB hard cap. The badge **token** is never here (only the loggable `badge_id`s); caveats are small structured predicates, never bulk context — no CAS ref needed.
- **COLLISION with the taxonomy drift guard (companion edit — applied same-session, per the DR-008/DR-010 precedent):** adding the `permit.delegated` table row makes `spec/ontology.md` declare 41 subjects vs. `rezidnt_types::taxonomy::SUBJECTS_V0`'s prior 40. `crates/rezidnt-types/tests/taxonomy_drift.rs` asserts the two match exactly (same set, ontology-table order) under `/vet`. Unlike the S4/DR-006 sessions (which flagged the const edit for a later commit because scoped to `spec/ontology.md` only), **this subject's warden protocol step 4 mandates the same-session companion edit** — `SUBJECTS_V0` gains `"permit.delegated"` in the existing `// permit` group, immediately after `"permit.escalated"` (preserving ontology-table order), applied this session. Not a pin↔ontology content collision — the const and the ontology agree on `permit.delegated`; the reducer + oracle fixtures the implementer builds will agree on it too.

### DR-021 set (ratified 2026-07-20 — C1 live spend-cap: post-action metering + permit spend-field retire)

DR-021 (ACCEPTED, owner, 2026-07-20) makes the C1 spend-cap enforcement path live and honest. §Decision A→option 3 (deferred/post-action attribution) + §Decision B2 (spend attribution moves OFF the pre-action permit decision) ratify the **direction** — measured actuals ride a post-action fact; the pre-action permit fact folds NO spend — and §"The metering fact and its subject are NOT designed here" explicitly hands the subject shape to this warden `/subject` session. §Consequences names `action.metered` and the reducer fold-arm move by name. This session does three coupled things in one gate: **(1) mints `action.metered` v1**; **(2) retires `spend_delta_usd?` from `permit.granted`/`permit.denied`** (the C1 fold source moves off the permit fact); **(3) records the reducer-arm fold obligation** for the implementer. Additive/subtractive at the payload-field level but **no `v`-bump on any subject** — see the versioning note below.

**`action.metered` v1** — NEW subject (DR-021 C1); the per-action post-action metering fact. Emitter: the harness PEP / run-substrate post-action metering path (minted by the C1 build, not this session).
- `run: string` — RunId ULID; the key every `agent.*`/`permit.*`/`run.*`/`action.*` fact carries — the run the action ran under. The reducer folds the measured spend onto this run's `permit_accumulators` (minting the run entry if absent — the log is truth, I3; an `action.metered` fact needs no prior `agent.spawned`, mirroring how permit/gate facts fold without a spawn).
- `spend_delta_usd: f64` — the **measured** dollars spent by this single action (post-action actual, not a pre-action estimate). This is the fold source `cumulative_spend_usd` sums (the arm that moved off `permit.*`; §reducer below). **Required, not optional** — an `action.metered` fact exists *because* an action was measured; an unmeasured action emits no fact (contrast the retired `permit.*.spend_delta_usd?`, which was optional because most permit decisions carried no spend). A measured-zero action is honest as `0.0`; absence-of-fact is honest as "not metered," so the field never needs the `?` optionality the permit field had.
- `input_tokens: u64`, `output_tokens: u64` — measured token counts for this single action, at per-**action** grain. Distinct from `agent.completed.cost.{input_tokens,output_tokens}`, which are per-**run** terminal totals. Recorded for per-action audit/attribution fidelity; whether a token accumulator is ever materialized is a follow-on (no `cumulative_tokens` fold is minted here — same recorded-only discipline as `permit.*.cost_ms?`).
- `action_ref?: CasRef` — optional ref to the action's identifying context (which tool call / argv this metering attributes to), persisted to the CAS; carried as a ref, never inline (I2). Optional: absent when the run+ordering on the log suffices to attribute the measurement. Bulk context is never inline.
- **New-subject checklist (recorded):**
  - *necessity* — a genuinely new lifecycle fact (a single action's **measured** resource cost, recorded post-action), NOT a payload field on an existing subject. Not a synonym of `agent.completed`: that carries `cost.{total_usd,input_tokens,output_tokens}` only as a per-**run** terminal total (DR-021's own framing — "already carries … but only as a per-RUN terminal total — too coarse"); `action.metered` is the per-**action** grain, which is the genuinely new thing a spend-cap fold needs (it must stop *after the action that crossed*, not only at run end). Not a synonym of the retired `permit.*.spend_delta_usd?` either: that was a *pre-action estimate on a decision fact* (the dishonest-if-summed accumulator DR-021 B2 rejected); this is a *post-action measured actual*. Has a folding consumer (`cumulative_spend_usd`) — not dead-letter.
  - *field-vs-subject (why NOT hang it off `agent.completed`, checked against DR-006 and DR-021)* — DR-021's context floats seeding B2 off `agent.completed`'s already-shipped `cost`/token fields, but that is a per-RUN terminal total on a run-lifecycle fact emitted once at reaper time; a per-ACTION metering fact is a genuinely new *event in time* (one per metered action, many per run) on a distinct grain, so it earns its own subject rather than a field on the one-per-run completion fact. This mirrors the `run.intent.declared`/`integrity.alarm` "genuinely new fact on a distinct key/grain → distinct subject" precedent, and is the deliberate contrast to the DR-014 `pep?` / DR-016 `role?` fields (those are spawn-time properties fixed once, correctly fields on `agent.spawned`). DR-021 §"What this does NOT decide" hands this shape call to this session; the per-action grain is what tips it to a subject. **No divergence from DR-021** — the DR ratified the *direction* (post-action, measured, off the permit fact) and left the subject shape open; `action.metered` as a standalone subject honors that direction and the taxonomy grammar.
  - *grammar* — `action.metered` parses strictly as past-tense `noun.verb` (`action` a NEW top-level noun; `metered` the past-tense fact that an action was metered). Conformant — **not grandfathered**. No existing subject renamed (never rename — deprecation only). New noun `action` justified per the DR-006/DR-008/DR-010 distinct-axis-→-distinct-noun precedent: post-action metering is a distinct axis from the pre-action `permit` "may" axis, the `run`-intent axis, and the `agent` lifecycle axis.
  - *family coherence* — new top-level noun `action` = the post-action metering axis. It folds onto the SAME `run`-keyed `AgentRunState` dossier that `agent.*`/`permit.*`/`run.*` fold onto (one entity, one dossier — the noun names an axis, not a new entity), exactly as `run.intent.declared` folds onto `AgentRunState` without fracturing the entity.
  - *emitter exists (named; minted by the C1 build, not this session)* — the harness PEP / run-substrate post-action metering path (DR-021 §Decision A→option 3). Naming it here keeps this from being a dead subject; wiring the emitter is C1 implementer scope.
  - *reducer consumer exists (obligation — the fold-arm MOVE, spec'd for the implementer, NOT built this session)* — see §reducer below. The measured `spend_delta_usd` folds into `AgentRunState.permit_accumulators.cumulative_spend_usd`. Because the accumulator field already exists (SP5 `PermitAccumulators`), the reducer change is a *move of the fold source*, not a new field.
  - *authority* — DR-021 (ACCEPTED, owner, 2026-07-20) §Decision A/B2 + §Consequences authorize the direction and name `action.metered` and the fold-arm move. The subject shape is this session's grant per DR-021 §"What this does NOT decide." No BINDING item is amended beyond DR-021's grant — no `/dr` pending.
  - *I2* — `run` is a ULID, `spend_delta_usd` a scalar f64, two `u64` token counts, and an optional `CasRef` — a payload of a few short scalars + one optional ref, orders of magnitude under the 32 KiB hard cap. DR-021 §Invariant-fit (I2) explicitly clears this: "the metering fact carries scalar $ + token counts, no bulk payload. No CAS pressure."
- **Reducer-arm fold spec (handed to the implementer — NOT built this session):** the `spend_delta_usd` → `cumulative_spend_usd` fold currently lives on the permit-decision reducer at `crates/rezidnt-state/src/lib.rs:725-726` (inside `apply_permit_decision`). Per DR-021 §reducer it **moves** to a new `"action.metered"` match arm in `apply` (the dispatch alongside `"permit.granted"` etc. at `:640-642`). Intended semantics:
  - Key on the payload `run` (`payload_run(event)`); a fact missing `run` folds counters-only / no-op, never panics — the established permit-reducer discipline (I3, "never guess a key").
  - Fold `payload["spend_delta_usd"].as_f64()` into `graph.agent_runs.entry(run).or_default().permit_accumulators.cumulative_spend_usd` (the SAME accumulator field the permit arm summed — the field does not move, only the *source fact* does). A measured `0.0` folds as a no-op-valued add; a denied action emits NO `action.metered` fact and therefore contributes ZERO to the cumulative (the B2 honesty property, DR-021 acceptance-criteria sketch #5).
  - Do **not** fold `input_tokens`/`output_tokens` into any accumulator (recorded-only; no `cumulative_tokens` — same discipline as `permit.*.cost_ms?`). A future token accumulator is its own change, not a payload break.
  - After the move, `apply_permit_decision` (`:725-726`) **stops reading `spend_delta_usd`** — the permit fact folds NO spend (DR-021 B2). `risk_delta` STAYS on the permit arm (`:728-729`) untouched (C6, out of DR-021 scope). **[Superseded by DR-024, 2026-07-20: the C6 wiring narrows the `risk_delta` fold to the `"granted"` arm ONLY (GRANTED-only, DR-024 Q3), and retires `risk_delta?` from `permit.denied`/`permit.escalated`. As of DR-021 the fold was all-outcomes and stayed on the permit arm; DR-024 keeps it on the permit arm but granted-only. See the DR-024 changelog entry.]** This is the load-bearing "fold-source move" acceptance criterion (#4): a `permit.*` fact folds no spend; an `action.metered` fact folds the measured spend.
- **`permit.granted` v1 / `permit.denied` v1 — `spend_delta_usd?` RETIRED as the C1 fold source (same session, DR-021 B2):** the `spend_delta_usd?: f64` accumulator-input bullet is **removed** from the `permit.granted` and `permit.denied` payload schemas (was at ~`:364` on granted, inherited at ~`:370` on denied). Per DR-021 B2 the pre-action permit decision folds NO spend, so the field leaves the permit payloads; the measured delta lives on `action.metered` instead. `risk_delta?` STAYS on all permit payloads (C6, untouched). **[Superseded by DR-024, 2026-07-20: `risk_delta?` is now RETIRED from `permit.denied` and `permit.escalated` too — DR-024 produces it only on `permit.granted` and folds it GRANTED-only, so it is dead on the other two decision facts. It remains ONLY on `permit.granted`, where it is the C6 fold source. See the DR-024 changelog entry.]** **`permit.escalated` also has `spend_delta_usd?` retired** — a warden coherence extend beyond the literal `granted`/`denied` scope, flagged in that payload's edit: the C1 fold source is now `action.metered` exclusively, so no `permit.*` fact sources it, and an escalated action (routed to a human, not yet run) is a pre-action estimate exactly like the others. Retiring it on all three decision facts is the coherent completion; leaving it on escalated alone would strand a dead pre-action spend field on the one fact where the action provably has not run.
- **Versioning note (why every subject stays `v = 1`):** *`action.metered`* is a brand-new subject, minted at `v = 1` per the taxonomy-v0 rule. *`permit.granted`/`permit.denied`* lose an **optional** field: readers already tolerate unknown/absent fields (additive-evolution rule, doc §5 — a `?` field's absence is always valid), and no live emitter produced `spend_delta_usd` on the permit fact (the C1 path was inert — DR-021 §Context: `DecisionDeltas::default()` hard-coded, `spend_delta_usd` never produced). Removing an optional field that was never emitted and whose absence readers already tolerate is **not a breaking payload change** — no reducer reads a value that will now be missing in a way that changes derived state (the fold that read it is being *moved*, not left dangling; see the reducer spec above, applied in the SAME session). So no `v+1` is minted. Had any live emitter been producing it, the honest move would be `v+1` on both permit subjects; it was not, so `v` stays 1. **The reducer arm that reads `permit.*.spend_delta_usd` MUST be updated in the same commit** (implementer obligation, spec'd above) so the field's retirement and the fold-source move land together — a half-applied change (field removed from ontology, reducer still folding it) would fold zero and silently under-count, which the DR-021 acceptance criteria (#4/#5) catch.
- **COLLISION with the taxonomy drift guard (companion edit — applied same-session, per the DR-008/DR-010/DR-017 precedent):** adding the `action.metered` table row makes `spec/ontology.md` declare 42 subjects vs. `rezidnt_types::taxonomy::SUBJECTS_V0`'s prior 41. `crates/rezidnt-types/tests/taxonomy_drift.rs` asserts the two match exactly (same set, ontology-table order) under `/vet`. **This session's warden protocol step 4 mandates the same-session companion edit** — `SUBJECTS_V0` gains `"action.metered"` under a new `// action` group at the **end of the list, after `"run.intent.declared"`** (matching the `action` section's position as the last subject section), applied this session. The two permit-field retires do NOT touch `SUBJECTS_V0` (the drift guard parses only `(subject, v)` table rows; field bullets and `permit.granted`/`.denied` stay `v = 1`). Not a pin↔ontology content collision — the const and the ontology agree on `action.metered`; the reducer + oracle fixtures the C1 implementer builds will agree on it and on the fold-source move.

### DR-029 set (ratified 2026-07-21 — C3 egress-mediation + brokered-credential facts)

DR-029 (ACCEPTED, owner, 2026-07-21) §Decision 6 requires this paired warden `/subject` to mint the real `egress.*`/`credential.*` subjects the c3-wire/c3-egress-fold run emits, replacing the DR-025/026/028 PLACEHOLDER strings in `compose.rs`/`egress.rs`/`runs.rs`. DR-029 names the family (`egress.mediated`/`egress.denied`/`egress.unavailable`/`credential.injected`) but hands the exact names, grammar-fit, and payload schemas to the warden ("the warden designs the taxonomy, not this DR"; "No ontology is minted in this DR"). This session mints **TWO new nouns** (`egress`, `credential`), **FIVE new subjects** (`egress.mediated`, `egress.unavailable`, `egress.denied`, `credential.injected`, `credential.dropped`), each with its payload schema + folding-reducer obligation (DR-006: no consumer-less subjects). No BINDING item is amended beyond DR-029's grant — no `/dr` pending. Additive; every subject minted at `v = 1`.

**Taxonomy judgment (recorded): `sandbox.*` folded into `egress.*` as a FIELD, not a parallel noun.** `compose.rs::compose_degrade` decides ONE composed state (the product of C3a sandbox availability × c3bc egress availability); a single decision must ride a single fact. So the three composed states map to `egress.mediated` (Mediated) + `egress.unavailable` (both ConfinedClosed AND Unsandboxed, disambiguated by the `sandbox` field), and the sandbox posture rides as a `sandbox: "available"|"unavailable"` field — the exact discipline `pep?`/`role?` follow (a spawn-time posture is a field on an existing fact, not its own subject). The alternative (parallel `sandbox.mediated`/`sandbox.unavailable` siblings) would fracture one `compose_degrade` decision across two subjects and require a consumer to correlate them to recover the single truth — rejected. `credential.*` IS a distinct noun (not folded into `egress.*`) because a brokered secret is a distinct entity (a `secret_ref`) with its own lifecycle and its own leak-discipline invariant.

**`egress.mediated` v1** — the enforcing composed-run posture (`ComposedDegrade::Mediated`; replaces the `sandbox.mediated` placeholder at `compose.rs::degrade_fact`). Not a degrade — its subject carries no `*.unavailable` marker.
- `run: string` — RunId ULID; the key every `egress.*`/`credential.*` fact folds on. The reducer folds this posture onto the run's dossier (`AgentRunState`), minting the run entry if absent (log is truth, I3 — a posture fact needs no prior `agent.spawned`).
- `network: "mediated"` — fixed on this subject: confined + egress-mediated over the shared netns.
- `sandbox: "available"` — the sandbox posture rode up (the composition requires it — `Mediated` is only reachable with the sandbox available).
- `egress_enforceable: bool` — `true` on this subject (the sealed netns exists; egress is the sole route out). Carried explicitly (not implied) so the honesty property is a readable fact, matching the placeholder payload the fold suites already pin.
- `backend?: string` — the composed backend label (`"pasta+bwrap"`), recorded for replay; inserted by `runs.rs::compose_spawn` (the placeholder already adds `backend`/`agent`). Optional/additive: absent on paths that do not label a backend.
- `agent?: string` — spec agent name, added by the spawn path for triage (matches the placeholder). Additive.
- **I2:** a ULID + three short enum/bool scalars + two short optional strings — ~150 B, orders under the 32 KiB cap. No CAS ref.

**`egress.unavailable` v1** — the CLOSED/unsandboxed loud degrade posture (`ComposedDegrade::ConfinedClosed` AND `ComposedDegrade::Unsandboxed`; replaces BOTH the `egress.unavailable` and `sandbox.unavailable` placeholders — one subject, the `sandbox` field carries the distinction). Loud, never a silent claim of mediation (I6; DR-026 CLOSED degrade / DR-028 §Decision 4).
- `run: string` — as `egress.mediated`.
- `network: "sealed" | "none"` — `"sealed"` for the confined+CLOSED floor (the sealed netns is KEPT, no network — DR-026); the unsandboxed floor carries no sealed netns and MAY omit or carry a distinct value — readers key on `sandbox`, not `network`, to tell the two apart (see below). (The placeholder emits `network="sealed"` on ConfinedClosed and omits `network` on Unsandboxed; both are valid — the discriminator is `sandbox`.)
- `sandbox: "available" | "unavailable"` — **the discriminator between the two floors.** `"available"` ⇒ confined + CLOSED (the sandbox held; the egress backend was down). `"unavailable"` ⇒ unsandboxed (the sandbox itself was down; no sealed netns for egress to be the sole route out of — the load-bearing sandbox-first dependency).
- `egress_enforceable: false` — fixed `false` on this subject: egress is NOT being enforced, on either floor. The honesty anchor — no silent claim of mediation.
- `injected: false` — fixed `false`: egress down does NOT mean open; nothing is injected (DR-026 — CLOSED degrade injects nothing).
- `reason: string` — a loggable human-readable degrade reason (I6 interrogability), distinct per floor (the placeholder emits `"egress backend unavailable — confined + CLOSED …"` vs. `"sandbox unavailable — unsandboxed spawn; egress un-enforceable …"`). A short diagnostic string (I2-safe; a large detail would ride a `CasRef`, never inline — it does not here).
- `backend?: string`, `agent?: string` — as `egress.mediated` (the spawn path adds them; `backend` is `"bwrap"` on the sandbox-only CLOSED floor, `"none"` on the unsandboxed floor). Additive.
- **I2:** a ULID + a handful of short scalars + one short `reason` string — ~200 B, far under the cap.

**`egress.denied` v1** — a per-**connection** off-allowlist denial (`egress.rs::Mediation::Deny`; the `EgressScope` `fail` verdict, DR-026 crit 3). A genuinely distinct event in time from the once-per-run posture facts (many denials per run), so a distinct subject, not a field.
- `run: string` — RunId ULID; the run the denied connection ran under. Folds onto the run dossier.
- `dest: string` — the denied (off-allowlist) destination host, recorded so *what was denied* is a fact, not a re-derivation.
- `policy_ref: CasRef` — the deciding egress policy (the allowlist that did not contain `dest`) as a CAS ref, so `gate_explain` can answer *why denied* against the exact policy (I6/I3). Bytes never ride inline (I2).
- `reason?: string` — a short human-readable denial note (e.g. `"destination not on the folded allowlist"`); optional/additive — `dest` + `policy_ref` are self-sufficient for triage, `reason` is the human-facing convenience.
- **I2:** a ULID, a host string, one CAS ref, an optional short string — well under the cap.

**`credential.injected` v1** — a brokered secret injected UPSTREAM on an approved mediated egress (`egress.rs::injected_fact`; DR-026 crit 4/5; replaces the `credential.injected` placeholder). **CRITICAL leak-discipline (I2/I3, DR-026 crit 5):** the payload carries ONLY the `secret_ref` (label/hash), the `dest`, and the `policy_ref` — **NEVER the value.** The value is redacted at the type level (`BrokeredSecret::{Debug,Display}` print `"<redacted>"`, reachable only via the audited `expose()` on the upstream-write path); `injected_fact` takes a `&BrokeredSecret` and lifts only `secret_ref()`, never `expose()` — so a value literally cannot ride this fact by construction. The `whole_emitted_fabric_carries_secret_ref_never_the_value` scan (DR-026 crit 5) forbids a careless impl from inlining `.expose()`.
- `run: string` — RunId ULID; the run the injection happened under. Folds onto the run dossier.
- `dest: string` — the allowlisted destination the secret was injected toward (the upstream host).
- `secret_ref: string` — the brokered secret's LABEL/HASH (`BrokeredSecret::secret_ref()`), the by-reference contract — the ONLY secret-identifying thing that ever rides a fact. **NEVER the value.**
- `policy_ref: CasRef` — the deciding egress policy as a CAS ref, so the injection is interrogable/replayable (I3/I6). Bytes never inline (I2).
- **I2:** a ULID, a host, a short ref label, one CAS ref — small; and by the leak-discipline above it *cannot* carry the (potentially large / secret) value.

**`credential.dropped` v1** — a `secret_ref` the `SecretSource` could NOT resolve, so the host→secret mapping was DROPPED with a loud fact (DR-029 §Decision 2 — never a fake/empty secret, never an injection of the empty string; the run proceeds mediated for hosts whose secrets resolved). A `credential.*` variant (NOT an `egress.*` degrade) because the dropped thing is a **credential** identified by its `secret_ref`; the honest floor of the credential-brokering axis.
- `run: string` — RunId ULID; the run whose fold dropped the mapping. Folds onto the run dossier. (May also be emitted at fold/startup time keyed on the workspace if no run exists yet — the emitter, C3-implementer scope, decides; the reducer folds keyed on `run` when present, keyless → no-op per the established discipline.)
- `dest: string` — the destination host the unresolvable secret was mapped to in `[egress.secrets]` — recorded so *which host lost its injection* is a fact.
- `secret_ref: string` — the unresolvable label (the `[egress.secrets]` value that `SecretSource::resolve` returned `None`/`Ok(None)` for). Carries NO value — there is none; that is the point (the fact is honest about the absence, never a fake token).
- `reason: string` — a loggable reason (e.g. `"secret_ref unresolvable by the configured SecretSource — mapping dropped, host mediated-without-injection"`), so *why uninjected* is interrogable (I6). Short diagnostic string (I2-safe).
- **I2:** a ULID + two short labels + a short reason — trivially under the cap; carries no secret value by construction.

**Reducer obligation (DR-006: no consumer-less subjects — spec'd for the C3 implementer, NOT built this session).** All five subjects fold onto the run-keyed `AgentRunState` (`crates/rezidnt-state/src/lib.rs`), the SAME dossier `agent.*`/`permit.*`/`run.*`/`action.*` fold onto — one entity, one dossier; the new nouns name axes, not entities. Each needs a `match` arm in `apply` keyed on the payload `run` (`payload_run(event)`; a keyless fact folds counters-only / no-op, never panics — the established permit/intent discipline, I3), and a new `#[serde(default)]` field on `AgentRunState` (so every pre-DR-029 golden fixture parses and compares equal unedited, I3 rebuild-stability — the exact pattern `intent`/`pep`/`role`/`delegations`/`integrity_alarms` use). Suggested shape (the implementer's oracle-first call to finalize): a single `egress: Option<EgressPostureState>` (last-write-wins the composed posture: `network`/`sandbox`/`egress_enforceable`/`backend`), a `Vec<EgressDenial>` for `egress.denied` (append order, replayable), a `Vec<CredentialInjection>` for `credential.injected` (append order — the by-ref audit trail, `secret_ref`/`dest` only), and a `Vec<CredentialDrop>` for `credential.dropped` (append order — the honest-floor audit trail). **These fields do NOT exist in `AgentRunState` yet** — flagged below for the implementer to add with the reducer arms (oracle-first). A subject with no consumer is not mintable; naming the reducer here discharges DR-006 while leaving the fold to the implementer, exactly as the `permit.delegated`/`action.metered` mints did.

**Placeholder-replacement map (for the implementer — the swap is C3-implementer scope, NOT applied here):**
- `crates/rezidnt-run/src/compose.rs::degrade_fact` — `"sandbox.mediated"` → **`egress.mediated`**; `"egress.unavailable"` (ConfinedClosed) → **`egress.unavailable`** (name now ratified, same string); `"sandbox.unavailable"` (Unsandboxed) → **`egress.unavailable`** (the two floors merge onto one subject, disambiguated by the `sandbox` field). The `#[cfg]`/doc WARDEN-GATED-placeholder comments (`compose.rs:175-187`) come out.
- `crates/rezidnt-run/src/egress.rs::injected_fact` — `"credential.injected"` → **`credential.injected`** (name now ratified, same string); the WARDEN-GATED-placeholder doc comment (`egress.rs:413-419, 435`) comes out.
- `crates/rezidnt-run/src/egress.rs` (`Mediation::Deny` path) — the denied-connection fact rides **`egress.denied`** (new emitter, C3-implementer scope; there is no placeholder string for it today — the `Mediation::Deny` verdict exists, the durable-fact emitter is the `c3-egress-fold` build).
- the DR-029 §Decision 2 drop path — rides **`credential.dropped`** (new emitter, C3-implementer scope; introduced by `c3-egress-fold`).
- `bins/rezidentd/src/runs.rs::compose_spawn` (`runs.rs:801-808`) — publishes `degrade_fact`'s `(subject, payload)` and augments it with `agent`/`backend`; the placeholder comment (`runs.rs:800-803`) comes out once `degrade_fact` returns ratified names.

**COLLISION with the taxonomy drift guard (companion edit — applied same-session, per the DR-008/DR-010/DR-017/DR-021 precedent):** adding FIVE table rows makes `spec/ontology.md` declare 47 subjects vs. `rezidnt_types::taxonomy::SUBJECTS_V0`'s prior 42. `crates/rezidnt-types/tests/taxonomy_drift.rs` asserts the two match exactly (same set, ontology-table order) under `/vet`. **Warden protocol step 4 mandates the same-session companion edit** — `SUBJECTS_V0` gains the three `egress.*` entries (new `// egress` group after `"action.metered"`) and the two `credential.*` entries (new `// credential` group), in ontology-table order, applied this session; the doc-comment subject count updates 42→47. Not a pin↔ontology content collision — the const and the ontology agree; the reducer + oracle fixtures the C3 implementer builds will agree on the five subjects and their fold.

## Changelog

- 2026-07-16 · warden · bootstrap: taxonomy v0 transcribed from architecture doc v0.2 Appendix B; DR-001 additions `agent.tool.invoked` and `agent.message` (native harness telemetry); DR-001 scope note on `worktree.observed`/`worktree.conflict` (out-of-band guard only, rezidnt sole allocator); all subjects minted at `v = 1`.
- 2026-07-16 · warden · S1 payload ratification: v1 payload baselines recorded for `workspace.opened`, `workspace.spec.applied`, `worktree.allocated`, `agent.spawned`, `agent.status.changed`, `agent.completed`, `agent.signaled`, `agent.tool.invoked`, `agent.message`, `artifact.captured` — additive documentation of shape, every subject stays `v = 1`; `artifact.captured` sketch normalized (top-level `mime`/`bytes` subsumed into `ref: CasRef`); capture chunks ride `artifact.captured` via `provenance.kind = "capture-chunk"` + `provenance.chunk`, dedicated capture subject deferred and flagged for `/dr`.
- 2026-07-17 · warden · S2 payload ratification: v1 payload baselines recorded for `worktree.observed`, `worktree.conflict`, `worktree.released`, `diff.ready` — strict supersets of the S2 oracle-pinned minimums (adapter tests, `s2_worktrees.rs` reducer tests, `spec/fixtures/s2_*.jsonl`); additive-only, every subject stays `v = 1`, no reducer/fixture changes required. Additive fields beyond the pinned minimum: `branch?` on observed/released, `claimed_path?` + `holder?` on conflict. Open items left tracked, not expanded: `daemon.warning` payload ratification, `badge.issued` emit-or-drop, capture-chunk subject (still flagged for `/dr`).
- 2026-07-17 · warden · S3 payload ratification: v1 payload baselines recorded for `gate.entered`, `gate.failed`, `gate.inconclusive`, `gate.explained` — strict supersets of the S3 oracle pins (`gate_explain.rs`, `mcp_http.rs`, `spec/fixtures/s3_gate_*.jsonl`); additive-only, every subject stays `v = 1`, no reducer/fixture/type changes required; verdict carried by the subject, `inputs` recorded verbatim per the §8 stdin contract (I6). `gate.passed` baseline deliberately not ratified (no S3 emitter/pin — S4 scope). Operator-badge concept noted in the badge section as a §12/DEFAULT doc-level matter, not fabric surface. Deferred with reasons: `badge_id` as an additive field on mutation facts (S3 board ties calls to the log via the acked `correlation`; no pin, no consumer — ratifying a cross-subject attribution convention without a folding reducer risks dead-letter fields); `badge.issued` emit-or-drop (no S3 pin or emitter, and the operator badge adds a daemon-lifetime-scope question that should be settled in the same session as the `badge_id` decision). Capture-chunk subject remains tracked and flagged for `/dr` — not resolved here.
- 2026-07-17 · warden · S4 payload ratification: (1) `gate.passed` v1 ratified — now has an emitter (S4 gate engine), so the S3-deferred baseline is minted: `{run, gate, verifiers: [{verifier, cost_ms, evidence: [CasRef], inputs}]}`, per-verifier records for replay + recorded cost; documented the intentional single-failing-verifier (`gate.failed`) vs. all-verifiers (`gate.passed`) asymmetry (first-failure short-circuit vs. full-pass evidence). (2) NEW subject `diff.merged` v1 `{run, worktree, diff: CasRef}` — golden-path merge/worktree-lifecycle-close fact; full new-subject checklist passed (grammar, `diff` family coherence, git-adapter emitter, S4 reducer consumer exists), distinct from v0 `merge.completed`. (3) `agent.spawned` governed-spawn additive fields `bare?: bool`, `allowed_tools?: [string]` (and cross-reference to the pre-existing `harness_version?`) — DR-001 enforcement-decisions-recorded-in-events; additive, independent of `idempotency_key?`/`badge_id`, badge deferrals not foreclosed. All subjects stay `v = 1`; strict supersets of the S4 pins; no oracle test/fixture edits. I2 verified: inputs doc ~182 B, 3-verifier `gate.passed` ~838 B — far under 32 KiB. COLLISION reported: the new `diff.merged` table row requires a same-commit one-line addition of `"diff.merged"` to `rezidnt_types::taxonomy::SUBJECTS_V0` (after `"diff.ready"`) or `taxonomy_drift.rs` fails `/vet`; flagged for the implementer since this session was scoped to `spec/ontology.md` only (scope collision, not a pin↔ontology content collision). Open questions: (a) exec-verifier network opt-in recorded inside the verbatim `inputs.params` — DECIDED, no new field; (b) replay-divergence integrity alarm as a durable log fact — TRACKED for `/dr` (CLI-report-only per the pins; a log fact would be additive, no consumer today).
- 2026-07-17 · warden · cleanup queue (4 items, no reducer/fixture/type edits — `spec/ontology.md` only): **(1) `gate.inconclusive.reason` gains `could_not_run`** — additive string under the existing "new causes arrive additively" rule (v stays 1); means the verifier program could not be executed (spawn/exec failure, nothing ran), distinct from `malformed_output` (a program that ran but emitted unparseable output); stays `Inconclusive`, never coerced (I6); wire form `"could_not_run"` pinned by `crates/rezidnt-gate/tests/exec_could_not_run.rs`. The `InconclusiveReason::CouldNotRun` enum variant is the implementer's same-board companion edit (compile-red until it lands); the ontology string is ratified here. **(2) DR-005 follow-on** — annotated `badge.issued` and `badge.revoked` as reserved subjects with **NO emitter in v1** (issuance attributable via `agent.spawned.badge_id`); updated the badge-section note that the emit-or-drop question is decided (drop) and the operator badge is a blessed daemon-lifetime class. The DR-005 §12 badge-rule narrowing ("every *state-mutating* call"; interrogation + `tail_events` read-class/unbadged) is a doc §12 policy matter, **out of ontology scope** — cross-referenced in the badge note, no ontology entry minted. **(3) `daemon.warning` v1 payload ratified** (carried item, closed) — `{what: string, error: string}` read from the sole live emitter `bins/rezidentd/src/runs.rs::warn_open_failed`; `what` vocabulary in use is `{"open-failed"}` (only value any emitter produces), additive thereafter; `error` is a short diagnostic string (I2-safe; large detail → `CasRef`, never inline). `daemon.error` baseline left unratified (no live emitter, same discipline as S3 `gate.passed`). **(4) `worktree.conflict` emission wording corrected to at-least-once** (S2 re-debrief T2 honesty fix, not a design change) — the adapter emits then persists the `conflicted` mark (set-flag → `emit` → `persist_registry` in `crates/rezidnt-adapters/git/src/lib.rs`), so a crash between emit and persist loses the mark and re-emits on restart; crash-free path stays exactly-once via the persisted mark; reducer/consumer treats the fact as idempotent per contested `path` (I3). Collision reported: the prior "exactly one per collision … forever" wording over-claimed vs. the code's real semantics — ontology corrected to match the code (ontology-vs-code honesty, not ontology-vs-pin). Residual flag (out of file scope): the git adapter module doc still says "exactly one … forever" and should be aligned by the implementer. — records the caller-supplied spawn idempotency key so the key→run map rebuilds from the log after restart (behavior pinned by `mcp_workspace_recovery.rs::spawn_key_idempotency_survives_daemon_restart`; mechanism ratified here, not in the test). Named to match `SpawnAgentArgs.idempotency_key` (cross-subject convention: `OpenWorkspaceArgs` carries the same name). Optional because non-MCP spawn paths are keyless; non-empty, ≤ 256 bytes UTF-8 (DEFAULT cap); dedup scoped to (envelope `workspace`, key). Existing fixtures unchanged (field absent = valid); no reducer or type edits required. The open `badge_id`-on-mutation-facts and `badge.issued` deferrals are untouched and not foreclosed.
- 2026-07-17 · warden · DR-008/DR-009 permit-engine subject mint (SP5): NEW top-level noun `permit` and FOUR new subjects **`permit.requested`**, **`permit.granted`**, **`permit.denied`**, **`permit.escalated`** — the pre-hoc authorization ("may") axis, per owner-ratified DR-008 (ACCEPTED, authorizes the four by name) + DR-009 (ACCEPTED, sizes the contextual payload); design basis `docs/design/permit-engine.md` §4/§5/§8, intel memo 001 C6/C7 sizing. Verdict carried by the subject (never a bare boolean): `permit.granted`=allow(`pass`), `permit.denied`=deny(`fail`), `permit.escalated`=inconclusive→route-to-human(`ask`), never coerced (I6, DR-008 §4); no `permit.allowed` synonym minted. Payloads: `permit.requested {run, request_id, action, target(inline small descriptor), context_ref?: CasRef, badge_id?}` — bulk action context (argv/diffs/file bytes) is a CAS ref, never inline (I2); decision facts `{run, request_id, policy_ref: CasRef, evidence_ref?: CasRef, cost_ms?, reason(denied/escalated), spend_delta_usd?, risk_delta?}` — `policy_ref`+`evidence_ref` give `gate why`/`gate_explain` the deciding policy + evidence (I6), and the optional `spend_delta_usd`/`risk_delta` size the payload for the downstream contextual/stateful C1/C7 verifiers to fold per-session accumulators (running risk score, cumulative spend) without a `v+1`. Correlation: `permit.requested` opens the chain (envelope `correlation`); decisions carry `causation`=request id and an explicit `request_id` ledger key (log-derivable, I3). Consumers (no consumer-less subjects — DR-006 precedent): `rezidnt-state` folds a per-run permit **ledger** (request→decision) + per-session **accumulators** (`Graph.permit_accumulators`); this session mints subjects + reducer scaffolding only — the verifiers are SP0–SP4. Full new-subject checklist passed (necessity/grammar/family-coherence/emitter/reducer/verdict-mapping/authority/I2). Companion edit **applied same-session** (taxonomy-drift precedent, unlike S4/DR-006 which deferred it): `rezidnt_types::taxonomy::SUBJECTS_V0` gains the four `permit.*` entries (new `// permit` group at the end, after `"integrity.alarm"`), keeping `taxonomy_drift.rs` green under `/vet`. Additive; every subject minted at `v = 1`. This session touched `spec/ontology.md`, `crates/rezidnt-types/src/taxonomy.rs`, and `crates/rezidnt-state/src/lib.rs` (reducer scaffolding).
- 2026-07-17 · warden · DR-006 subject mint: NEW top-level noun `integrity` and NEW subject **`integrity.alarm` v1** `{run, gate, verifier, recorded: <verdict>, replayed: <verdict>}` — the durable log fact of a replay divergence (recorded ≠ replayed) raised by `debrief`, per owner-ratified DR-006 (ACCEPTED). Emitter: the daemon single writer (DR-006 routes the `debrief` append through the sole log writer, I3); CLI keeps its direct read for `report.alarms[]`/exit 3, to which the durable fact is strictly additive. Consumer: `rezidnt-state` folds to `agent_runs[run].integrity_alarms` (NOT dead-letter). Full new-subject checklist passed: grammar (`integrity.alarm` parses strictly `noun.verb`, past-tense-ish fact — NOT grandfathered); family coherence (new noun `integrity` = log/replay-integrity axis, distinct from `gate.*` verdicts and `daemon.*` lifecycle — DR-006 explicitly rejected reusing `daemon.error` AND polluting gate vocab); emitter + folding reducer both exist (board-scaffolded). Dedup: at-least-once on the wire (like `worktree.conflict`), deduped by `(run, gate, verifier)` at both APPEND (daemon reads existing alarm facts off the log, I3) and FOLD (duplicates collapse to one queryable record); evidence stays CAS-ref'd on the originating `gate.*` fact (I2; ~120 B payload). Strict superset of the pins `crates/rezidnt-state/tests/dr006_integrity_alarms.rs` (FOLD) and `bins/rezidentd/tests/golden_path.rs::dr006_*` (daemon-routed EMIT); mirrors `rezidnt_gate::IntegrityAlarm`. **Resolves ontology open-question (b)** ("replay-divergence integrity alarm as a durable log fact — TRACKED for `/dr`") — closed by DR-006; (b) annotated RESOLVED above. COLLISION reported (scope, not content): the new table row makes the ontology declare 35 subjects vs. `SUBJECTS_V0`'s 34 — requires a same-commit one-line companion edit inserting `"integrity.alarm"` into `rezidnt_types::taxonomy::SUBJECTS_V0` at the END (after `"badge.revoked"`, new `// integrity` group), or `taxonomy_drift.rs` fails `/vet`; flagged for the main thread since this session was scoped to `spec/ontology.md` only. Additive; subject minted at `v = 1`; no oracle test/fixture edits. This session touched `spec/ontology.md` only.
- 2026-07-18 · warden · DR-014 enforcement-mode visibility (FIELD, not a subject): added `pep?: "enforced"` to `agent.spawned` (S1 baseline; new **DR-014 set** note block) per owner-ratified DR-014 (ACCEPTED, 2026-07-18) §Decision 5, which committed the signal but left the field-vs-subject design to this session. **Chose the field over a new `permit.enforcement.declared` subject.** Semantics: records whether the permit PEP (the `rezidnt permit-hook` `PreToolUse` hook) was wired at spawn, so `gate_explain` honestly distinguishes *mid-run-PEP-enforced* (`pep = "enforced"`) from *edge-gated-only* (field **absent**) — I4 degradation honesty. Value fixed at spawn (opt-in keyed on `[gates.permit]`, DR-014 §Decision 2; never changes post-spawn → a spawn-time property, i.e. a field, not a lifecycle event). Absence is honest "no PEP wired" — never synthesized to `false`/`"unenforced"` (DR-012 declared-vs-absent discipline; no declared-empty state exists — PEP is wired or not). Enum string not bool, so a future degraded/partial mode arrives additively without `v+1` (v1 emits only `"enforced"`). Direct `bare?`/`allowed_tools?` precedent (a spawn-time enforcement property recorded as a field on the run's existing spawn fact). **Field-vs-subject justified against DR-006:** a new subject would need a folding consumer folding onto the SAME `AgentRunState`/`run` key `agent.spawned` already folds — redundant with the fact on the log; and would misplace the signal on the `permit.*` action-request axis (keyed on the request, DR-008) rather than the run's spawn (`agent.*` axis). The `run.intent.declared`/`integrity.alarm` "distinct axis → distinct subject" precedent does NOT apply (those were genuinely new facts on distinct keys; this is a property of the spawn fact). **Reducer obligation (DR-006 read for a field = the reducer must surface it):** the folding consumer is the existing `agent.spawned` reducer arm in `crates/rezidnt-state/src/lib.rs` (~line 329) — currently folds `run → status` only, does not yet read `bare?`/`allowed_tools?`/`pep?`; the implementer's follow-on (criterion 6, oracle-first) extends it to fold `pep?` onto a new `#[serde(default)]` `AgentRunState` field that `gate_explain` (`crates/rezidnt-mcp/src/lib.rs`) reads. **Drift guard: subject list UNCHANGED** — a field, not a subject; ontology still declares 40 table rows, `taxonomy_drift.rs` parses only `(subject, v)` rows and `agent.spawned` stays `v = 1`, so **no `SUBJECTS_V0` / hash companion edit** (unlike the S4/DR-006/DR-008/DR-010 sessions which each added a row). Additive; `agent.spawned` stays `v = 1`. This session touched `spec/ontology.md` only.
- 2026-07-18 · warden · DR-010 run-intent subject mint (SP-intent process gate): NEW top-level noun `run` and NEW subject **`run.intent.declared` v1** `{run: string, intent_ref: CasRef, allowed_tools: [string]}` — the run-intent axis (least-privilege in time), per owner-ratified DR-010 (ACCEPTED 2026-07-18) §4, which authorizes exactly this subject by name and shape; design basis `docs/design/intent-lock.md` §5, intel memo 001 C7. A run's initiating intent (`intent_ref` — the task/prompt text, ALWAYS a CAS ref, never inline, I2) + the intent-scoped tool set (`allowed_tools`) the future `intent-lock` permit-verifier (SP-intent) enforces. Content-pinned so the verifier reads it as a deterministic replayable input, never a live re-derivation (determinism BINDING; DR-010 §3). Full new-subject checklist passed: *necessity* — new lifecycle fact with a folding consumer, distinctness from `agent.spawned.allowed_tools?` load-bearing (DR-010 §4: intent-derived least-privilege set vs. composed harness allowlist — distinct axis, distinct subject; DR-006 precedent); *grammar* — parses as the three-segment house form `noun.qualifier.verb` (`run`·`intent`·`declared`, like `workspace.spec.applied`/`agent.status.changed`), past tense, NOT grandfathered; *family coherence* — NEW noun `run` for the run-intent axis (DR-006/DR-008 precedent: distinct axis → distinct noun), folds onto the EXISTING `AgentRunState` keyed on the `run` ULID (no fractured entity; the `agent.*` lifecycle facts stay grandfathered, unrenamed) — noun confirmed as ratified in DR-010 §4 by name (renaming to `agent.intent.declared` would override a same-day owner-ratified name → `/dr`, out of scope); *emitter* — the run/spawn intent-declaration path (design §3(a)), minted by SP-intent NOT this session (named so it is not a dead subject); *reducer consumer* — BUILT this session: `rezidnt-state` folds to `AgentRunState.intent: Option<IntentState { allowed_tools, intent_ref }>` (keyless fact → counters-only no-op, never panics, I3; rebuild-stable under `fold(log)==snapshot`); *I2* — small scalars + one CAS ref, far under 32 KiB. Companion edit **applied same-session** (work order mandate / DR-009 SP5 taxonomy-drift precedent, unlike S4/DR-006 which deferred it): `rezidnt_types::taxonomy::SUBJECTS_V0` gains `"run.intent.declared"` (new `// run` group at the end, after `"permit.escalated"`; count 39→40) keeping `taxonomy_drift.rs` green under `/vet`. Additive; subject minted at `v = 1`. Scope discipline: subject + reducer scaffolding ONLY — the `intent-lock` verifier and its oracle tests are the NEXT slice (SP-intent, oracle-first), deliberately not written here. This session touched `spec/ontology.md`, `crates/rezidnt-types/src/taxonomy.rs`, and `crates/rezidnt-state/src/lib.rs` (+ scaffold test `crates/rezidnt-state/tests/run_intent.rs`).
- 2026-07-19 · warden · DR-017 SP4b delegation subject mint (macaroon-attenuated delegation): NEW subject **`permit.delegated` v1** `{run: string, parent_badge_id: string, child_badge_id: string, added_caveats: [Caveat]}` under the existing `permit` noun (the pre-hoc "may" axis, DR-008) — the durable capability-chain fact that a lead agent's macaroon badge was attenuated (a narrowing caveat appended, re-keyed offline — the delegation property) to mint a child badge for a sub-agent spawn. Per owner-ratified DR-017 (ACCEPTED 2026-07-19) §Decision 2, which authorizes exactly this subject + shape by name and **explicitly chose a SUBJECT over a field** — a delegation is a new *event in time* (attenuation happens when a lead spawns a sub-agent), unlike the SP4a `role?` / DR-014 `pep?` spawn-time FIELDS on `agent.spawned`. Design basis `docs/design/permit-macaroon-delegation-sp4b.md` §4 (caveat shape) / §7 (delegation fact). `parent_badge_id`/`child_badge_id` follow the `blake3(identifier)[..8]` convention (loggable, NEVER the token — the macaroon lives only under `REZIDNT_BADGE`, never on the fabric; I2/§12). `added_caveats` are small, deterministically-serialized, tagged first-party predicates — each a `Caveat` = exactly one of `{kind:"workspace", workspace}` / `{kind:"verb", verbs:[…]}` / `{kind:"expiry", not_after}` (RFC3339, evaluated against a PASSED-IN timestamp, never ambient `now()` — I6, DR-017 §Decision 3) / `{kind:"role", role}` (opaque, DR-016 discipline); new kinds arrive additively as `kind` strings. Full new-subject checklist passed: *necessity* (new lifecycle fact, NOT a field — DR-017 chose subject-over-field; NOT a synonym of the `permit.granted/denied/escalated` decision facts — those are PDP decisions keyed on `request_id`, this is a capability-chain fact keyed on `run`, distinct payload with no verdict/`policy_ref`); *grammar* (`permit.delegated` parses strictly past-tense `noun.verb`, NOT grandfathered); *family coherence* (extends the existing `permit` noun — a delegation is a "may" narrowed + handed off — no new noun minted); *emitter* (the daemon PDP on attenuation; macaroon crypto is SP4b implementer scope); *reducer consumer* (OBLIGATION recorded, NOT built — DR-017 §Decision 2 requires a folding reducer; folds onto the run dossier `AgentRunState` in `crates/rezidnt-state/src/lib.rs` keyed on `run`, consistent with how `permit_ledger`/`permit_accumulators` fold — implementer's SP4b oracle-first follow-on adds a `#[serde(default)]` `delegations` field, keyless fact = no-op, rebuild-stable, I3); *authority* (DR-017 §Decision 2, ACCEPTED — no BINDING amended beyond DR-017's grant, no `/dr` pending); *I2* (two short `badge_id`s + a few small tagged caveats, ~hundreds of bytes, far under 32 KiB — token never inline, no CAS ref). Companion edit **applied same-session** (DR-008/DR-010 taxonomy-drift precedent, unlike S4/DR-006 which deferred it): `rezidnt_types::taxonomy::SUBJECTS_V0` gains `"permit.delegated"` in the existing `// permit` group after `"permit.escalated"` (count 40→41), keeping `taxonomy_drift.rs` green under `/vet`. Additive; subject minted at `v = 1`. Scope discipline: subject + payload schema + reducer OBLIGATION only — the folding reducer, the macaroon crypto (`check_badge` verify/attenuate), and their oracle tests are the implementer's SP4b work (oracle-first), deliberately NOT written here. This session touched `spec/ontology.md` and `crates/rezidnt-types/src/taxonomy.rs`.
- 2026-07-20 · warden · DR-018 §Decision (a)/(b) macaroon `badge_id` derivation change (additive wording, no subject added/renamed): re-derived the loggable macaroon `badge_id` from `blake3(identifier)[..8]` → `hex(blake3(sig)[..8])` (an 8-byte hex prefix of `blake3` over the badge's running MAC sig), per owner-ratified DR-018 (ACCEPTED 2026-07-20) §Decision (a). Rationale: a faithful `Macaroon::attenuate` **preserves the identifier**, so identifier-derived ids collide between parent and child and the `permit.delegated` edge collapses to a self-loop; the running sig re-keys per appended caveat, so deriving from the sig makes a *true* same-identifier `attenuate` yield distinct parent/child edge ids **while preserving the offline-delegation property** (only the sig-derived form keeps SP4b's raison d'être intact). We hash `blake3(sig)`, not the raw sig bytes, so no bytes of the MAC land on the fabric. **`badge_id` shape is unchanged** — an 8-byte hex loggable prefix, NEVER the token (I2/§12); only the **pre-image** changes. **NOT a payload schema break** — `badge_id` / `parent_badge_id` / `child_badge_id` stay `string`, every subject stays `v = 1` (an opaque loggable id with no consumer parsing its pre-image; re-deriving `Macaroon::badge_id()` is a definition change, not a schema change). Edits (all `spec/ontology.md`, additive/clarifying only): (1) `permit.delegated.parent_badge_id` (~410) + `child_badge_id` (~411) — derivation reworded to the sig-derived form, "loggable, NEVER the token" intact, DR-018 cited; (2) `permit.delegated` semantics row (~156) — corrected per DR-018 §Decision (b) to a **real `attenuate` at the daemon boundary** (caveat appended + sig re-keyed offline-of-the-root-key), with the **holder-offline process boundary named as DEFERRED to a later SP4 sub-slice** (does not overclaim holder-offline delegation); (3) `agent.spawned.badge_id` general note (~191) — added a one-line DR-018 cross-reference to the sig-derived form and disambiguated the macaroon (agent) badge (sig-derived) from the DR-005 daemon-lifetime **operator** badge (not a macaroon, id derivation untouched by DR-018). **Drift guard UNCHANGED** — no subject added, renamed, or removed and no `v` bumped, so `rezidnt_types::taxonomy::SUBJECTS_V0` (41 subjects) and `taxonomy_drift.rs` (parses only `(subject, v)` rows) stay green with **no companion edit** (no `SUBJECTS_V0` row / drift-hash change; the line-355 `badge_id?` DR-005 caller-badge field on the mutation-request path is a different class and was left untouched). Additive; every subject stays `v = 1`. Scope discipline: `spec/ontology.md` only — the `Macaroon::badge_id()` re-derivation, the producer's `attenuate` call (`bins/rezidentd/src/runs.rs`), and their oracle tests are the implementer/oracle SP4b follow-on, deliberately NOT written here. This session touched `spec/ontology.md` only.
- 2026-07-20 · warden · SP5 close-out `cost_ms` semantics note (NO field/subject/schema change): annotated the canonical `permit.granted.cost_ms?` definition (~362) as **intentionally recorded-only** — carried on the decision fact for per-decision latency/audit fidelity (design §10.2 signal) but with **no folding accumulator**: `rezidnt-state`'s `apply_permit_decision` (`crates/rezidnt-state/src/lib.rs:719-730`) folds `spend_delta_usd`/`risk_delta` into `PermitAccumulators` and deliberately does NOT sum `cost_ms` (there is no `cumulative_cost_ms`). Resolves the SP5 close-out "defined-but-unfolded" question by owner decision (recorded-only, NOT materialize an accumulator); the missing fold is intentional, not a reducer bug. Reading `cost_ms` back is a per-fact log-derivable read (I3); a future accumulator, if ever wanted, is its own change (new `PermitAccumulators` field + reducer arm), not a payload break. `permit.denied` (~368)/`permit.escalated` (~373) inherit the note via their "exactly as `permit.granted`" `cost_ms?` lines — single annotation, no duplicate edits. **No `v`-bump** (payload schema unchanged, field stays `cost_ms?: u64`), **no new subject**, **no `SUBJECTS_V0` / `taxonomy_drift.rs` change** (contrast the DR-008 mint which added rows — this adds no row, renames nothing, bumps no `v`; drift guard parses only `(subject, v)` rows and stays green). Not BINDING — no invariant/DR amended, so no `/dr` routing. Scope discipline: `spec/ontology.md` only — deliberately NO reducer/`.rs`/fixture edit (recorded-only was the owner's choice; there is no fold to add). This session touched `spec/ontology.md` only.
- 2026-07-19 · warden · DR-016 SP4a RBAC role (FIELD, not a subject): added `role?: string` to `agent.spawned` (S1 baseline; new **DR-016 set** note block) per owner-ratified DR-016 (ACCEPTED, 2026-07-19) §Decision 2, which committed the field by name as required SP4a warden work (`role: Option<String>` on `AgentSpec`, recorded on `agent.spawned`, injected into `decide_permit`'s per-run params) but does NOT design the role taxonomy. **Chose the field over a new subject** — a direct precedent-match to the DR-014 `pep?` field. Semantics: the agent's RBAC role taken verbatim from `AgentSpec.role`, an OPAQUE string the policy interprets, recorded so permit policies key decisions on role + workspace + action (permit-engine §7) and both native + exec permit verifiers read it as a new input axis on the existing SP1–SP3 dispatch (no new verifier machinery). Value fixed at spawn (rides the run's spawn-time params; never changes post-spawn → a spawn-time property, i.e. a field, not a lifecycle event). Absence is honest "no role declared" — never synthesized to a default like `"contributor"` (DR-012 declared-vs-absent; exact mirror of the `bare?`/`allowed_tools?`/`pep?` absent-is-honest discipline). A short scalar (I2-safe; no CAS ref). Independent optional — distinct axis from `pep?` (enforcement mode), `allowed_tools?` (composed tool allowlist), `harness_version?`, `idempotency_key?`, `badge_id`. **Field-vs-subject justified against DR-006:** a new subject would need a folding consumer folding onto the SAME `AgentRunState`/`run` key `agent.spawned` already folds — redundant with the fact on the log; and a `permit.*` subject would misplace a permit *input* on the action-request "may" axis (keyed on the request, DR-008) rather than the run's spawn (`agent.*` axis). The `run.intent.declared`/`integrity.alarm` "distinct axis → distinct subject" precedent does NOT apply (those were genuinely new facts on distinct keys; the role is a property of the spawn fact). **Reducer obligation (DR-006 read for a field = the reducer must surface it):** the folding consumer is the existing `agent.spawned` reducer arm in `crates/rezidnt-state/src/lib.rs` (~line 350) — already folds `run → status` and reads `pep?`; the implementer's SP4a follow-on (oracle-first) extends it to fold `role?` onto a new `#[serde(default)]` `AgentRunState` field (e.g. `role: Option<String>`, the same pattern `AgentRunState.pep`/`.intent` use) that `decide_permit` reads to inject into the permit input params. **Drift guard: subject list UNCHANGED** — a field, not a subject; ontology still declares 40 table rows, `taxonomy_drift.rs` parses only `(subject, v)` rows and `agent.spawned` stays `v = 1`, so **no `SUBJECTS_V0` / hash companion edit** (identical to the DR-014 `pep?` session; unlike S4/DR-006/DR-008/DR-010 which each added a row). Additive; `agent.spawned` stays `v = 1`. This session touched `spec/ontology.md` only.
- 2026-07-20 · warden · DR-021 C1 live spend-cap: NEW subject **`action.metered` v1** + RETIRE `spend_delta_usd?` from `permit.granted`/`permit.denied`/`permit.escalated`. Per owner-ratified DR-021 (ACCEPTED 2026-07-20) §Decision A→option 3 (post-action attribution) + §Decision B2 (spend attribution moves OFF the pre-action permit decision); DR-021 §Consequences names `action.metered` and the reducer fold-arm move; §"What this does NOT decide" hands the subject shape to this session. NEW top-level noun `action` (post-action metering axis) minted per the DR-006/DR-008/DR-010 distinct-axis-→-distinct-noun precedent. **`action.metered` v1** `{run: string, spend_delta_usd: f64, input_tokens: u64, output_tokens: u64, action_ref?: CasRef}` — the per-**action** MEASURED post-action metering fact; `spend_delta_usd` is REQUIRED (a metered action's actual, `0.0` for a measured-zero action; contrast the retired optional permit field) and is the C1 fold source; tokens recorded-only (no `cumulative_tokens`, same discipline as `cost_ms?`). Distinct from `agent.completed.cost` (per-RUN terminal total — too coarse; DR-021's own framing) and from the retired `permit.*.spend_delta_usd?` (a pre-action estimate on a decision fact — the dishonest-if-summed accumulator B2 rejected). Full new-subject checklist passed: *necessity* (new per-action lifecycle fact w/ folding consumer, not a synonym of `agent.completed` or the retired permit field), *field-vs-subject* (per-action grain + genuinely-new-event-in-time tips it to a subject over an `agent.completed` field — the `run.intent.declared`/`integrity.alarm` precedent; DR-021 left the shape open, no divergence), *grammar* (`action.metered` parses strictly past-tense `noun.verb`, NOT grandfathered), *family coherence* (new `action` noun, folds onto the SAME `run`-keyed `AgentRunState` — one entity, one dossier), *emitter* (harness PEP / run-substrate post-action path — minted by the C1 build, NOT this session), *reducer consumer* (the fold-arm MOVE spec'd for the implementer, NOT built here), *authority* (DR-021 §A/B2 + §Consequences — no BINDING amended beyond DR-021's grant, no `/dr` pending), *I2* (scalar $ + token counts + optional ref, far under 32 KiB; DR-021 §Invariant-fit clears it). **RETIRE (same session, DR-021 B2):** `spend_delta_usd?` removed from `permit.granted` (~364), `permit.denied` (~370), AND `permit.escalated` — the last is a **warden coherence extend beyond the literal `granted`/`denied` work order, explicitly flagged** in that payload's edit (the C1 fold source is now `action.metered` exclusively, so no `permit.*` fact sources spend; an escalated action has not run, so a pre-action spend delta is the same B2-rejected estimate). `risk_delta?` STAYS on all three (C6, untouched). **No `v`-bump on any subject** — the retired field is optional AND was never emitted by a live producer (C1 path inert, `DecisionDeltas::default()` hard-coded, DR-021 §Context), so removing it is not a breaking payload change (doc §5 additive-evolution: absence of a `?` field is always valid; no reducer is left reading a now-missing value — the fold that read it is MOVED, not stranded, in the same commit). **Reducer-arm fold spec handed to the implementer (NOT built this session):** the `spend_delta_usd`→`cumulative_spend_usd` fold at `crates/rezidnt-state/src/lib.rs:725-726` (in `apply_permit_decision`) MOVES to a new `"action.metered"` match arm in `apply` (alongside `:640-642`), keyed on payload `run` (keyless → no-op, I3), folding `payload["spend_delta_usd"].as_f64()` into `AgentRunState.permit_accumulators.cumulative_spend_usd` (the accumulator FIELD is unchanged; only the source fact moves); `apply_permit_decision` stops reading `spend_delta_usd`; `risk_delta` stays on the permit arm (`:728-729`). Same-commit obligation — a half-applied change (field retired from ontology, reducer still folding it off the permit fact) folds zero and under-counts, which DR-021 acceptance criteria #4/#5 catch. Companion edit **applied same-session** (DR-008/DR-010/DR-017 taxonomy-drift precedent): `rezidnt_types::taxonomy::SUBJECTS_V0` gains `"action.metered"` under a new `// action` group after `"run.intent.declared"` (count 41→42), doc-comment count `41`→`42`, keeping `taxonomy_drift.rs` green under `/vet`. `rezidnt-types` has no typed permit/metering payload struct (payloads are untyped `serde_json::Value`; `Subject` is a `String` newtype), so the ONLY `rezidnt-types` change is the const row — the field retire is doc-only. Scope discipline: subject + payload schema + field retires + reducer-arm SPEC only — the `action.metered` emitter, the fold-arm move, the DR-021 §8/§9 PDP-injection wiring, and their oracle tests are the C1 implementer's work (oracle-first), deliberately NOT written here. This session touched `spec/ontology.md` and `crates/rezidnt-types/src/taxonomy.rs`.
- 2026-07-20 · warden · DR-024 C6 running-risk cap `risk_delta?` placement (FIELD retire, no subject added/renamed): RETIRE `risk_delta?` from `permit.denied` (~378) and `permit.escalated` (~384); CONFIRM + document `risk_delta?` on `permit.granted` (~372) as the C6 fold source. Per owner-ratified DR-024 (ACCEPTED 2026-07-20) §Decision Q3 (GRANTED-only fold) + Q5 (produced only on `permit.granted` via the shared `risk_score` pure fn, contract-free seam through the existing `DecisionDeltas` carrier). This mirrors EXACTLY what DR-021 B2 did for `spend_delta_usd?`: a field folded/produced only on the granted arm is DEAD on the denied/escalated facts (a denied/escalated action never ran → folds ZERO → the I3 honesty property), so it is retired from those two payloads, leaving `risk_delta?` ONLY on `permit.granted`. DR-024 Q2 confirms `risk_delta?`'s `permit.granted` placement as CORRECT not incidental — risk is deterministically COMPUTED pre-action from the request axis, so it legitimately rides the permit fact (unlike the dishonest-if-denied spend *estimate* DR-021 moved to `action.metered`). `permit.granted` already carried `risk_delta?` (kept from DR-021), so NO add was needed — only the confirming annotation naming it the fold source + the produce/fold/seam semantics (shared scorer, GRANTED-only, `VerifierOutput`/`PermitOutcome` unchanged). **No `v`-bump on any subject** — `risk_delta?` is optional AND was never emitted by a live producer (the C6 path was inert until DR-024 wires it), so removing it from denied/escalated is not a breaking payload change (doc §5 additive-evolution: a `?` field's absence is always valid) and all three permit subjects stay `v = 1`. **Drift guard UNCHANGED — this is a payload-field edit, NOT a subject add/remove/rename and no `v` bumped**, so `rezidnt_types::taxonomy::SUBJECTS_V0` (42 subjects) and `crates/rezidnt-types/tests/taxonomy_drift.rs` (parses only `(subject, v)` table rows) stay green with **NO companion `SUBJECTS_V0` edit** — there is NO `rezidnt-types` change (payloads are untyped `serde_json::Value`; the retire is doc-only). Not a new BINDING item — DR-024 ratifies this placement; no invariant/DR amended beyond DR-024's grant, so no `/dr` routing. Two forward-reference supersession notes added to the DR-021-set narrative (~460, ~461) so the "risk_delta STAYS on all permit payloads" claims (accurate as of DR-021) point to this entry. **Ripple set handed to the C6 implementer/oracle (NOT fixed this session — ontology only):** the reducer at `crates/rezidnt-state/src/lib.rs:750-752` currently folds `risk_delta` UNCONDITIONALLY (all decisions) — DR-024 Q3 narrows it to the `"granted"` arm (implementer, same commit as the C6 build; the DR-024 fold-arm delta); and every test/fixture that emits `risk_delta` on a `permit.denied`/`permit.escalated` fact must drop it (they assert now-dead behavior): fixtures `spec/fixtures/permit_request_decision.jsonl` (denied `risk_delta:3.0` line 6, escalated `risk_delta:2.0` line 8 + `.expected.json`), `spec/fixtures/permit_escalate_not_coerced.jsonl` (escalated `risk_delta:5.0` + `.expected.json`), `spec/fixtures/s5b_board_permit.jsonl` (denied `risk_delta:3.0` line 6, escalated `risk_delta:2.0` line 8 + `.expected.json`); tests `crates/rezidnt-state/tests/permit_ledger.rs` (denied `risk_delta:3.0` ~line 117; the granted-fold assertions must expect GRANTED-only `risk_score` totals), `crates/rezidnt-state/tests/permit_cost_ms_recorded_only.rs` (emits `risk_delta:4.0` on a granted fact ~line 101 — the granted arm is fine; verify no denied/escalated risk asserted), `crates/rezidnt-state/tests/action_metered_fold.rs` (`risk_delta:2.0` on a permit fact ~line 152 + the "risk_delta STAYS/STILL folds" comment/assert ~151/165 — reword to GRANTED-only), `crates/rezidnt-gate/tests/permit_emit.rs` (`DecisionDeltas.risk_delta: Some(3.0)`/`Some(2.0)` emit-on-denied/escalated assertions ~lines 328/337/381/421 — drop or move to granted); and `spec/fixtures/README.md` line 38 ("folding its `risk_delta`" on the escalate fixture) needs its wording corrected to fold-ZERO. The DR-024 acceptance criteria (#6 GRANTED folds / DENIED+ESCALATED fold ZERO; #7 stamped delta == verdict score, seam contract-free) arrive with the C6 slice via `/oracle`. Scope discipline: `spec/ontology.md` ONLY — deliberately NO reducer/`.rs`/fixture edit (the fold-arm narrowing + fixture drops are the C6 implementer's oracle-first work). This session touched `spec/ontology.md` only.
- 2026-07-21 · warden · DR-029 C3 egress-mediation + credential-brokering subject mint (paired `/subject`, DR-029 §Decision 6 / criterion 5): TWO new nouns `egress` + `credential`, FIVE new subjects — **`egress.mediated` v1** `{run, network:"mediated", sandbox:"available", egress_enforceable:bool, backend?, agent?}` (the enforcing composed posture; replaces the `sandbox.mediated` placeholder), **`egress.unavailable` v1** `{run, network:"sealed"|"none", sandbox:"available"|"unavailable", egress_enforceable:false, injected:false, reason, backend?, agent?}` (the CLOSED/unsandboxed loud degrade — ONE subject for BOTH `ComposedDegrade::ConfinedClosed` AND `Unsandboxed`, disambiguated by the `sandbox` field; replaces BOTH the `egress.unavailable` and `sandbox.unavailable` placeholders), **`egress.denied` v1** `{run, dest, policy_ref:CasRef, reason?}` (per-connection off-allowlist denial; `Mediation::Deny` / `EgressScope` fail verdict, DR-026 crit 3), **`credential.injected` v1** `{run, dest, secret_ref, policy_ref:CasRef}` (brokered secret injected upstream; replaces the placeholder — carries ONLY `secret_ref`, NEVER the value, redacted at the `BrokeredSecret` type level, `injected_fact` lifts `secret_ref()` never `expose()`, DR-026 crit 4/5), **`credential.dropped` v1** `{run, dest, secret_ref, reason}` (DR-029 §Decision 2 unresolvable-`secret_ref` drop — loud, honest, never a fake/empty secret). Per owner-ratified DR-029 (ACCEPTED 2026-07-21) §Decision 6, which names the family and hands the exact names/grammar/schemas to the warden ("the warden designs the taxonomy, not this DR"); design basis DR-026 (credential-brokering posture, redacted `BrokeredSecret`, degrade CLOSED), DR-028 (composition + the three composed degrade facts). **Taxonomy judgment:** `sandbox.*` folded into `egress.*` as a FIELD (`sandbox: available|unavailable`), NOT a parallel noun — `compose_degrade` decides ONE composed state, so it rides ONE fact (the `pep?`/`role?` posture-as-field precedent); parallel `sandbox.*`/`egress.*` siblings would fracture one decision across two subjects. `credential.*` IS a distinct noun (its entity is a `secret_ref`, its leak-discipline its own invariant). Full new-subject checklist passed for all five: *necessity* (genuinely new C3 facts; `egress.denied` per-connection + the two credential facts distinct events in time; not synonyms of any existing subject), *grammar* (all parse strictly past-tense `noun.verb`, NOT grandfathered; `egress`/`credential` new nouns per the DR-006/DR-008/DR-010/DR-021 distinct-axis-→-distinct-noun precedent), *family coherence* (both nouns fold onto the SAME `run`-keyed `AgentRunState` — axes not entities), *emitter* (named in the placeholder-replacement map: `compose.rs::degrade_fact`, `egress.rs::injected_fact` + the `Mediation::Deny`/DR-029-§2-drop emitters — the swap is C3-implementer scope, NOT this session), *reducer consumer* (DR-006 OBLIGATION recorded, NOT built — new `#[serde(default)]` `AgentRunState` fields `egress: Option<EgressPostureState>` / `Vec<EgressDenial>` / `Vec<CredentialInjection>` / `Vec<CredentialDrop>`, keyless fact = no-op, rebuild-stable, the exact `intent`/`pep`/`role`/`delegations` pattern — flagged for the implementer, none exist yet), *I2* (all payloads short scalars + at most one `CasRef`, far under 32 KiB; the credential facts CANNOT carry the value by construction), *authority* (DR-029 §Decision 6, ACCEPTED — no BINDING amended beyond the grant, no `/dr` pending). Companion edit **applied same-session** (DR-008/DR-010/DR-017/DR-021 taxonomy-drift precedent): `rezidnt_types::taxonomy::SUBJECTS_V0` gains the three `egress.*` (new `// egress` group after `"action.metered"`) + two `credential.*` (new `// credential` group) in ontology-table order, doc-comment count `42`→`47`, keeping `taxonomy_drift.rs` green under `/vet`. `rezidnt-types` has no typed payload structs (payloads are untyped `serde_json::Value`; `Subject` is a `String` newtype), so the ONLY `rezidnt-types` change is the five const rows. Additive; every subject minted at `v = 1`. Scope discipline: subjects + payload schemas + placeholder-replacement map + reducer OBLIGATION only — the emitter swaps in `compose.rs`/`egress.rs`/`runs.rs`, the two new emitters, the reducer arms + `AgentRunState` fields, and their oracle tests/fixtures are the C3 implementer's `c3-egress-fold` work (oracle-first), deliberately NOT written here. This session touched `spec/ontology.md` and `crates/rezidnt-types/src/taxonomy.rs`.
