# rezidnt — Architecture Plan

**Version:** 0.1 (pre-code) · **Owner:** TwofoldTech LLC · **License posture:** Apache-2.0 (see §17)
**Status flags used throughout:** **BINDING** (change requires a written decision record), **DEFAULT** (best current call; cheap to revisit), **PROVISIONAL** (expected to change; do not build against).

> Standing gate, restated once: the name ships only after registry checks pass (`rezident` is the fallback string). Nothing in this document overrides it. (A second gate — repository privacy pending an employer IP memo — was retired by DR-003.)

---

## 1. Thesis and scope

> **Amended by [DR-008](decisions/DR-008-permit-engine-pivot.md).** The compose-only framing is dropped: rezidnt takes the pre-hoc "may" axis natively via a permit engine; Omnigent becomes a baseline/adapter, not a required companion.

rezidnt is a local-first **resident daemon** that runs, verifies, and audits a fleet of coding agents across workspaces. It is one Rust binary that owns three things no current tool unifies: a **typed event fabric** (every fact about the fleet, append-only, replayable), a **supervisor** (substrates and agents run under restart-with-backoff discipline, like a session-scoped init), and a **verifier gate engine** (deterministic checks that produce evidence, not vibes). Everything else — terminals, harnesses, UIs — attaches to those three through narrow seams.

Positioning, compressed from the strategy record: Omnigent governs what agents *may* do (pre-hoc permissions); rezidnt proves what agents *did* (post-hoc evidence). herdr owns the terminal substrate today; rezidnt integrates it at arm's length and replaces it in Phase 3 with a permissively-licensed kernel it assembles itself. The wedge buyers are (a) the single operator running a fleet on their own hardware and (b) Microsoft-stack enterprises that need an audit trail a compliance reviewer will sign.

**The golden path is the product contract (BINDING):** cold machine → `curl` install → `rezidnt open <repo>` → worktrees allocated, agents spawned under gates, fleet state visible, first verified diff merged — one take, zero config edits, single-digit minutes. Every phase exit is judged against this demo, not a feature list.

> **Amended by [DR-036](decisions/DR-036-operator-onboarding.md)** (the `zero config edits` clause — `spec init`/`doctor`/`init`) and **[DR-037](decisions/DR-037-distribution-installer.md)** (the `curl` install step — a real Linux/WSL-first installer + release machinery so this recorded one-take demo runs from `curl`, not `cargo install`).

**Twelve-month non-goals (BINDING):** writing a VT parser from scratch (Phase 3 *assembles* permissive components), real-time multi-device session sync, mobile clients, hosted/cloud control plane, plugin marketplace.

## 2. Design invariants (BINDING)

**I1 — Zero pixels in core.** `rezidentd` renders nothing. Every UI (CLI, TUI, Tauri, web) is a client of the socket/MCP surface. A renderer decision must never force a daemon change.

**I2 — Control plane and data plane never mix.** The event fabric carries facts and references. PTY bytes, transcripts, diffs, and artifacts move out-of-band (substrate-direct or via the CAS, §10). Payload hard cap 32 KiB (DEFAULT); larger content becomes a CAS ref. Rationale: terminal output is tens of MB/s aggregate under agent load; the bus is designed for ≤ ~10³ events/min. Mixing them is the classic event-sourcing death.

**I3 — The log is the truth; state is derived.** Materialized state can be deleted and rebuilt from the log at any time (`rezidnt rebuild`). Reducers are pure. Any feature that cannot be reconstructed from events + CAS is misdesigned.

**I4 — Substrates behind traits.** Terminal, agent-harness, repo, and window-arrangement capabilities are Rust traits. herdr, git, and Omnigent are *implementations*, individually swappable. This is the hedge against both dependencies turning adversarial (herdr's maintainer is heading toward "agent runtime"; Databricks ships on their schedule).

**I5 — MCP-first, UI-second.** Every capability ships as an MCP tool/resource before it gets a keybinding. Agents are first-class operators of rezidnt itself.

**I6 — Verifiers are deterministic and interrogable.** Same inputs (pinned by content hash) → same verdict, replayable from the log. "Why was I blocked" returns the failing verifier and its evidence, machine-readably. A gate that can't explain itself doesn't ship.

**I7 — One static binary, no runtime dependencies, no telemetry.** Install is `curl | sh` or `cargo install`. The daemon phones home to no one; this is a product stance, not a config default.

> **I8 was rewritten by [DR-001](decisions/DR-001-native-substrates.md#invariant-i8-rewritten-binding).** The AGPL-firewall wording below is superseded v0.1 text; the current invariant is the clean-room rule in the DR.

**I8 — AGPL firewall.** herdr is consumed strictly as a separate process over its socket/CLI. No linking, no vendoring, no source consultation for implementation. CI enforces a denylist (no `herdr` source in-tree). Omnigent (Apache-2.0) may be *ported* with NOTICE preservation; herdr may not.

## 3. System topology

> **Amended by [DR-001](decisions/DR-001-native-substrates.md).** The native run substrate replaces the herdr-integrated topology described below.

```
┌────────────────────────── clients ──────────────────────────┐
│  rezidnt CLI    TUI board (S5)    Claude Code / MCP agents  │
│  Tauri dashboard (later)          any MCP-speaking tool     │
└───────────────┬──────────────────────────┬──────────────────┘
                │ UDS / named pipe (JSONL)  │ MCP (stdio + localhost HTTP)
┌───────────────▼──────────────────────────▼──────────────────┐
│                     rezidentd (daemon)                       │
│  fabric (broadcast bus) ── event log (SQLite, append-only)   │
│  reducers → materialized workspace graph (watch channels)    │
│  gate engine (native + exec verifiers, evidence → CAS)       │
│  supervisor (adapter tasks, backoff, health events)          │
└───┬──────────────┬───────────────┬───────────────┬───────────┘
    │ herdr socket │ git CLI + gix │ Omnigent WS   │ komorebi/Win32
┌───▼───┐      ┌───▼───┐       ┌───▼────┐      ┌───▼─────┐
│ herdr │      │ repos │       │Omnigent│      │arranger │
│(AGPL, │      │ + FS  │       │(Phase 2│      │(optional│
│ IPC   │      │watcher│       │ donor) │      │ feature)│
│ only) │      └───────┘       └────────┘      └─────────┘
```

Process model: one `rezidentd` per user session, **started by the operator**. Trust boundary is the local user account (§12).

> **Corrected 2026-07-25 — this clause described a mechanism the code does not have.** Original text: ~~"auto-started by the CLI on first use (`rezidnt` connects, spawns the daemon if absent, waits for the hello)"~~. `rezidnt` is a socket client: it connects and waits for the hello, and a connection failure exits 4 ([DR-004](decisions/DR-004-exit-code-table.md)) rather than spawning anything. Verified twice: the only `Command::new` sites in `bins/rezidnt/src/` are the DR-041 verifier toolchains, and `crates/rezidnt-client` has no spawn path at all. `install.sh`, `docs/quickstart.md` and `README.md` all correctly told operators to start the daemon themselves; this document was the outlier, and under the constitution's "when they disagree, the doc wins" it was the authoritative source of the false claim. **Whether auto-spawn remains a design goal or is dropped is UNDECIDED and needs a `/dr`** — this correction records what is true today and rules nothing about intent. Multi-machine is out of scope until Phase 3+; the seam is that clients already speak a socket protocol, so a remote bridge is additive.

## 4. Crate workspace layout

> **Amended by [DR-001](decisions/DR-001-native-substrates.md).** `rezidnt-run` replaces `rezidnt-adapters/herdr`; the current crate set is the [repository map](../README.md#repository-map).

Cargo workspace, one repository. Library crates dual-licensed `MIT OR Apache-2.0` (Rust convention, maximizes reuse); binaries Apache-2.0.

```
rezidnt/
  crates/
    rezidnt-types      # event envelope, subjects, entity models, schemars derives — published first (0.0.1)
    rezidnt-fabric     # bus, log writer/reader, replay, hash chain
    rezidnt-state      # reducers, workspace graph, watch channels, snapshots
    rezidnt-supervise  # adapter runtime: spawn, backoff, health, circuit breaker
    rezidnt-gate       # gate engine, native verifier trait, exec-verifier contract, evidence
    rezidnt-adapters/
      herdr/           # socket client + CLI fallback (IPC only — see I8)
      git/             # gix reads, git-CLI mutations, notify watcher, worktree registry
      omnigent/        # Phase 2; WebSocket client + ported harness adapters (NOTICE)
      arranger/        # feature-flagged; komorebi IPC / Win32
    rezidnt-mcp        # MCP server (resources + tools)
    rezidnt-proto      # socket protocol frames, versioned hello
    rezidnt-cas        # content-addressed store (blake3)
  bins/
    rezidentd          # the daemon
    rezidnt            # the CLI — socket client; does NOT spawn the daemon (§3)
  spec/
    ontology.md        # subject taxonomy — the IP; versioned like code
    schemas/           # generated JSON Schema (schemars) → published to @rezidnt npm scope
  bench/
    harness/           # public benchmark harness (held-out cases live elsewhere — §15)
```

## 5. Event fabric

**Envelope (BINDING in shape; additive evolution only):**

```rust
pub struct Event {
    pub id: Ulid,                  // time-ordered, globally unique
    pub ts: OffsetDateTime,        // daemon clock, UTC
    pub v: u16,                    // payload schema version for this subject
    pub source: SourceId,          // adapter/component that emitted
    pub workspace: Option<WorkspaceId>,
    pub subject: Subject,          // e.g. agent.status.changed (Appendix B)
    pub correlation: Ulid,         // groups a causal chain (one `open`, one gate run)
    pub causation: Option<Ulid>,   // the event that directly triggered this one
    pub payload: serde_json::Value // ≤ 32 KiB; larger content → CasRef
}
```

Serialization is JSON Lines on the wire and JSON in the log column (DEFAULT). Control-plane volume makes binary encodings premature optimization; `postcard`/CBOR is a drop-in later because `rezidnt-types` owns serde derives.

**Subjects** are dot-namespaced, never renamed, only deprecated (BINDING). Payloads evolve additively; a breaking change mints `v+1` and reducers must handle all live versions. The taxonomy v0 is Appendix B and lives in `spec/ontology.md` — treat that file as the crown jewel; it is the artifact that outlives every implementation choice here.

**Delivery semantics:** append to the log is the commit point (exactly-once by ULID uniqueness); in-process fan-out is `tokio::sync::broadcast` (at-least-once to live subscribers). A lagged subscriber that overflows its buffer receives `Lagged(n)` and **must resync from the log by last-seen ULID** rather than pretend continuity (BINDING rule for all clients and adapters). This single rule is what keeps slow UIs from ever back-pressuring the daemon.

## 6. Event log and materialized state

**Storage decision (DEFAULT, with reasoning): SQLite via `rusqlite`, WAL mode.** Rejected: `redb` (no ad-hoc SQL — `debrief`, the benchmark, and future compliance exports are all query workloads), Postgres (violates I7), flat JSONL (no indexed reads for replay-from-ULID). SQLite gives single-file backup, `PRAGMA integrity_check` for `doctor`, and a query surface the audit story is literally sold on. Revisit only if write amplification appears at >10⁴ events/min, which is two orders of magnitude above design load.

```sql
CREATE TABLE events (
  seq        INTEGER PRIMARY KEY,          -- monotonic append order
  id         TEXT NOT NULL UNIQUE,         -- ULID
  ts         TEXT NOT NULL,
  v          INTEGER NOT NULL,
  source     TEXT NOT NULL,
  workspace  TEXT,
  subject    TEXT NOT NULL,
  correlation TEXT NOT NULL,
  causation  TEXT,
  payload    TEXT NOT NULL,                -- JSON
  chain      BLOB NOT NULL                 -- blake3(prev.chain || id || payload) — §12
);
CREATE INDEX idx_events_subject ON events(subject, seq);
CREATE INDEX idx_events_ws       ON events(workspace, seq);
CREATE INDEX idx_events_corr     ON events(correlation);
```

Retention: the log is forever by default; compaction/archival is PROVISIONAL and gated on real disk pressure, because the log *is* the compliance artifact and the eval corpus.

**Materialized state** is a CQRS-lite fold. Reducers are pure functions `fn apply(&mut Graph, &Event)` living in `rezidnt-state`; the graph is the entity model below; every entity class exposes a `tokio::sync::watch` channel so clients subscribe to *state*, not raw events, unless they ask for the firehose.

```
Project ─┬─ Workspace (jurisdiction; config, layout intent)
         │    ├─ Worktree (canonical path, branch, allocator, claim state)
         │    ├─ Session (terminal substrate handle: herdr ws/tab/pane ids)
         │    └─ AgentRun (harness, model, status, badge, gate history)
         │         └─ Dossier (derived per-agent view: runs, verdicts, evidence refs, cost)
         └─ GateDef / VerifierDef (named policy points and their verifier lists)
Artifact (CAS ref + mime + provenance)   ·   AdapterHealth (per substrate)
```

Snapshots: periodic (every 5,000 events or 15 min, DEFAULT) into a `snapshots` table keyed by last `seq`; startup = load snapshot, fold the tail. `rezidnt rebuild` drops snapshots and refolds from `seq 0`; a rebuild that diverges from the running graph is a reducer bug and a release blocker (property-tested, §15).

## 7. Substrate adapter layer

> **Amended by [DR-001](decisions/DR-001-native-substrates.md) and [DR-007](decisions/DR-007-release-worktree.md).** `TerminalSubstrate` is deferred to Phase 3, `AgentSubstrate` absorbs spawn-with-PTY, and `RepoSubstrate` is the three-method form in DR-007 — not the sketch below. The herdr and Omnigent adapters below are superseded by the native substrates.

**Runtime model:** each adapter is a supervised tokio task owning its connection, receiving commands over `mpsc`, emitting events into the fabric. Supervision policy (DEFAULT): exponential backoff with jitter, base 500 ms, cap 60 s; a crash-loop breaker trips after 5 restarts/5 min and parks the adapter in `Faulted` — visible as `adapter.health.changed`, never a silent retry storm. Health states: `Starting → Healthy → Degraded → Faulted`, all on the bus, because an invisible supervisor is a failed supervisor.

**Trait seams (shape BINDING, signatures DEFAULT):**

```rust
#[async_trait]
pub trait TerminalSubstrate: Send + Sync {
    async fn ensure_workspace(&self, spec: &WorkspaceSpec) -> Result<SessionMap>;
    async fn spawn_pane(&self, ws: &WorkspaceId, cmd: Command) -> Result<PaneId>;
    async fn send_keys(&self, pane: &PaneId, input: &[u8]) -> Result<()>;
    async fn subscribe_lifecycle(&self) -> Result<EventStreamHandle>; // facts only; bytes stay out-of-band (I2)
}

#[async_trait]
pub trait AgentSubstrate: Send + Sync {
    async fn spawn(&self, spec: &AgentSpec, badge: Badge) -> Result<AgentRunId>;
    async fn signal(&self, run: &AgentRunId, sig: AgentSignal) -> Result<()>;
    async fn status(&self, run: &AgentRunId) -> Result<AgentStatus>;
}

#[async_trait]
pub trait RepoSubstrate: Send + Sync {
    async fn alloc_worktree(&self, req: WorktreeReq) -> Result<Worktree>;
    async fn diff_summary(&self, wt: &WorktreeId) -> Result<CasRef>;
}
```

**herdr adapter (Phase 1).** Speaks herdr's JSON socket (Unix socket; named pipe on Windows) for workspace/tab/pane creation, spawn, and state subscription; falls back to the CLI via `HERDR_BIN_PATH` for verbs the socket lacks. Version-gated hello: the adapter records herdr's version and refuses semver-major jumps until its contract tests (recorded socket transcripts, §15) pass against the new version. Confidence that the socket covers Slice 1's needs: moderate-high from documented API surface; the contract-test harness exists precisely because herdr ships weekly.

**git adapter (Phase 1).** Reads via `gix` (status, branch, diff summaries — high confidence in crate maturity for read paths); mutations via the `git` CLI (`worktree add/remove`, fetch, merge) because the CLI is the compatibility truth; filesystem events via `notify`, debounced 250 ms (DEFAULT). Owns the **worktree registry**, which resolves the two-allocators problem: herdr mints worktrees, Omnigent's delegation mints worktrees, and rezidnt itself allocates on `open`. Rule (BINDING): every worktree is registered under its canonicalized path with an `allocator` field; a second claim on the same path emits `worktree.conflict` rather than silently double-tracking. rezidnt is *allocator* for its own spawns and *observer/normalizer* for substrate-minted trees.

**Omnigent adapter (Phase 2).** Two distinct values, kept separate: (a) a live adapter to an Omnigent server over its WebSocket, pinned to an exact alpha version — integration value is real but churn risk is high, so it ships feature-flagged and never on the golden path; (b) **donor value**: Omnigent's Apache-2.0 harness adapters (Claude Code, Codex, Cursor spawn/telemetry logic) are ported into `rezidnt-adapters/omnigent` as the *reference implementation* for our native `AgentSubstrate` impls, with NOTICE preservation. The port pattern is the Bun pattern: reference implementation + our contract tests as the oracle. Their Python never runs in our process.

**Arranger adapter (optional, feature-flagged).** Windows window placement delegates to komorebi over its IPC/CLI (high confidence it exists; moderate on current IPC details — verify at implementation). Never hand-roll `SetWindowPos` choreography. Absent komorebi, the adapter is a no-op that still emits layout-intent events for future clients.

## 8. Gate and verifier engine

> **Amended by [DR-006](decisions/DR-006-replay-divergence-signal.md).** Replay divergence now lands a durable `integrity.alarm` fact on the log, not a CLI-only report.
>
> **Amended by [DR-008](decisions/DR-008-permit-engine-pivot.md).** A fourth lifecycle point `permit` joins vet/pre_merge/post_run; permit-verifiers make the gate engine the policy engine (pass→allow, fail→deny, inconclusive→escalate).

This is the differentiation layer; over-invest here, under-invest everywhere else.

**Model.** A **Gate** is a named policy point bound to a lifecycle transition: `vet` (pre-spawn: is this agent spec, badge scope, and workspace state acceptable?), `pre_merge` (is this diff verified?), `post_run` (`debrief`: what did the agent actually do, and does the evidence support the claim?). A **Verifier** is a deterministic check attached to a gate. Verdicts are `pass | fail | inconclusive` — never a bare boolean, because `inconclusive` routed to a human is honest and `fail` faked as `pass` is the product's death.

**Two verifier kinds (BINDING).** *Native* verifiers implement a Rust trait (built-ins: diff-scope check, test-suite runner, forbidden-path touch, secret-leak scan, build-passes). *Exec* verifiers are any argv program speaking a JSON contract — this is the polyglot seam and the ecosystem play; a Roslyn analyzer pack, a Bun script, and a Python linter all plug in identically:

```json
// stdin
{ "gate": "pre_merge", "workspace": "…", "refs": { "diff": "cas:blake3:…", "transcript": "cas:blake3:…" },
  "params": { … }, "timeout_ms": 120000 }
// stdout
{ "verdict": "fail", "evidence": [ { "kind": "finding", "msg": "test regression: auth::login", "ref": "cas:…" } ],
  "cost_ms": 8412 }
```

**Determinism requirements (BINDING):** inputs are pinned by content hash (verifiers receive CAS refs, not mutable paths); no network by default (exec verifiers run with network disabled unless the gate def opts in, recorded in the event); wall-clock timeout 120 s DEFAULT; nonzero exit or malformed output = `inconclusive`, never `pass`. Evidence blobs go to the CAS; `gate.passed|failed` events carry refs.

**Interrogability (I6, the AX feature no one ships):** `rezidnt gate why <run>` and the MCP tool `gate_explain` return the failing verifier, its evidence refs, and the exact inputs — so a blocked agent can read *why* it was blocked and fix the actual defect instead of thrashing against a refusal string. **Replay:** `rezidnt debrief <session>` re-executes recorded verdicts from log + CAS; divergence between the recorded and replayed verdict raises an integrity alarm (either the verifier was nondeterministic — a verifier bug — or the log was altered — see §12). This replay property is what makes the audit trail *evidence* rather than assertion, and it is the sentence that gets a compliance reviewer to sign.

**Composition with Omnigent, stated once for positioning:** their policies gate *permissions* before the act; rezidnt gates *evidence* after it. The models compose; only one of them survives an auditor.

Verifier packs are separate crates/repos. The generic packs are open; domain judgment packs (DXP/Microsoft-stack failure modes) are the commercial seam (§17) and stay out of this repository entirely.

## 9. Command surfaces

> **The permit-config resolution seam (`permit_config_for`) is amended by [DR-011](decisions/DR-011-permit-pdp-config-seam.md) (the seam itself) and [DR-020](decisions/DR-020-sp4c-wire-layered-permit-sourcing.md) (three-source admin/dev/session layering, admin sourced outside the workspace spec).**
>
> **Spend (C1) — amended by [DR-021](decisions/DR-021-live-spend-cap-c1.md):** the `SpendCap` verifier becomes live (caps injected via this seam), and spend attribution moves OFF the pre-action permit decision fact onto a post-action metering fact (B2, I3-honest) — so `spend_delta_usd` retires from the `permit.*` reducer fold source. Implies a downstream warden `/subject` (`action.metered`).
>
> **MCP tool register — amended by [DR-039](decisions/DR-039-board-view-mcp-resource.md):** adds `board_view`, a READ-ONLY tool (unbadged, `tail_events` class) returning the `BoardView` fleet projection; its `project()` + view types are hoisted from `rezidnt-tui` into `rezidnt-state` so `rezidnt-mcp` reuses one projection without depending on the board (preserving DR-031's read-only proof). Schema generated no-drift like every other tool.

**MCP (primary, per I5).** Server via the official Rust SDK (`rmcp` — moderate confidence on crate name and API maturity; verify at Slice 3 and be prepared to write a thin JSON-RPC layer if it disappoints). Transports: stdio (spawned by local clients like Claude Code) and streamable HTTP on `127.0.0.1` (DEFAULT port 0/announced via lockfile, not a fixed port). Resources: workspace graph nodes, dossiers, event ranges by ULID, gate definitions. Tools: `open_project`, `spawn_agent`, `vet`, `debrief`, `gate_explain`, `tail_events`, `alloc_worktree`, `arrange_layout` (PROVISIONAL). Every tool is idempotent or carries an idempotency key; every tool's JSON Schema is generated from `rezidnt-types` via `schemars`, so the MCP surface and the npm-published types can never drift.

**Socket protocol.** UDS at `$XDG_RUNTIME_DIR/rezidnt.sock` (fallback `~/.local/state/rezidnt/`); Windows named pipe `\\.\pipe\rezidnt`. JSONL frames; first frame is a versioned hello `{proto: 1, schema: <ontology hash>, daemon: <semver>}`; mismatched proto majors disconnect with a machine-readable upgrade hint. The CLI, TUI, and future Tauri client are all just consumers of this protocol — no privileged in-process clients (I1).

**CLI.** `rezidnt open <repo|spec>` (materialize), `status`, `tail [--subject …]`, `vet <agent-spec>`, `debrief <session|run>`, `gate why <run>`, `rebuild`, `doctor`, `spec init`, `init` (the onboarding entry: `doctor` → `spec init` → `open`; `doctor`/`spec init`/`init` all implemented by [DR-036](decisions/DR-036-operator-onboarding.md)). Global `--json` on every verb; stable exit codes (BINDING, ratified by DR-004): **0** ok · **1** unexpected internal error · **2** local input/usage error (clap convention; daemon never reached) · **3** substrate fault, including daemon-side refusals · **4** daemon unreachable · **5** gate-fail (`vet`/`debrief`/`pre_merge` verdict `fail`; `inconclusive` is NOT 5 — it is 3, never coerced toward pass or fail, per I6). Lore vocabulary stops at `vet`, `debrief`, and `dossier` (BINDING) — everything else stays boring on purpose.

## 10. Data plane

PTY bytes never touch the fabric (I2). In Phase 1, interactive output stays entirely inside herdr (its clients render it); rezidnt consumes only lifecycle facts and, on gate events, captured artifacts. Artifacts — diffs, transcripts, verifier evidence, build logs — land in a content-addressed store at `~/.local/share/rezidnt/cas/<blake3-hex>` (blake3 DEFAULT: fast, incremental-friendly), written once, referenced by `CasRef { hash, bytes, mime }` in events. GC is reachability-from-log and PROVISIONAL — with the log retained forever, unreachable blobs are rare by construction. Phase 3's owned terminal substrate will route PTY streams substrate→client over dedicated pipes/shared memory with the daemon carrying only chunk manifests; that design lands with Phase 3 and is deliberately unspecified here.

## 11. Cross-platform topology

Linux and macOS are native and boring. Windows is the wedge and gets explicit treatment:

**Phase 1 topology (DEFAULT):** the daemon and all substrates (herdr, git worktrees, agents) run inside WSL2; Windows-side clients (CLI.exe, Claude Code on Windows) reach the daemon over loopback TCP, because AF_UNIX sockets do not bridge the Windows/WSL2 boundary (moderate-high confidence) and herdr's native Windows builds are preview-beta. Path canonicalization is a first-class concern: every workspace records its dual mapping (`/mnt/c/...` ↔ `C:\...`) at registration, and all events carry the canonical (WSL) form with translation at the client edge.

**Phase 3 differentiation:** native-Windows daemon with ConPTY as a first-class citizen via `portable-pty`, named-pipe transport, komorebi arrangement, Entra-conscious enterprise packaging. This is the "nobody owns Windows" wedge from the strategy record; it is *sequenced after* the fabric proves itself in WSL2, not before.

## 12. Security and audit posture

> **Amended by [DR-005](decisions/DR-005-badge-consolidation.md).** The badge requirement is narrowed to *state-mutating* calls; interrogation (`gate_explain`) and `tail_events` are read-class and unbadged.

Trust boundary: one user on one machine. Socket at mode 0600; named pipe ACL'd to the current user. **Badges** (the one lore term in the security model): per-`AgentRun` capability tokens — 256-bit random, scoped to `{workspace, verb set, expiry}` — required on every mutating socket/MCP call. DEFAULT is opaque bearer checked by the daemon; macaroon-style attenuation is PROVISIONAL and waits for a real delegation use case. The point in Phase 1 is not cryptographic ceremony; it is that *an agent's writes are attributable to that agent* in the log, which is what makes the dossier meaningful.

Secret hygiene: fabric ingress runs a redaction pass (denylist patterns: cloud keys, PATs, connection strings) before append; exec verifiers run with a scrubbed environment. Log integrity: the `chain` column (blake3 over previous chain + id + payload) makes the log tamper-evident at near-zero cost; `doctor` re-walks it. DEFAULT on. No telemetry (I7). Threats explicitly out of scope for now, documented so nobody pretends otherwise: hostile local root, malicious verifier binaries installed by the operator themselves, and multi-tenant isolation.

## 13. Configuration and the project spec

Global config `~/.config/rezidnt/rezidnt.toml` (Windows: `%APPDATA%\rezidnt\`): substrate paths, default gates, arranger toggle. Per-project spec — the file `rezidnt open` materializes from — borrows herdr-plus's proven TOML semantics (independently reimplemented; I8 does not apply to a config shape, but keep it clean anyway):

```toml
[project]
name = "acme-checkout"
repo = "."

[[workspace.tab]]
name = "build"
panes = [{ cmd = "just watch" }, { cmd = "just test --watch" }]

[[agent]]
name = "impl"
harness = "claude-code"          # AgentSubstrate impl
worktree = "auto"                 # allocator: rezidnt
gates = ["vet", "pre_merge"]

[gates.pre_merge]
verifiers = [
  { native = "tests-pass" },
  { exec = "verifiers/scope-check", params = { allow = ["src/checkout/**"] } },
]
```

`rezidnt spec init` generates this interactively; the golden path must work with the generated file untouched. Implemented by [DR-036](decisions/DR-036-operator-onboarding.md), which also adds `rezidnt init` (the `doctor` → `spec init` → `open` wrapper) and `docs/quickstart.md` (the narrated one-take demo).

## 14. Observability

> **Amended by [DR-006](decisions/DR-006-replay-divergence-signal.md).** Integrity divergence at replay is mirrored onto the fabric as a durable fact.

`tracing` + `tracing-subscriber` throughout; WARN and above are mirrored onto the fabric as `daemon.*` events so the system's own misbehavior is queryable with the same tools as everything else. `rezidnt doctor` checks: socket reachability, SQLite `integrity_check` + WAL state, chain verification, adapter health, substrate versions against tested ranges, CAS disk headroom. A `--smoke` self-benchmark (spawn a null agent through a null gate, assert end-to-end latency) is PROVISIONAL.

## 15. Testing, oracles, and the benchmark seam

> **Amended by [DR-022](decisions/DR-022-benchmark-harness-slice.md).** Slices the benchmark into an in-repo `bench/harness` collating three of the four locked metrics (task-completion, merge success, cost-per-verified-diff — all log-derived); gate precision/recall is structurally fenced behind the permanently-external held-out set (§17), exposed only as a seam that returns `inconclusive` when no labeled set is supplied.

Ordered by the oracle principle that sequences the whole project — build where a deterministic judge exists:

**Reducer determinism (Phase 1's oracle):** property tests assert `fold(log) == snapshot` under arbitrary event interleavings and that rebuild equals live state; golden log fixtures live in-repo and every release replays them. **Adapter contracts:** herdr and Omnigent adapters are tested against *recorded* socket/WebSocket transcripts (record/replay harness in `rezidnt-supervise` test utils); a substrate version bump that breaks the recording blocks the adapter, not the daemon. **Verifier conformance:** a suite that feeds exec verifiers malformed input, timeouts, and nondeterminism traps, asserting `inconclusive`-not-`pass` behavior. **Phase 3 oracles:** vttest/esctest conformance plus grid-snapshot comparison against a reference emulator, per the phase plan. **The benchmark (GTM-grade testing):** the harness is public in `bench/harness`; the held-out case set is private, full stop — you know exactly what contamination does to a measuring stick. Metrics locked now so the category argument is ours: orchestrated task completion rate, gate precision/recall against labeled defects, worktree merge success rate, cost per merged verified diff.

## 16. Phased roadmap and slice acceptance criteria

> **Superseded by [DR-001](decisions/DR-001-native-substrates.md#roadmap-supersedes-16).** The current roadmap lives in the DR; the slices below are the pre-DR-001 (herdr-integrated) plan, kept for provenance.
>
> **Amended by [DR-008](decisions/DR-008-permit-engine-pivot.md).** Adds a permit-engine phase (SP0–SP5) between gates (Phase 2) and terminal fidelity (Phase 3).
>
> **Amended by [DR-033](decisions/DR-033-operator-client-resolve-escalation.md).** The permit PDP consults the folded ledger for a prior human `permit.resolved` before dispatching verifiers, applying that decision instead of re-escalating (operator resolve-escalation, slice 2).
>
> **Amended by [DR-034](decisions/DR-034-operator-client-live-unblock.md).** Adds a bounded server-assisted long-poll (`REZIDNT_UNBLOCK_TIMEOUT_MS`) so a live-held PEP resumes the currently-stalled agent when a matching `permit.resolved` lands, degrading fail-closed to `ask` on expiry (operator live-unblock). Pulls forward and amends DR-033 §Decision 1's "no live-unblock" clause; layers on top of the ledger-check, which stays the correctness path.
>
> **Amended by [DR-035](decisions/DR-035-escalation-resolution-scope-and-lifetime.md).** `resolution_for` gains a log-derived expiry filter (a resolution's envelope ULID + TTL vs the incoming request's envelope ULID — a pure fold, I3-clean) and an opt-in single-axis wildcard match; a broad resolution MUST carry a TTL (broad-and-permanent is un-representable). Pulls forward and amends DR-033 §Decision 2 (no-TTL) and §Decision 3 (request-scoped). Sequenced `escalation-ttl` then `escalation-grant-all`.
>
> **Amended by [DR-009](decisions/DR-009-match-omnigent-scope.md).** Folds four memo-001 capabilities into the permit phase — spend/rate limits (C1→SP1), intent-lock (C7→new SP-intent), layered admin/dev/session precedence (C8→SP4) — and adds a distinct later sole-chokepoint enforcement phase (C3), fenced behind its own design + DR.
>
> **Amended by [DR-022](decisions/DR-022-benchmark-harness-slice.md).** Gives the Phase-2 exit ("the benchmark harness runs end-to-end against rezidnt itself") its first defined acceptance criteria + exit demo — a headless in-repo dogfood harness over the completed S4 golden path.
>
> **Amended by [DR-025](decisions/DR-025-c3a-linux-sandbox.md).** Slices the DR-009 sole-chokepoint (C3) phase: **C3a** — a Linux `bwrap`-backed `SandboxSubstrate` (I4) wrapping the S1 spawn seam, confinement from folded policy, loud `sandbox.unavailable` degrade when absent — ships first with its own acceptance criteria + exit demo. C3b (egress proxy), C3c (credential brokering), and the macOS/Windows backends stay fenced, each behind its own DR; the Windows tier is coupled to the deferred native-Windows Platform phase.
>
> **Amended by [DR-026](decisions/DR-026-c3bc-egress-credential-brokering.md).** Folds **C3b+c** into one slice — the full L7 egress chokepoint: a TLS-terminating `EgressProxy` (I4) that is the sole, inescapable route out of C3a's sealed netns (`pasta` connector, transparent TCP+DNS interception), a process-lifetime rezidnt CA (`rustls`/`rcgen`), and credential brokering (secrets injected upstream, never held by the agent, logged by-ref-never-value). Degrades CLOSED. First C3 slice to add linked deps (App. A). macOS/Windows egress backends stay fenced behind the same trait; Windows coupled to the deferred Platform phase.
>
> **Amended by [DR-027](decisions/DR-027-c3bc-split-decide-enforce.md).** Sequences the folded c3bc into **c3bc-decide** (the egress decision/governance + type-safety + CA/TLS scaffolding layer, landed ENFORCEMENT-INERT — decides + builds certs but carries no live traffic, not wired live) then **c3bc-enforce** (the dataplane: `pasta` netns inescapability + live TLS byte-path + real injection + the WSL integration suite — carries DR-026's crit 3/4 and exit demo). DR-026 not weakened; its criteria are partitioned across the two slices.
>
> **Amended by [DR-028](decisions/DR-028-c3-wire.md).** Carves the **`c3-wire`** slice (Linux/WSL) after c3bc-enforce: wires C3a bwrap + the c3bc egress dataplane into the live spawn over one shared netns (pasta-outer), threads the daemon-owned composed child (S1), folds binds/allowlist/secrets from spec through `from_folded_authority` only (C6), and defines the product of the two asymmetric degrades (sandbox loud-OPEN × egress CLOSED) as three distinct loud facts. No invariant, posture, dependency, or ontology change. *(c3-wire's honestly-minimal empty deny-all fold — its dead run-loop Mediated arm — is sourced for real by DR-029.)*
>
> **Amended by [DR-029](decisions/DR-029-c3-egress-fold.md).** Carves the **`c3-egress-fold`** slice (Linux/WSL) after c3-wire: sources a non-empty egress allowlist + brokered secrets from a new project-spec `[egress]` block (allowlist + `[egress.secrets]` host→secret_ref) and a daemon-side `SecretSource` I4 seam (host-file MVP now via a `REZIDNT_EGRESS_SECRETS` authority-boundary file à la DR-020; a 1Password `op`-CLI backend fenced as the next backend), activating c3-wire's run-loop Mediated arm end-to-end so a shipped run proves DR-026 crit 4 (mediated + injected, agent never holds the token) at the run-loop level. Absent block ⇒ deny-all; unresolvable secret ⇒ dropped with a loud fact. C6 preserved; no invariant/posture/dep change; the `egress.*`/`credential.*` subjects are minted in a paired warden `/subject`.

Estimates are mine, part-time-founder calibrated, moderate confidence, wide intervals dominated by your available hours.

**Phase 1 — the fabric (4–8 weeks).**
*S0 (2–3 days):* ontology v0 + envelope + log + broadcast + `rezidnt tail`. Accept: two concurrent subscribers; `kill -9` the daemon mid-stream; restart; `rebuild` reproduces identical graph state; chain verifies.
*S1:* herdr adapter + `rezidnt open` materialization from spec. Accept: golden path on a clean VM in ≤ 5 minutes with zero config edits; every materialization step visible in `tail`.
*S2:* git adapter. Accept: `diff.ready` within 1 s of write (post-debounce); two-allocator worktree test produces exactly one registry entry and one `worktree.conflict` on deliberate collision.
*S3:* MCP surface. Accept: Claude Code, using MCP only, opens a project, spawns an agent, reads its dossier, and receives a `gate_explain` for a forced failure. **Phase 1 exit = golden path demo, one take, recorded.**

> **Amended by [DR-036](decisions/DR-036-operator-onboarding.md).** Attaches an operator-onboarding arc supporting the Phase-1 golden-path exit (S1 open/spawn, S3 gated run): implements the §9/§13-specified-but-unimplemented `rezidnt spec init` interactive plain-CLI generator, a read-only `doctor` env preflight, and a `rezidnt init` wrapper chaining `doctor → spec init → open` (I1: not a TUI; I5: a recorded pre-MCP bootstrap exception; I3: fact-free local file gen; I7: no telemetry) so the golden path's BINDING "zero config edits" clause is finally reachable, plus a `docs/quickstart.md` that IS the narrated one-take demo. Sequenced `spec-init` (end-to-end `init`→`open` pins "generated file untouched") → `onboarding-doctor` → `init-wrapper` → `quickstart`. No new phase, invariant, subject, or dependency.

> **Amended by [DR-037](decisions/DR-037-distribution-installer.md).** Makes the golden path's FIRST step real: a `curl | sh` installer + release machinery so the recorded one-take demo's `curl` install runs (DR-036 shipped every step EXCEPT install; the quickstart framed it "not yet live"). Linux/WSL-first scope fence (I6 — covers only substrates that run; macOS/native-Windows deferred). Ships two static `x86_64-unknown-linux-musl` binaries via GitHub Releases + sha256 (the pre-existing two-binary reality — the combined literal-I7 binary is a named follow-up, not a per-artifact re-read); clean-room `install.sh` (I8), checksum gate before install, no telemetry (I7). Reconciles with DR-007 (unrelated runtime trait). Three slices: `release-ci` → `install-script` → `quickstart-real`. No new phase/invariant/subject/dep.

**Phase 2 — harness and gates (6–12 weeks).** Port Omnigent's Claude Code harness adapter as the native `AgentSubstrate` reference (donor pattern, NOTICE); verifier engine v1 with the built-in native pack + exec contract; `vet` and `pre_merge` live on the golden path. *S4 accept:* an agent spawned under rezidnt gates produces a verified merged diff with replayable `debrief`. Phase 2 exit = the benchmark harness runs end-to-end against rezidnt itself.

> **Amended by [DR-044](decisions/DR-044-live-lead-sub-fanout.md).** Records that **Phase 2's exit criterion is MET**: the benchmark harness runs end-to-end against rezidnt itself via a real `DaemonDriver` (`bench/harness/tests/real_driver.rs`, `#[cfg(unix)]`) over the DR-022 scenario set, per DR-022/DR-023. On that basis the owner opened Phase 3 (2026-07-24) for the **orchestrator** arc first — the DR-042 live lead→sub fan-out slice. The ordering cost is recorded plainly in DR-044: the owned terminal substrate below remains the other, larger Phase-3 arc and the actual herdr-removal endgame; taking the orchestrator slot first does NOT supersede or reorder it.

**Phase 3 — owned terminal substrate (3–6 months to herdr-2024 parity).** Assemble the permissive kernel — libghostty-vt (MIT, high confidence) or `alacritty_terminal`/`vte` (Apache-2.0/MIT family, moderate — verify at kickoff) + `portable-pty` (WezTerm lineage, high) — then the multiplexer, persistence, and agent-detection layer as a `TerminalSubstrate` impl behind the same trait herdr sits behind today. *S5 (can precede Phase 3):* ratatui read-only fleet board consuming only watch channels — the proof that I1 held. Phase 3 exit = golden path runs with `substrate = "native"` and herdr uninstalled.

> **Amended by [DR-031](decisions/DR-031-board-read-only-operator-client-split.md).** The S5 board is read-only permanently; "build out the board" means richer read-only render only. Operator write actions (resolve an escalated permit, kill a run) live on a separate MCP write client — the operator-client seam — never the board.

Sequencing law, restated because it is the project's most-violated invariant in the wild: **fabric → harness → terminal.** Any pressure to reorder is scope gravity and gets the phase-exit-demo test applied to it.

## 17. Repository, licensing, and the commercial seam

> **Amended by [DR-003](decisions/DR-003-retire-ip-memo-gate.md).** The employer-IP-memo gate is retired. Per [DR-001](decisions/DR-001-native-substrates.md), no third-party code is ported, so the NOTICE obligation described below does not arise.
>
> **Amended by [DR-022](decisions/DR-022-benchmark-harness-slice.md).** The permanently-excluded benchmark held-out set (below) is recorded as a slice-level boundary: gate precision/recall cannot be computed in-repo, so `bench/harness` carries only the seam and the labeled data lives external forever.

Public repo `rezidnt/rezidnt` from first push: Apache-2.0 at root, `MIT OR Apache-2.0` on `crates/*` libs, DCO enforced, NOTICE carrying Omnigent attributions for ported code, TRADEMARKS.md (mark owned by TwofoldTech LLC), SECURITY.md, CONTRIBUTING.md. Excluded from this repo permanently: `rezidnt-enterprise` (RBAC/SSO/audit-export, hosted control plane if ever), domain verifier judgment packs, and the benchmark held-out set. The seam is structural — separate repos, separate crates — so a future dual-license or paid tier requires zero untangling, exercised or not.

## 18. Risk register

| Risk | Signal | Mitigation already in the architecture |
|---|---|---|
| herdr API churn or adversarial turn | weekly releases; maintainer's "agent runtime" ambitions | trait seam (I4), recorded contract tests, version-gated hello, Phase 3 replacement path |
| Omnigent alpha churn | breaking WS/API changes | pinned version, feature flag, donor-not-depend posture |
| Databricks ships server MCP + RBAC | their stated roadmap | differentiation is evidence-gates + local-first + Windows, none of which their model rewards |
| First-party absorption (Anthropic/OpenAI orchestration) | Claude Code teams features | cross-vendor governance seat; rezidnt orchestrates *their* harnesses |
| AGPL contamination | any herdr code in-tree | I8 + CI denylist; IPC-only integration |
| Scope gravity | building layer N+1 before N has users | phase-exit demos are the only definition of done |
| Solo-founder bus factor | — | boring tech, spec/ontology as versioned docs, golden fixtures |
| Name fails a registry check | tonight's lookups | fallback string `rezident`, conventions unchanged |
| Installer drifts from what CI publishes (DR-037: wrong artifact name, stale checksum, moved endpoint) silently breaks golden-path step one | a `curl \| sh` that 404s or installs a mismatched binary | `install-script` verifies the fetched sha256 against the published one (fail-closed); quickstart prose pinned to the ratified GitHub-asset endpoint; Linux/WSL platform refusal is plain-language, never a half-install |

## 19. Open decisions register

> **Amended by [DR-031](decisions/DR-031-board-read-only-operator-client-split.md).** A separate operator write client (resolve-escalation, kill-run) is the sanctioned home for board-adjacent writes — distinct from the read-only board, DR-first, demand-gated.
>
> **Amended by [DR-032](decisions/DR-032-operator-client-kill-run.md).** Operator client slice 1: `rezidnt operator kill-run <run>` over loopback MCP-HTTP (badged `kill_run` tool → existing reaper → `agent.signaled`). Resolve-escalation sequenced to slice 2.
>
> **Amended by [DR-038](decisions/DR-038-operator-client-gui-tauri.md).** Promotes §19's demand-gated "Tauri dashboard" to a ratified, separate-repo GUI operator client (`rezidnt-operator`, Tauri; a static-web on-ramp that promotes without a frontend rewrite; terminal UI rejected for the IDE-scale surface). Lives OUTSIDE the governed crates (I1 — daemon headless), write-capable but split from the read-only board (DR-031), rides existing MCP + read/tail + DR-032 badged loopback-HTTP with no new backchannel. Gates only the live-daemon wiring.
>
> **Amended by [DR-042](decisions/DR-042-golden-path-orchestrator.md).** Ratifies the shape + invariant contract of a first-party headless golden-path orchestrator — a lead run fans out to N sub-runs over the EXISTING `AgentSubstrate` + §7 worktree registry (no new substrate, I4); the orchestration graph is a pure fold over events (I3, the inverse of a server-held session — the memo-002 wedge), result-fold is control-plane-clean (I2, `CasRef`s not inline diffs), exposed MCP-first (I5), verdicts fold through the unchanged I6/DR-041 path. Cites intel memo 002 (rezidnt's sharpest exposure). GATES the live golden-path fan-out wiring to Phase 3 by the sequencing law (design/prototype free); owes a Phase-3 slice (§9 no-drift pin + I3 `rebuild`-equivalence proof) and a warden `/subject` only if the graph cannot fold from existing facts.
>
> **Amended by [DR-044](decisions/DR-044-live-lead-sub-fanout.md).** OPENS the DR-042 Phase-3 gate for this arc (owner-authorized 2026-07-24; Phase 2's exit criterion is met — see §16) and moves the orchestrator register line from ratified-but-gated to OPEN. Mints one mutating MCP tool `fan_out` (§9 no-drift), fixes the self-edge defect at both the emit site and the projection, bounds fan-out at a DEFAULT 8/call as the sole backpressure (the §4 supervisor breaker does not exist), and settles the subject question: no orchestration subject is owed, but a warden `/subject` IS owed to widen `worktree.allocated.allocator`'s value vocabulary for a delegating lead-scoped allocator. The owned terminal substrate remains the other, larger Phase-3 arc and is NOT superseded.

`rmcp` maturity (verify at S3; fallback: thin hand-rolled JSON-RPC). SQLite write amplification (revisit only past 10⁴ events/min). WASM verifiers as a third kind (post-Phase 2; exec contract already covers polyglot). Macaroon-attenuated badges (needs a real delegation use case). Komorebi IPC depth (verify at arranger implementation). First graphical client: ratatui board is S5; Tauri dashboard is demand-gated. Hash-chain externalization (periodic chain-head publication for third-party timestamping) — PROVISIONAL, compliance-driven.

## 20. Decision records

BINDING items change only through a dated decision record. Records live one per file under [`docs/decisions/`](decisions/), numbered sequentially; each closes by naming the number its own future amendment must take. This catalog is the index — **where a section above is marked "amended by DR-00N", the plan's text is no longer the whole truth until you have read that record.**

| DR | Title | Status | Amends |
|---|---|---|---|
| [DR-001](decisions/DR-001-native-substrates.md) | Native substrates from day one | ACCEPTED | §1, §3, §4, §7, I8, §16, §18 |
| [DR-002](decisions/DR-002-prior-art-protocol.md) | Prior-art protocol for competitor sources | ACCEPTED | DR-001 (tightens) |
| [DR-003](decisions/DR-003-retire-ip-memo-gate.md) | Retire the employer IP memo standing gate | ACCEPTED | preamble, §17 |
| [DR-004](decisions/DR-004-exit-code-table.md) | Stable exit-code table | ACCEPTED | §9 |
| [DR-005](decisions/DR-005-badge-consolidation.md) | Badge model consolidation | ACCEPTED | §12 |
| [DR-006](decisions/DR-006-replay-divergence-signal.md) | Replay-divergence integrity signal | ACCEPTED | §8, §14 |
| [DR-007](decisions/DR-007-release-worktree.md) | RepoSubstrate `release_worktree` as-built | ACCEPTED | §7 |
| [DR-008](decisions/DR-008-permit-engine-pivot.md) | Permit-engine pivot (rezidnt owns both axes) | ACCEPTED | §1, §8, §16 |
| [DR-009](decisions/DR-009-match-omnigent-scope.md) | Match-Omnigent scope (four memo-surfaced permit capabilities) | ACCEPTED | §16 |
| [DR-010](decisions/DR-010-intent-lock-scope.md) | SP-intent scope + criteria (C7 intent-lock) | ACCEPTED | §16 |
| [DR-011](decisions/DR-011-permit-pdp-config-seam.md) | Permit PDP config-resolution seam (McpSubstrate method) | ACCEPTED | §8, §9 |
| [DR-012](decisions/DR-012-empty-vs-absent-intent.md) | Declared-empty vs absent intent allowlist (intent-lock) | ACCEPTED | §16 |
| [DR-013](decisions/DR-013-permit-pep-sp2-integration.md) | Permit PEP integration + fail-posture (SP2) | ACCEPTED | §9, §16 |
| [DR-014](decisions/DR-014-permit-pep-hook.md) | Permit PEP hook binary + spawn injection (SP2 hook sub-slice) | ACCEPTED | §9, §16 |
| [DR-015](decisions/DR-015-permit-exec-verifier.md) | Permit axis dispatches exec verifiers (SP3 bring-your-own policy DSL) | ACCEPTED | §8, §9, §16 |
| [DR-016](decisions/DR-016-permit-roles-sp4-slicing.md) | SP4 slicing + SP4a roles scope | ACCEPTED | §8, §9, §16 |
| [DR-017](decisions/DR-017-permit-macaroon-delegation-sp4b.md) | SP4b macaroon-attenuated delegation (crypto + dep choice) | ACCEPTED | §12, §16 |
| [DR-018](decisions/DR-018-delegation-edge-id-and-offline-boundary.md) | Delegation edge id from the running sig + offline-boundary defer (SP4b) | ACCEPTED | §12, §16 |
| [DR-019](decisions/DR-019-c8-layered-precedence-sp4c.md) | SP4c: C8 layered policy precedence via monotone concat | ACCEPTED | §8, §9, §16 |
| [DR-020](decisions/DR-020-sp4c-wire-layered-permit-sourcing.md) | SP4c-wire: three-source layered permit wiring (admin outside the workspace spec) | ACCEPTED | §9, §16 |
| [DR-021](decisions/DR-021-live-spend-cap-c1.md) | Live spend-cap (C1): spend measured post-action, off the permit fact (B2) | ACCEPTED | §8, §9 |
| [DR-022](decisions/DR-022-benchmark-harness-slice.md) | Benchmark-harness slice: in-repo three-metric dogfood, precision/recall fenced external | ACCEPTED | §15, §16, §17 |
| [DR-023](decisions/DR-023-shared-daemon-driving-client.md) | Extract shared `rezidnt-client` socket driver (unblocks DaemonDriver); fixtures stay dev-only test-support | ACCEPTED | §4 |
| [DR-024](decisions/DR-024-running-risk-cap-c6.md) | Running-risk cap (C6): deterministic rule-table scorer, pre-action, granted-only fold, contract-free shared-scorer seam | ACCEPTED | §8, §9 |
| [DR-025](decisions/DR-025-c3a-linux-sandbox.md) | C3a Linux OS-sandbox: bwrap-backed `SandboxSubstrate` (I4), folded-policy binds, loud degrade when absent, `PathConfinement` verdict stays a permit-verifier | ACCEPTED | §16, §18 |
| [DR-026](decisions/DR-026-c3bc-egress-credential-brokering.md) | C3b+c full L7 egress MITM + credential brokering: `EgressProxy` (I4) sole inescapable route out of C3a's netns, rezidnt CA (rustls/rcgen), secrets injected upstream/never-held, logged by-ref-never-value, degrade CLOSED; first C3 linked deps | ACCEPTED | §16, §18, App. A |
| [DR-027](decisions/DR-027-c3bc-split-decide-enforce.md) | C3b+c split decide→enforce: land the egress governance/type-safety/CA-scaffold layer ENFORCEMENT-INERT; sequence DR-026's full-MITM dataplane (inescapability + real injection, crit 3/4 + exit demo) into its own next slice. DR-026 not weakened — criteria partitioned | ACCEPTED | §16, §18 |
| [DR-028](decisions/DR-028-c3-wire.md) | C3 run-loop wiring (c3-wire): compose C3a bwrap + c3bc egress dataplane into the live rezidnt open/spawn over ONE shared netns (pasta-outer, sealed-before-agent), daemon-owned composed child (S1), folded-from-spec first source (C6 preserved), three composed degrade facts. No invariant/posture/dep/ontology change | ACCEPTED | §16, §18 |
| [DR-029](decisions/DR-029-c3-egress-fold.md) | Real egress fold (c3-egress-fold): source a non-empty egress allowlist + brokered secrets from a project-spec `[egress]` block + a daemon-side `SecretSource` I4 seam (host-file MVP now, 1Password op-CLI backend fenced next) so a shipped run reaches the run-loop Mediated arm end-to-end (DR-026 crit 4 at run-loop level). C6 preserved; no invariant/posture/dep change; egress.*/credential.* subjects minted in a paired /subject | ACCEPTED | §16, §18 |
| [DR-030](decisions/DR-030-c3-op-secrets.md) | 1Password op SecretSource backend (c3-op-secrets): a headless `OpSecretSource` behind the DR-029 seam resolving `op://vault/item/field` refs via exec'd `op read` (not linked, I7), authed by a vault-scoped service account (`OP_SERVICE_ACCOUNT_TOKEN`, env-only, never facted); `CompositeSecretSource` scheme-dispatch (op:// → op, else host-file); resolution daemon-side at fold time; op absent/token unset/read-fail ⇒ loud credential.dropped, never a fake secret. "1Password for Claude" connector explicitly OUT (wrong layer). No invariant/posture/dep/ontology change | ACCEPTED | §16, §18 |
| [DR-031](decisions/DR-031-board-read-only-operator-client-split.md) | Board read-only / operator-client split: `rezidnt-tui` stays read-only permanently (guarded by `crate_has_no_writer_dependency`, I1); the two operator write actions — resolve an escalated permit, kill a run — live on a SEPARATE explicitly-authorized MCP write client through the existing PDP (I2/I5), not on the board. "Build out the board" = richer READ-ONLY render only. Operator client is DR-first + demand-gated (Phase 3). No test weakened; the structural guard is promoted to a permanent invariant | ACCEPTED | §16, §19 |
| [DR-032](decisions/DR-032-operator-client-kill-run.md) | Operator client, slice 1 (kill-run): a badged MCP `kill_run { run }` tool behind the §12 operator-badge door (operator badge REQUIRED, agent macaroons NOT admitted — kill is an operator action) driving the existing reaper `stop_with_escalation` and emitting `agent.signaled` through the single writer; NO new PDP path (refines DR-031's PDP-route language for the non-decision kill action); a `rezidnt operator kill-run <run>` subcommand over loopback MCP-HTTP (not the bare socket — explicit authorization over ambient UDS identity), distinct from the read-only board (I1 proof untouched). REUSE `agent.signaled` + additive operator attribution (paired /subject). Resolve-escalation SEQUENCED to slice 2 (partitioned per DR-027, not weakened). No invariant/posture/dep change | ACCEPTED | §16, §19 |
| [DR-033](decisions/DR-033-operator-client-resolve-escalation.md) | Operator client, slice 2 (resolve-escalation): a badged MCP `resolve_permit { run, request_id, decision, reason? }` tool behind the operator-only door (macaroons refused, mirrors DR-032) recording a durable `permit.resolved` fact via the single writer; semantics = "honored on next ask" — `decide_permit` consults the folded ledger BEFORE verifiers and applies a matching prior human resolution as `permit.granted`/`permit.denied` citing the resolution as authority (I6), keyed on ACTION identity `(run, tool, action/target)` since request_id is re-minted per ask. NO live-unblock / long-poll — one-shot PEP contract unchanged (live-unblock rejected, demand-gated future). Defaults: no TTL (override by new resolve), request-scoped not broad-predicate. Paired /subject mints `permit.resolved`. No invariant/posture/dep change | ACCEPTED | §16, §19 |
| [DR-034](decisions/DR-034-operator-client-live-unblock.md) | Operator client, live-unblock: resume the CURRENTLY-stalled agent on resolve (pulls forward DR-033's demand-gated deferral). A bounded server-assisted long-poll — the PEP re-issues its request, the daemon holds it up to a SEPARATE longer deadline (`REZIDNT_UNBLOCK_TIMEOUT_MS`, distinct from the 250ms hot-path budget), returning the applied `permit.granted`/`permit.denied` if a matching `permit.resolved` lands (agent resumes, no re-prompt) or fail-closed `ask` on expiry (DR-033's fallback). Rejects the held-open `Reply::PermitUpdate` push (unbounded I2 coupling) and a pub/sub wake seam. Layers ON TOP of DR-033's ledger-check (unchanged, still the correctness path + fallback); wakes by the ORIGINAL escalated request_id held on the open connection. I2 bounded-and-honest, NOT untouched (dissent recorded); I3/I5/I6/I1/I7 preserved. Likely NO new subject (flagged if a `resumed_live` audit marker earns it). Amends DR-033 §Decision 1 | ACCEPTED | §16, §19 |
| [DR-035](decisions/DR-035-escalation-resolution-scope-and-lifetime.md) | Escalation resolution scope & lifetime (TTL/expiry + grant-all-matching predicate): pulls forward BOTH of DR-033's demand-gated deferrals (§Decision 2 no-TTL, §Decision 3 request-scoped), designed together because the security surface is DURATION × BLAST-RADIUS. TTL = log-derived expiry from event ULIDs (the incoming request's envelope ULID vs the resolution's + TTL, a pure fold — I3-clean, no decision-time wall-clock; the `SystemTime::now()` variant rejected). Grant-all = a minimal opt-in single-axis wildcard (default stays exact request-scoped; broad grants are recorded + interrogable, not silent). Interaction COUPLED: a broad resolution MUST carry a bounded TTL (broad OR permanent, never both — the worst quadrant is structurally un-representable, refused at the `resolve_permit` boundary). Both land in `resolution_for`, so DR-034 live-unblock + the DR-033 PDP path inherit both for free. Additive optional `ttl?`/`scope?` args (no new verb, §9 no-drift) + paired /subject for additive `permit.resolved` fields. Sequenced: `escalation-ttl` first, then `escalation-grant-all`. No test weakened | ACCEPTED | §16, §19 |
| [DR-036](decisions/DR-036-operator-onboarding.md) | Operator onboarding arc (golden-path first-run): OPERATIONALIZES the already-BINDING golden path for an adopting operator — closes the gap that its "zero config edits" clause is UNMET because the §9/§13-SPECIFIED `rezidnt spec init` generator does not exist in the CLI (verbs are Rebuild/Tail/Board/Open/Attach/Vet/Debrief/Gate/Operator/PermitHook only). Owner ratified the fullest command scope: `spec init` (interactive PLAIN-CLI generator — I1: prompts, NOT a TUI — writing a §13 `rezidnt.toml` the golden path runs UNTOUCHED), a read-only `doctor` env preflight (I6 pass/inconclusive, never coerced), and a `rezidnt init` wrapper chaining `doctor → spec init → open`. A legitimate pre-MCP bootstrap exception (I5, stated); pure local file gen, NO fabric fact (I3) and NO telemetry (I7). Docs = an operator quickstart under `docs/` that IS the narrated one-take golden-path demo, kept in lockstep. Four slices: `spec-init` (end-to-end `init`→`open` pins "generated file untouched") → `onboarding-doctor` → `init-wrapper` → `quickstart`. No new invariant/subject/dep/lore; no test weakened (raises the golden path's zero-config clause to testable) | ACCEPTED | §9, §13, §16 |
| [DR-037](decisions/DR-037-distribution-installer.md) | Distribution/release posture — a real `curl \| sh` installer for the golden path's install step: OPERATIONALIZES the already-BINDING §1/§18 FIRST step (still aspirational after DR-036 shipped every OTHER step; `docs/quickstart.md` framed `curl … install.sh` as "not yet live", `cargo install --path` the works-today path). Linux/WSL-first scope fence (I6 honesty — covers only substrates that run; macOS/native-Windows deferred to a later DR). Ships two static `x86_64-unknown-linux-musl` binaries (rezidnt+rezidentd) — packaging the PRE-EXISTING two-binary reality, NOT a new per-artifact I7 re-read; the combined multi-call single binary (literal-I7 form, a daemon-crate extraction) is NAMED as a follow-up, not left as a reinterpretation. GitHub Releases off semver tags + sha256, clean-room `install.sh` (I8) with a checksum gate before install and no telemetry (I7). Reconciles with DR-007 (runtime `release_worktree` trait, NOT a release-build worktree — no collision). Three slices: release-ci → install-script → quickstart-real. quickstart lockstep does NOT cover the install prose — a hand-held honesty obligation, stated. No new invariant/subject/dep/lore; no test weakened | ACCEPTED | §1, §18, §16 |

| [DR-038](decisions/DR-038-operator-client-gui-tauri.md) | Operator client GUI (Tauri), separate-repo: ratifies a first-party IDE-like GUI operator surface (`rezidnt-operator`, Tauri = Rust backend + web frontend; a static web page on-ramp promotes to Tauri with no frontend rewrite; a terminal UI REJECTED for an IDE-scale surface — the board keeps the TUI role) living in a SEPARATE repo sibling to rezidnt, NOT the governed crates — the structural consequence of I1 (daemon stays headless). Write-capable (operator actions), deliberately split from the read-only board whose writer-freedom is an I1 proof (continues DR-031). Talks over EXISTING surfaces only — MCP (I5) + read/tail for inspection, DR-032 loopback-HTTP badge for writes — NO new privileged backchannel, no authority beyond the operator badge. First artifact = a start screen ("Open a project" / "Look inside rezidnt"). GATES only the live-daemon wiring (design/shell/prototype free now). I1 preserved (no pixels in core), I5 honored (MCP-first, buttons follow tools), I7 untouched (daemon still one static binary; the client is a separate artifact), I8 applies to the client's sources. No new invariant/subject/dep/lore; no test weakened | ACCEPTED | §19 |

| [DR-039](decisions/DR-039-board-view-mcp-resource.md) | `board_view` read-only MCP resource (fleet BoardView over the surface): adds one READ tool `board_view` (unbadged, `tail_events` class, DR-005) returning the `BoardView` projection (events_folded, workspaces open/closed, counts_by_subject, runs[], worktrees[]) via a pure `replay(None) → rezidnt_state::fold → project` path — the governed MCP read DR-038's GUI runs/worktrees panels must ride (I5: buttons follow tools). CRUX: `project()` + `BoardView`/`RunRow`/`WorktreeRow` HOISTED from `rezidnt-tui` DOWN into `rezidnt-state` so both `rezidnt-mcp` (a writer) and the board reuse one projection with zero new deps / zero cycles — mcp-depends-on-tui REJECTED (entangles DR-031's `crate_has_no_writer_dependency` I1 proof), mcp-reimplements REJECTED (drift). §9 BINDING no-drift: `BoardViewArgs` schemars-generated + oracle-pinned (jsonrpc_surface.rs). I1 preserved (data not pixels; board dep set + read-only proof untouched), I3 preserved (pure fold, verbatim, one derivation), I5 the mechanism (governed read before the button), I6 deterministic read. NO badge/write/PDP/subject/dep/invariant; snapshot whole-log fold cost noted; since/incremental + push DEFERRED. No test weakened | ACCEPTED | §9 |

| [DR-040](decisions/DR-040-get-escalations-mcp-resource.md) | `get_escalations` read-only MCP resource (outstanding permit escalations over the surface): adds one READ tool `get_escalations` (unbadged, `tail_events`/`board_view` class, DR-005) taking `GetEscalationsArgs { run: Option<String> }` (one run or all) and returning `Vec<EscalationRow>` (run, request_id, action, target?, reason?, policy_ref?) — the drill-down DETAIL behind `board_view`'s per-run `permit_escalated` COUNT, the governed MCP read DR-038's GUI escalation-resolution panel must ride (I5: buttons follow tools), feeding the EXISTING `resolve_permit` write (DR-033/035, untouched). FINDING that sizes it: PURE PROJECTION — `AgentRunState.permit_ledger` already folds each escalation (decision == "escalated") keyed by request_id, so a new pure `escalations(&Graph, filter)` over `replay(None) → fold` reads existing derived state with NO reducer arm / NO new field / NO subject change. SEPARATE tool over extending `BoardView` (board stays the count summary; this is the lazy operator-driven detail — extending `RunRow` would bloat every board read). One new `EscalationRow` view type beside `BoardView` in `rezidnt-state` (DR-039 precedent), sourced verbatim from `PermitLedgerEntry`. §9 BINDING no-drift: `GetEscalationsArgs` schemars-generated + oracle-pinned (jsonrpc_surface.rs). I1 preserved (data not pixels), I3 preserved (pure projection of already-folded ledger, no new derivation), I5 the mechanism (governed read before the button), I6 honored (deterministic; escalations surfaced verbatim with reason/policy_ref, never coerced — a read must not resolve). NO badge/write/PDP/reducer/subject/dep/invariant; snapshot whole-log fold cost noted; since/incremental + push DEFERRED. No test weakened | ACCEPTED | §9 |
| [DR-041](decisions/DR-041-production-verifier-pack-v1.md) | Production verifier pack (v1): replace the `tests-pass` exec STUB on the golden path with real §8 verifiers. S4's engine + exec contract (`ExecVerifier{name,argv}`) are built and green, but the shipped test-suite verifier is a fixture STUB (`golden_path.rs`) — so an adopter's `[gates.pre_merge]` has cheap policy natives and a stub for the thing they most want gated (that tests pass). Decides: (1) v1 CORE = `cargo-test` + `clippy` + `fmt-check`, fast-follow `secret-scan` + `dependency-audit`; coverage/user-exec/non-Rust DEFERRED. (2) RULE: exec when the verdict comes from a subprocess-natural external tool (cargo-test/clippy/fmt/dep-audit), native when in-process CAS-replayable over the pinned diff (secret-scan). (3) LOCATION: ship the pack as `rezidnt verify <name>` subcommands of the same static binary, invoked as exec argv, reusing `resolve_one`'s existing exec path — serves literal-I7; PATH-lookup + verifiers-dir REJECTED. Honesty note recorded plainly: in-binary is the WRAPPER only — `cargo-test` still shells to host `cargo`; missing toolchain → inconclusive/could-not-run, never a silent pass. (4) I6: no network, pinned toolchain; compile-error/toolchain-absent → inconclusive(could-not-run), test-fail → fail, timeout/flaky → inconclusive(timeout), never coerced; `gate_explain` names the failing test/lint; `cost_ms` recorded not asserted. (5) GUARDRAIL: `golden_path.rs` KEEPS its fast `tests-pass` stub — the real pack ships for real project specs, not by slowing the one-take demo. NO new invariant/subject/badge/PDP/reducer/dep. No test weakened | ACCEPTED | §8, §16 |

| [DR-042](decisions/DR-042-golden-path-orchestrator.md) | Golden-path multi-agent orchestrator (lead → parallel sub-runs) over the worktree registry: ratifies the SHAPE + invariant contract of a headless first-party orchestrator — a lead run fans out to N sub-runs, each spawned via the EXISTING `AgentSubstrate` under the supervisor and isolated in its own registered §7 worktree (reuse the rails; NO new isolation substrate or run backend, I4). Cites intel memo 002 (DR-002 rule 3) — Omnigent's shipped lead→parallel-worktree orchestrator is rezidnt's sharpest exposure; built the rezidnt way, the orchestration graph is a PURE FOLD over events (I3, `rebuild`-able), the inverse of Omnigent's server-held session state (the wedge), never a mutable in-daemon session of record. Result-fold control-plane-clean (I2: `CasRef`s/≤32KiB, never inline diffs on the fabric). Exposed MCP-first (I5: governed tool, schemars no-drift, buttons follow tools); sub-run verdicts fold through the UNCHANGED I6/DR-041 verifier path (inconclusive never coerced). GATES the live golden-path fan-out wiring to Phase 3 (design/prototype free now) — rides shipped+green rails (S3 demo, S4 path, DR-041 pack) so it's composition-of-rails, but Phase-3 by the sequencing law (DR-038 gate-only-the-wiring pattern). Owed: a `/subject` (warden) lead↔sub lifecycle fact ONLY if the graph cannot fold from existing facts; a Phase-3 slice carrying the §9 no-drift pin + an I3 `rebuild`-equivalence proof. Fan-out cap + mixed-harness DEFERRED. NO new invariant/subject/badge/PDP/reducer/dep; no test weakened | ACCEPTED | §19 |

| [DR-043](decisions/DR-043-secret-scan-content-ref.md) | Unblock the `secret-scan` native verifier: pin the diff's added content to CAS as a new input ref. AMENDS DR-041 Decision 2 — KEEPS `secret-scan` NATIVE, closes the input-shape gap that made that classification unbuildable. The blocker (found building the slice): at `pre_merge` the daemon passes natives only a content-free path-status summary (`git_diff_summary` → `refs["diff"]`, `runs.rs:1576`) — the git adapter never `cas.put()`s file content and natives ignore cwd — so a native `secret-scan` has NO bytes to scan (an I6-dishonest label). Decides: (1) native/exec RULE reaffirmed, not overturned; (2) the git adapter `cas.put()`s per-file ADDED CONTENT as a new pinned `refs["content"]` CasRef beside the retained path-status summary; (3) the native scans that pinned ref — verdict a pure function of the content hash, CAS-replayable; (4) oversized/non-text content → inconclusive (I2 ≤32KiB→CAS discipline), never coerced. Exec-reads-live-worktree REJECTED: unpinned mutable on-disk state is an I3/I6 determinism regression — the CAS-pinned ref is why "native" was correct. Owed by the build slice: an input-contract pin test, a CAS-replay-equivalence test, and the inconclusive-on-unscannable-content path. New event subject/field is a warden question if the ref can't ride existing `diff.ready`/CAS facts — FLAG, don't mint. NO new invariant/badge/PDP/reducer/dep | ACCEPTED | §8 |

| [DR-044](decisions/DR-044-live-lead-sub-fanout.md) | Live golden-path lead→sub fan-out — EXECUTES the DR-042 §Consequences (b) deferral and OPENS its Phase-3 gate (owner-authorized; Phase 2's exit criterion is met — `bench/harness/tests/real_driver.rs` drives a real `DaemonDriver` end-to-end). Mints ONE mutating MCP tool `fan_out` (`FanOutArgs{badge,workspace,tasks}`/`FanOutTask{agent,idempotency_key}`, schemars-generated, §9 no-drift) — one-tool-N-tasks over N `delegate` calls (a per-call shape cannot enforce a width cap atomically); idempotency composes PER TASK through the existing log-derived `spawn_keys` map; partial failure is a per-task outcome vector with NO fake all-or-nothing rollback; authorized by the lead's own badge on the EXISTING `spawn` verb (no new verb/badge kind). LOAD-BEARING FINDING that sizes the slice: the shipped read side is a MIRAGE — `runs.rs:985` emits `permit.delegated` keyed on the run BEING SPAWNED with that run's own base badge as parent, and `orchestration_graph` has no `sub_run != lead_run` guard, so every role-declaring run projects as a lead OF ITSELF (`fan_out: 1`); the green projection test pins a lead-keyed cross-run edge NO emitter produces. Fix at BOTH ends: (a) the projection gains a self-edge guard — DR-017's role-attenuation edge is a capability-chain fact on a DIFFERENT axis the projection was over-reading; (b) the fan-out path emits a genuinely lead-parented edge (`child_badge_id` MUST equal the sub's folded `agent.spawned.badge_id`). NO discriminator minted on `permit.delegated` — the guard makes "lead-keyed edge whose child badge belongs to a different run" the discriminator, derivable from existing fields. Sub isolation on the EXISTING §7 registry, allocator = the lead's run ULID; a `worktree.conflict` makes a task a REFUSED sub (no run minted → nothing folds as fail) with NO in-daemon retry loop (livelock against a sole allocator). Width cap DEFAULT 8/call (`[orchestrator] max_fan_out`), whole-call refusal before any effect — recorded honestly as the ONLY backpressure, since the §4 supervisor backoff/circuit-breaker does NOT exist in the tree. Fold-back by REUSE only (I2: existing CAS refs + `agent.completed` cost; the lead READS `orchestration_graph` and holds nothing — the I3-rot guard). Subject verdict: NO orchestration/lead↔sub subject owed (the graph folds from existing facts), but ONE warden `/subject` IS owed — widening `worktree.allocated.allocator`'s v1 vocabulary (`"rezidnt"`/`"human"` reserved) to admit a delegating lead-scoped allocator. Owes five guards: write-side no-drift pin, I3 fold-equivalence over a REAL fanned-out log, self-edge regression, width-cap refusal, and an I6 conflicted/inconclusive-uncoerced test. Records the ordering cost plainly: the owned terminal substrate (the herdr-removal endgame) is the other Phase-3 arc and is NOT superseded. Nested fan-out, lead-side cancellation, cross-workspace fan-out, mixed-harness all DEFERRED. NO new invariant/subject/badge/PDP/reducer/dep; no test weakened | ACCEPTED | §19, §16, §9 |

| [DR-045](decisions/DR-045-fan-out-lead-only.md) | `fan_out` is lead-only — an operator badge is refused at the door. AMENDS DR-044 §Decision 1 ONLY, adding the door semantic that record left undefined. The gap (found by the oracle while writing DR-044's failing tests): DR-044 derives the lead run from the caller's badge, but the §12 door is DUAL-PATH (`rezidnt-mcp/src/lib.rs:563-647`) — path 1 admits the DR-005 opaque OPERATOR badge by token equality, path 2 verifies an agent macaroon — and the operator badge maps to NO RUN, so an operator-badge call passes the door with no lead to key DR-044 §Decision 2(b)'s edge on. Decides: (1) `fan_out` admits ONLY a verified agent macaroon; an admitted operator token is REFUSED on POLICY, not on verification — a third door beside `check_badge`/`check_operator_badge`, in the DR-032 shape. (2) MINTS one additive refusal code `codes::FAN_OUT_LEAD_ONLY = "fan_out.lead_only"` (dotted-lowercase per the existing vocabulary; older peers already tolerate unknown codes). (3) Refusal PRECEDES any effect — door order badge-required → lead-only → badge-invalid → width cap → substrate; no worktree, no spawn, no fact. (4) The refusal is DISTINGUISHABLE from a failed badge (I6): an operator badge is VALID, merely the wrong KIND, and returning `BADGE_INVALID` would tell the caller something false; since the refusal precedes any effect no fact reaches the log, so the tool result's code IS the interrogable surface — hence its own code. Rationale: keeps "every sub has a lead" TOTAL (an unparented sub is the same class of lie DR-044 closed from the other direction), withholds no capability (an operator already has `spawn_agent`), and mirrors DR-032 exactly — killing is a human's prerogative, delegation is a run's; neither tool admits both badge kinds. COUNTERARGUMENT RECORDED: this forecloses a plausible operator workflow (fan out without first spawning a lead); the explicit `lead_run`-arg alternative is DEFERRED, not rejected on principle — additive when demand appears. Owes one guard (operator badge → `FAN_OUT_LEAD_ONLY`, zero effect, plus a non-vacuity leg that a real macaroon IS admitted) and RE-CUTS the oracle's `fan_out_door_and_width_cap.rs`, four of whose five tests present an operator token on an un-root-keyed core and so assert the behavior this record forbids. NO new invariant/subject/badge kind/tool/arg/PDP/reducer/dep; no test weakened | ACCEPTED | §9, §12 |

| [DR-046](decisions/DR-046-fanout-limits-edge-and-registry-deferral.md) | Three things the DR-044 build surfaced. (1) **`fan_out` is structurally same-process-only** — DR-045 admits only a verified macaroon, DR-017 §6 mints the root key PER PROCESS, so a lead's badge dies with the daemon and no fan-out is admissible post-restart under ANY implementation; the ephemeral key is KEPT (persisting it would convert a process-lifetime secret into a long-lived stealable one to satisfy a requirement no record stated), the limitation is NAMED, and the oracle's restart leg is re-cut to what DR-044 §Decision 1 actually requires (the across-a-restart framing came from the orchestrator's brief, not the record; the dedup map DOES survive — only the badge cannot). (2) **The lead→sub edge is MIS-MODELLED — the slice's `/debrief` fail.** The ontology fixes `permit.delegated` as a fact about ATTENUATION (`:449` "was attenuated", `:451` "the badge that was attenuated", `:453` "the predicates that make the child strictly ⊆ the parent"), but the sub's badge is a FRESH mint over the sub's own run — nothing is attenuated, and `added_caveats: []` asserts a delegation that narrowed nothing, a stronger false claim than silence; the implementation's own comment concedes it ("the ontology's `added_caveats` is a REQUIRED field, making the empty list the only truthful value available"). I3, not naming: `BoardRow.delegated` is delegation-chain DEPTH, so a lead fanning out to five subs shows depth 5 and five attenuation records on the dossier `debrief` reads — none of which occurred. WITHDRAWS DR-044 §Decision 2b's emit and OVERTURNS §Decision 6's no-subject verdict BY EVIDENCE (DR-042 §Decision 6 reserved exactly this case: mint only when the graph cannot fold honestly from what exists — now demonstrated, not assumed). One warden `/subject` OWED, named not designed. Withdrawal and replacement land in ONE commit: the emit is never left in place for continuity, since retaining a known-false fact would be a deliberate choice to keep lying. The DR-017 self-edge stays valid and untouched. Genuine-attenuation-of-the-lead's-badge CONSIDERED and not taken (feasibility unverified — the daemon holds the lead's badge id, not its token), recorded so a future record can pick it up knowingly. (3) **Registry wiring DEFERRED to its own named slice** converging BOTH allocation paths. The owner's first instinct was to wire it now; it flipped on EVIDENCE, not cost — the owed I6 conflict test cannot be written black-box even WITH the registry wired (worktree paths are ULID-derived, so no test can pre-claim a path and two fan-out tasks can never collide), the conflict obligations are already pinned in the adapter's own suites, and the repoint is an I3 hazard on the golden path while a lane is red. The full brief for that slice is FIXED here: the daemon has NO dependency on `rezidnt-adapter-git` at all, `WorktreeReq` has no principal field, `alloc_worktree` hardcodes `"rezidnt"`, and the adapter's emitter broadcasts on a CHANNEL with a `None` workspace envelope rather than appending to the fabric — so a naive repoint drops the allocation fact off the log or double-emits. Deferral paid for in writing: DR-044 §Decision 3's conflict semantics are recorded as UNREACHED (isolation holds by ULID uniqueness, not by any registry guard) and `spec/ontology.md:216`'s ratified claim is recorded as FALSE of the code, owed to a warden correction. A conflict refusal code is owed to that slice, not minted here (no path can emit it yet). NO new invariant/subject/field/badge/tool/arg/code/PDP/reducer/dep | ACCEPTED | §7, §9, §19 |

| [DR-047](decisions/DR-047-registry-convergence-landed.md) | Registry convergence **LANDED** (DR-046 §Decision 8 discharged in full) — and the brief that authorized it was wrong. Both allocation paths now allocate through `RepoSubstrate::alloc_worktree` on ONE `GitAdapter` per **canonicalized repo root** (`Daemon::repo_adapter`); `WorktreeReq` carries an `Allocator` closed to `Rezidnt \| Run(Ulid)` (the reserved `"human"` sentinel is structurally unconstructible from a request); adapter facts **APPEND** through an injected `FactSink` whose `Err` fails the allocation — a broadcast is not an append (I3) — while the adapter keeps **NO** `rezidnt-fabric` dep (I4); exactly ONE `worktree.allocated` per allocation; `codes::WORKTREE_CONFLICT` minted from a structural `GitError::Conflict{path,holder}`, so **DR-044 §Decision 3's refused-sub rule is REACHED for the first time** (a contended task refused ALONE, siblings still spawn) and DR-046 guard (e) is discharged. **THE RECORD'S POINT:** DR-046 §Decision 8 said its brief was "all verified against the tree" and it was not — ONE stated premise FALSE ("additive, every existing caller unchanged"; five exhaustive struct literals needed edits) and FIVE conditions **unnamed**, one a HARD BLOCKER (`REGISTRY_PATH` was a JSONL **file** at the exact path the daemon `create_dir_all`s a **directory** — mutually exclusive; the repoint could not land as briefed), plus the empty-repo orphan fallback (~8 daemon suites), the CAS split, `startup_facts` running inside `open` before a sink can attach, and the allocation fact's id for causation. Omission is the graver failure: it reads as absence of risk. **A record saying "verified against the tree" is still a PREDICTION until a build meets it.** Ratifies two DEFAULT layout rulings (`REGISTRY_PATH` → `.rezidnt/registry.jsonl`; the **adapter** moved to the daemon's in-repo layout, not the reverse). Rules `diff.ready`'s **TWO emitters** deliberate — the watcher's is debounced/dedup'd/droppable, `pre_merge` pins its input **synchronously** (I6: gate inputs never inherited from a race) — and OWES the warden the emitter cell plus the `source: "rezidnt-adapter-git"` attribution the daemon's own fact carries. Records a derived-state defect the repoint introduced and this slice fixed (`git add -A` **reads** woke the watch; a 26-byte header-only `diff.ready` clobbered `last_diff` on a `"merged"` worktree) that host `/vet` could **never** catch — `ReadDirectoryChangesW` has no read notification, Windows 0 events, WSL 1 — fixed by `is_change_event` rather than dropping the watch, since `release_worktree` would fold `"released"` over `"merged"`. **OPEN and named next: the worktree RELEASE lifecycle** — `release_worktree` has no production caller, so DR-007's allocate→use→release never completes and every allocation leaks a watcher + debounce task + open registry entry. Arc ledger: **eleven** instances of one defect class, **three introduced by remediation commits** — remediation prose is the highest-risk prose in the repo. Untouched: DR-017 §6, DR-045, DR-046 §1–7, DR-001's sole-allocator model (now enforced **by the registry** rather than by path uniqueness — a strengthening). NO new invariant/subject/field/value/tool/arg/PDP/reducer/dep; no test weakened, two restored from tautology | ACCEPTED | §7, §5/§6, §8 |

| [DR-048](decisions/DR-048-trials.md) | **Trials arc OPENED** — one task run across N candidate variants (harness × model × agent-spec/skills), each in an isolated worktree (DR-044/046), verifier-adjudicated, with historical comparison and a best-of-N selection gate. The name "Trials" is ratified (a plain word — "arena" rejected for minting lore past the vet/debrief/dossier cap, "bench" for colliding with DR-022). **v1 selection = winner-take-all**: verifiers score all candidates, the best PASSING diff merges (an inconclusive candidate never wins — I6); integrator-synthesis is v2, deferred not rejected, its output gated by the same vet because an LLM is never judge-of-record. **v1 comparison = verdicts + wall time only** — cross-vendor token comparison refused as a fabricated metric (a Claude token and a Codex token are not the same unit). Slices: **A** extract `AgentSubstrate` (prose-only until now; the trait becomes real per I4, claude-code green throughout) + codex CLI adapter + `model` field on `AgentSpec` (no such field exists anywhere today); **B** trial matrix primitive — must answer `FanOutTask`'s per-task idempotency, which dedupes N samples of one task to ONE run; **C** collator v2 (tokens + `duration_ms` already ride `agent.completed`, nobody collates them) + leaderboard as another pure fold atop DR-022's collate — I3 makes history free; **D** the selection gate. `trial.opened`/`trial.scored` QUEUED for a warden `/subject`; nothing minted. **Binding lane boundary:** slice A phase 1 stays inside `crates/rezidnt-run` behind an unchanged constructor API so it can run concurrent with DR-049, which owns `bins/rezidentd/src/runs.rs`; daemon wiring lands only after that slice merges. NO new invariant/subject/badge/PDP; no test weakened | ACCEPTED | §7, §8, §5/§6 |

| [DR-049](decisions/DR-049-worktree-release-lifecycle.md) | **Worktree release lifecycle OPENED and its four undecided questions SETTLED** (discharges DR-047 §Decision 6). (1) A merged worktree is **released at merge** — DR-007's ratified allocate→use→release completes in production for the first time; a tree whose content the log can rebuild is not a record (I3), and retaining it retains its watcher. (2) The fold's collapsed `status` String (`rezidnt-state/src/lib.rs:847` vs `:866`) is **SPLIT into `lifecycle` (allocated→released) + `outcome` (merged\|failed\|**absent**; §Decision 2 also names `abandoned`, UNREACHABLE today — no minted fact sets it, `spec/ontology.md:359`)** — `worktree.released` sets lifecycle only, `diff.merged` sets outcome only, neither clobbers the other, so DR-047 §Decision 5's declined trade is DISSOLVED, not chosen between; the board's rendered shape and golden fixture move with it. (3) A failed run's tree **SURVIVES for triage** until an EXPLICIT release (operator or lead, MCP-first per I5, never the read-only board) — no TTL, timers are nondeterministic policy deferred to a later DR; the accumulation is accepted and named. (4) Release **drops the watcher + debounce task** — mechanism verified ALREADY BUILT in the adapter (the verb drops the watcher, removes the tree, closes the registry entry, emits the fact); the slice owes the production call paths and the fold, not the verb. Plus `WorktreeId` threaded through `RunTaskContext` (carries only a `PathBuf` today), and CARRIES the DR-047 §Decision 4 warden `/subject` (diff.ready emitter cell + source attribution), which rides because it moves with golden fixtures this slice touches anyway. Risks named: failed trees unbounded by design in v1; the fold-shape migration touches every `status` consumer — a missed one is the arc's eleventh-defect class; the watcher-drop behavioral leg is WSL-only. Existing single-`status` fold tests rewritten to the split shape — disclosed, asserts strictly more. ~~NO new invariant/subject/trait method/dep~~ **LEDGER CORRECTED by [DR-052](decisions/DR-052-worktree-release-ledger-correction.md) (2026-07-25): NO new invariant, subject, or dep — but the merged diff DID mint FIVE things: one defaulted `McpSubstrate::release_worktree` trait method, one mutating MCP tool `release_worktree` + `ReleaseWorktreeArgs`, one refusal code `worktree.unknown`, a breaking `WorktreeRow` shape change amending [DR-039](decisions/DR-039-board-view-mcp-resource.md), and one additive-optional payload FIELD `gate.failed.worktree?` on a ratified v1 subject (`v` stays 1; carries its own DR-012 absence ruling; criterion (c) is unreachable without it); all five already authorized in substance by Decisions 2/3, ratified after the fact.** Risk (1) ESCALATED by DR-052: post-restart, a retained failed tree is unreachable by the release door (`Daemon::allocations` is process-lifetime) — refuses loudly, bounded by nothing | ACCEPTED | §7, §5/§6 |

| [DR-050](decisions/DR-050-trials-slice-a-closeout.md) | **Trials slice A closeout** — records three findings slice A surfaced but could not fix in scope. (1) The version gate (`version_gate`/`codex_version_gate`) is a **well-tested unreachable**: zero non-test callers, and the vet native requires a spec to *declare* `harness_version` without ever comparing it to a tested list or to the spawned binary — dormant only while the daemon admits `"claude-code"` alone. (2) Three `bins/rezidentd/src/runs.rs` traps NAMED as slice B **entry criteria** DR-048 did not state — the `pep = "enforced"` stamp keyed on gate-name presence rather than `plan.permit_hook_config().is_some()` (a codex run could log itself PEP-enforced while nothing intercepts; closed from the crate side by `for_codex` refusing a declared `[gates.permit]`, still mis-keyed daemon-side), `AdapterError::ContractViolated` at risk of degrading into the tolerant garbage-line warn branch (the adapter refuses loudly, the daemon whispers), and a second unpinned `agent.completed` fallback literal that agrees with `Completion::into_fact` today by coincidence, not by construction. (3) THE CONTESTED ITEM AT THE TIME — ACCEPTED on evidence; see [DR-051](decisions/DR-051-codex-failure-recording-and-fallback-fidelity.md) — codex's `turn.completed → status:"success"` mapping: implementer reads it as the harness *positively asserting* termination (unknown → `None` → no fact); auditor holds, narrowed, that `turn.completed` asserts the turn TERMINATED while `status` is the ontology's OUTCOME — two propositions, one signal — and that the claude-code comparison cuts the other way, since there `type:"result"` and `subtype`/`is_error` carry termination and outcome SEPARATELY. Evidence recorded as **consistent-with, not dispositive** (the fixture's line-3 `error` is benign skills noise; no recorded transcript shows a turn that ended badly). Entry-gated: record a failing codex transcript before any Trials scoring trusts a codex `"success"`, since a mismapped failing turn would score as `pass` — the exact miscoercion I6 exists to forbid. Residual is **mismapped** failure, not unmapped: the daemon's `completed_id.is_none()` fallback already covers a codex run that fails silently. NO new invariant/subject/trait/dep; no test weakened, three criteria added | ACCEPTED | §7, §8 |
| [DR-051](decisions/DR-051-codex-failure-recording-and-fallback-fidelity.md) | Codex turn-failed mapping CONFIRMED by recording, scoped to codex-cli 0.145.x — amends DR-050. A real failing codex-cli 0.145.0 transcript (bogus `-m` model) terminates in `turn.failed`, never `turn.completed`; the auditor's mismapped-failure branch is REFUTED for `TESTED_CODEX_VERSIONS = &[(0,145)]` only, not generalized, with the auditor's structural dissent preserved as still-true description. STRIKES DR-050 risk item 5. CORRECTS DR-050's "clean residual" claim: the daemon's `completed_id.is_none()` fallback (`runs.rs`, the `completed_id.is_none()` literal in `drive_run`; `:1702-1723` as of `da17699`) fires but hardcodes a zeroed, reasonless completion, discarding the harness's now-proven failure reason. BONUS FINDING: `turn.failed` carries no `usage` object — a failed candidate's cost is ABSENT not zero, folding to `None` end-to-end in the reducer; uncaught, DR-048 slice C would score a failed run as free. SHARPENS DR-050 §Decision 2(c) from a structural key-set pin to a semantic failure-payload one (a key-set-only test over success payloads would pass while the fallback's zeroed literal contradicts absence). ADDS one DR-048 slice-C entry criterion: absence must survive to every cost-field consumer, not just the reducer — `rezidnt debrief`'s cost block (the `cost` block of the `rezidnt debrief <run>` command in `bins/rezidnt/src/main.rs`) serialized an unfolded `Option` as JSON `null` — LIVE on `main` at `d91a7ea`, fixed by the commit that lands this record; independent of codex. NO new invariant/subject/trait/dep; no test weakened, one criterion corrected before merge, one live defect named | ACCEPTED | §7, §8 |

| [DR-052](decisions/DR-052-worktree-release-ledger-correction.md) | DR-049's ledger corrected — an incomplete banner, not unauthorized work (`/debrief`-adjudicated). DR-049 §Amends claimed "Mints NO invariant, NO subject, NO trait method, NO new dep"; RATIFIES five items DR-049 §Decision 2/3 already authorized in substance but the banner missed: (a) one method on `McpSubstrate` — NOT an I4 violation, precedent [DR-011](decisions/DR-011-permit-pdp-config-seam.md) §Decision 1 (the MCP core's daemon seam is not one of the four §7-bound traits); (b) one mutating MCP tool `release_worktree` + `ReleaseWorktreeArgs`, precedent [DR-044](decisions/DR-044-live-lead-sub-fanout.md); (c) one refusal code `worktree.unknown`, precedent [DR-045](decisions/DR-045-fan-out-lead-only.md); (e) one additive-optional payload FIELD `gate.failed.worktree?` on a ratified v1 subject (`spec/ontology.md:79`/`:330`, lane commit `df61f89`) — "NO subject" was true and still insufficient, since the house ledger enumerates fields explicitly and criterion (c) is unreachable without failure attribution; (d) a breaking `board_view` response-shape change — `WorktreeRow` drops `status`, gains `lifecycle`+`outcome` — amending [DR-039](decisions/DR-039-board-view-mcp-resource.md)'s shape, left un-flagged there. ESCALATES DR-049 risk (1): `Daemon::allocations` is process-lifetime, so a failed tree surviving a restart is not MCP-releasable at all — refuses LOUDLY (`worktree.unknown`), never silently (I6-consistent; NOT a DR-049 criterion (c) gap, since that criterion never claimed restart survival) — but post-restart accumulation is bounded by NOTHING. NAMES, doesn't fix, an ordering exposure: the reducer folds `outcome=failed` for ANY `gate.failed` carrying a worktree, gate-name-agnostic; a future post-merge gate with a worktree cwd could overwrite `merged`. NO guard added — a `gate=="pre_merge"` restriction would be a CONTENT filter narrowing `spec/ontology.md:330`'s ratified multi-gate semantics, and is NOT the ordering-guard shape DR-049 §Decision 2 already rejected (conflated in-flight, corrected here). SETTLES two door-policy calls out of rustdoc into decision text: `release_worktree` admits a DUAL door (operator badge OR agent macaroon), unlike `kill_run`'s operator-only door; and reuses verb `"merge"` rather than minting a new one. NO new invariant, NO new subject, NO new dep — the five "mints" are disclosure corrections for work already authorized | ACCEPTED | §7, §9, §5/§6 |

| [DR-053](decisions/DR-053-agent-completed-error-mint-disclosure.md) | **A SIXTH undisclosed mint, disclosed** — `agent.completed.error?: {message}`, an additive-optional payload FIELD on ratified `agent.completed` v1, minted on the DR-048 slice-A lane for the `turn.failed` arm [DR-051](decisions/DR-051-codex-failure-recording-and-fallback-fidelity.md) ratifies. Recorded twice in `spec/ontology.md` (payload block + warden changelog) and emitted by `Completion::into_fact`, but disclosed in NO record in `docs/` — DR-051's §Amends says "Mints NO invariant, NO subject, NO trait method, NO dep" — every limb TRUE, and the FIELD the work minted has no slot in that sentence to be named in. Same defect [DR-052](decisions/DR-052-worktree-release-ledger-correction.md) item (e) ratified against DR-049, one record over, found by the same pass. ALSO discloses the `cost.input_tokens?`/`output_tokens?` present-or-absent-TOGETHER ruling (a failed candidate's cost is ABSENT not zero; zeros would read as a free run — an ACCOUNTING defect on a reported figure, explicitly NOT a selection defect, since DR-048 v1 admits only PASSING candidates and compares verdicts + wall time only, never tokens). CORRECTS, strike-preserved, [DR-050](decisions/DR-050-trials-slice-a-closeout.md) §Deferred's "any ontology change the failing-transcript work might reveal — none is expected" — falsified by the very work it predicted about; the second such falsified prediction in the arc, structurally identical to DR-049 §Decision 6's. AMENDS NO SECOND LEDGER: two drafts tried (DR-051's, then DR-050's) and **both are withdrawn** — DR-051's own diff is paperwork (true under record-scope), and DR-050 explicitly DEFERRED the failing-transcript work that minted the field, which landed at `4ceccb7`, posterior to slice A's closeout `a559623`, so its slice-scoped ledger is true too. The second draft reached its verdict by applying slice-scope to one record and record-scope to the other — an equivocation, withdrawn and recorded. Resolving that scoping question is OWED ITS OWN RECORD, since the likely rule reaches back into DR-049's ratified falsity. RETIRES the four-limb ledger form as a house DEFAULT in favour of **invariant · subject · FIELD · trait method · dep** — on a premise that WEAKENED under review and is stated at surviving strength: **one** false ledger (DR-049's, already ratified by DR-052) plus one incomplete rendering by the author correcting the form (DR-052's own self-ledger omits the trait-method limb). Carried on additive-and-cheap-to-revisit, not on weight of evidence; "add no convention" is recorded as a live, respectable alternative. Back-pointers into DR-050/DR-051 deliberately WITHHELD while PROPOSED (the discipline DR-052 applied to itself). Mints NOTHING; no test weakened, no criterion added or relaxed | PROPOSED | §7, §5/§6 |

| [DR-054](decisions/DR-054-contract-violated-debrief-exit-code.md) | `rezidnt debrief` exits **3**, not **0**, for a run carrying a folded `run.contract.violated` record — amends [DR-004](decisions/DR-004-exit-code-table.md)'s exit-3 class ("substrate fault, including daemon-side refusals") to include it. Joins the EXISTING exit-3 disjunction (`report.alarms` non-empty OR any gate `inconclusive` OR now `contract_violated.is_some()`) and DOMINATES exit 5: a run that is both contract-violated and gate-failing exits 3, since "cannot certify this run at all" outranks "one gate said fail" — mirroring the standing inconclusive-over-fail rule (I6: neither trusted nor coerced, and a withdrawn premise is never a failure verdict). Exit 5 REJECTED (a contract violation is not a gate verdict; coding it 5 would let a caller "fix" it by making gates pass); a dedicated new code REJECTED (exit 3 already means cannot-certify, and a second such code fragments a 0-vs-not-0 CI contract for detail the `--json` report already carries). Discharges the OPEN question `bins/rezidnt/tests/dr050_contract_violated_debrief.rs`'s header names; that test's violated-run assertions FLIP from exit 0 to exit 3 BY DESIGN, per its own disclosure — a red test there is the ruling's judge arriving, not a regression. Does NOT block a merge: `contract_refused` is read only in `drive_run`'s stream-loop short-circuit, so a violated run still reaps, chunks its capture, runs `pre_merge`, and CAN STILL MERGE — whether a withdrawn stream premise should block a merge is UNRULED, named DR-048 slice-C territory. Does NOT widen `RunRow`/`SubRow` — a violated run's totals stay unmarked on the board, deferred to slice-C scoping (owner, 2026-07-25). Prospective in practice: the emitter is WIRED-BUT-UNREACHABLE on any live daemon path (`SUPPORTED_HARNESSES` refuses `"codex"`, the sole `ContractViolated` construction site), so this exit class fires today only against directly-seeded logs. Mints NO invariant, NO subject, NO field, NO trait method, NO dep; one test's assertions flip by design | ACCEPTED | §9 |

| [DR-055](decisions/DR-055-trials-slice-b-matrix-primitive.md) | **Trials slice B: the trial matrix primitive.** Sample identity is DAEMON-DERIVED — one trial-level idempotency key, from which each sample's spawn key is derived deterministically with its (variant, sample-index); re-running a trial re-derives identical keys, so retry dedupes exactly as before while N samples get N distinct runs. **Weakens nothing** (DR-048 §Consequences' binding clause, discharged in plain words): the dedup RULE is untouched and still resolves through the EXISTING per-workspace `spawn_keys` map — only the key SPACE is extended, by the map's caller rather than the map. Rejected: caller-supplied per-sample keys (incompatible with server-side expansion); a dedup-scope discriminator (that WOULD change the rule). NEW operator-badged mutating MCP tool **`open_trial`** + `OpenTrialArgs` — `fan_out` is unusable on two independent grounds: it is lead-only by [DR-045](decisions/DR-045-fan-out-lead-only.md) §Decision 1 and a trial has no lead run to key `agent.spawned.lead_run?` on, and server-side expansion is forced by the key derivation. Mirrors DR-045's door the OTHER way, reusing `check_operator_badge` VERBATIM — **no new refusal code** (a presented macaroon falls through to existing `BADGE_INVALID`, the conflation DR-045 §Decision 4 declined to fix on `kill_run` and this does not fix either). THREE mints via [DR-006](decisions/DR-006-replay-divergence-signal.md): subject `trial.opened` (records INTENT — a worktree-conflicted sample mints no run and is REFUSED on the tool response, never the log, so without it a 3×3 matrix that spawns 7 is indistinguishable from 7 unrelated runs); fields `agent.spawned.trial?` (membership — correlation CANNOT carry it, DR-049 verified that join unsound: one correlation per spec spans N runs and N trees) and `agent.spawned.model?` (**`AgentSpec.model` reaches spawn argv and the spec preimage but NEVER the fact** — so a leaderboard folded from the log cannot attribute a result to a model, load-bearing for a feature whose point is comparing models). TWO refusals recorded prominently: `variant?` as a synonym (a variant IS (`agent`, `harness`, `model`), all three on the fact once `model?` lands) and **`trial.scored`** as derived state — DR-048 §Context already rules the leaderboard "just another fold — I3 makes history free," and a computed projection on the log is what I3 forbids; a QUEUED item RETIRED, not re-minted. Backpressure INHERITS `fan_out`'s whole-call refusal (`FAN_OUT_TOO_WIDE` precedent, never partially spawned) — **no scheduler**, since DR-048 v1 states the cap is the only backpressure and `rezidnt-supervise` does not exist; V×N load NAMED as risk, DR-048 risk (2) mitigated but NOT eliminated by DR-049's landed release lifecycle (failed samples still accumulate, no TTL). Scoring stays OUT (slice C); selection is slice D. States plainly that the **harness axis is PROSPECTIVE** — `SUPPORTED_HARNESSES` admits only `"claude-code"`, so a slice-B trial varies model and agent-spec within one harness and the implementer must refuse a harness-varying matrix honestly rather than silently narrow it. Inherits, does not build, the DR-050-set/[DR-054](decisions/DR-054-contract-violated-debrief-exit-code.md) obligation that slice C treat a contract-violated sample's accounting as untrusted. `SUBJECTS_V0` 49 → 50. **Ledger CORRECTED same day, strike-preserved (§Decision 7), by the oracle pass before any implementation:** the banner's "NO refusal code" was incompatible with §Decision 4's own design — reusing `FAN_OUT_TOO_WIDE` on `open_trial` would answer an operator with a refusal naming a tool they never invoked (the I6 defect fixed in `loopback_post` the same day), so the cap mints **`TRIAL_TOO_WIDE`**; and `open_trial` needs an `McpSubstrate` method like `fan_out`/`release_worktree` before it, so **ONE trait method** (not an I4 matter — the MCP core's daemon seam is not one of the four §7-bound traits, DR-011 §Decision 1 as ratified by DR-052 item (a)). Corrected in place on DR-052's disclosure-correction precedent rather than in a DR-056; §Decision 4's substance is unchanged. Note the deliberate asymmetry: the DOOR mints no code (a macaroon falls through to the existing, still-accurate `BADGE_INVALID`), the CAP does (the borrowed code would be false) — reuse is right when the borrowed code still tells the truth. §Decision 8 additionally REFUSES a matrix carrying duplicate variant triples (`ARGS_INVALID`, the `FanOutArgs` empty-list precedent: caller bugs answered loudly), since identical triples are indistinguishable downstream and would put two rows for one variant on the leaderboard. NO invariant, NO dep; no test weakened | ACCEPTED | §7, §9, §12 |

| [DR-056](decisions/DR-056-prose-maintenance-tax.md) | **The prose-maintenance tax.** Code claims are pinned by the gauntlet; prose claims (DR ledgers, doc-comment rationale, `file:line` citations) have no verifier, so they drift and the correction instrument is itself prose that drifts. THREE decisions: (1) ledger/citation corrections **amend the original record IN PLACE**, dated strikethrough, original verbatim — the pattern [DR-049](decisions/DR-049-worktree-release-lifecycle.md) §Status already used and [DR-055](decisions/DR-055-trials-slice-b-matrix-primitive.md) §Decision 7 applied before this record existed; only a changed DECISION mints a new numbered record, retiring the errata-record genre [DR-052](decisions/DR-052-worktree-release-ledger-correction.md)/[DR-053](decisions/DR-053-agent-completed-error-mint-disclosure.md) exemplify (both stand as accurate history; they are the last built the old way). (2) A doc comment asserting another module's behaviour or citing `file:line` **must be pinned by a source-text guard or not written** — the corollary to the standing mutation-proof rule. (3) Records **cap at ~800 words** (Context/Decision/Ledger/Consequences). Evidence: DR-052 (2,136 words) exists only to correct DR-049's ledger, DR-053 (3,121) only to correct DR-050/051's, its Status field alone ~500 words adjudicating its own back-pointers; ~80,399 words of records against ~412,740 of Rust; `intel/003` (DR-002 rule 3) asserted a gap DR-049 closed the day before. **Cuts UNPINNED prose ONLY** — oracle boards, source-text guards, the gauntlet, the auditor's verdict contract are the moat and are untouched ([DR-051](decisions/DR-051-codex-failure-recording-and-fallback-fidelity.md) is the proof the discipline is load-bearing); explicitly not a license to test less. **The ~800 cap is already falsified by four data points and wants revisiting before ratification** — DR-056 itself 851, DR-057 866→986→1282 under its own corrections, DR-058 pending; the four-section SHAPE held every time, the number never once did. Mints NO invariant, subject, field, trait method, or dep | PROPOSED | §20 |

| [DR-057](decisions/DR-057-diff-review-mcp-resource.md) | **`diff_view` + `cas_read` — the read surface that closes Review.** The operator cockpit (`rezidnt-operator`, [DR-038](decisions/DR-038-operator-client-gui-tauri.md)) wires Watch and Steer; Compare is DR-055; **Review had no surface at all**, so an operator could not see what an agent changed without opening an IDE. Diff bytes have been CAS-pinned since S2 (`runs.rs` `diff.ready.diff`, `cas.put(text, "text/x-diff")`) with no door to read them back — absent on CLI *and* MCP alike. TWO read-class, UNBADGED tools on the DR-039/DR-040 precedent: `diff_view` keyed by **worktree, never run** (settled against the tree, not by analogy — `RunRow` carries no worktree and `WorktreeState.allocator` is the bare string `"rezidnt"` on the ordinary path, so a run-keyed lookup has nothing sound to join on; DR-049 already ruled the correlation join UNSOUND), serving `diff: null` for a diffless tree and never a fabricated empty ref; `cas_read` bounded at `MAX_CAS_READ_BYTES_DEFAULT` 256 KiB, **REFUSING over-bound content rather than chopping it** — a review surface that silently truncates is worse than none. `WorktreeState.last_diff` widens `String` → `Option<CasRef>`: **additive on the INFORMATION axis, BREAKING on the SERIALIZED axis** (three goldens, five reader suites incl. one `#[cfg(unix)]` the record missed) — same undisclosed-`WorktreeRow`-shape-change class DR-052 item (d) ratified against DR-049, recurring one record later, disclosed here rather than found at merge; `WorktreeRow` maps `ref.hash` explicitly so `board_view`/DR-039 is unamended. **I2's scope left OPEN and named, not ruled** — an earlier draft settled "fabric-scoped" and was narrowed, since `cas_read` is one-shot and refuses rather than streaming, hence I2-consistent under either reading; the record instead names the real open question, that `board_view`/`orchestration_graph` return whole-log JSON with **no size bound at all**. Carries THREE in-place corrections under DR-056 §Decision 1 (the I2 narrowing, a false self-claim about the word cap, the "additive" error), which under the retired genre would have been three new records. `/vet` + `/debrief` both PASS; the debrief found an unbadged **path-traversal metadata oracle** in `cas_read` (fixed, mutation-proven: pre-guard it leaked the blake3 of any daemon-readable blob) and that DR-057's own "text mimes only" bound is **vacuous** — the CAS persists no mime, so the gate judges the caller's claim. SIX `cas.*` refusal codes minted, **ratification owed DR-058** along with the trust-tier ruling. NO invariant, subject, field, or dep | PROPOSED | §9, §19 |

*The next record is DR-058.*

> **Index convention CORRECTED 2026-07-26.** ~~Rows and this pointer landed only on ACCEPTANCE~~ — the practice [DR-056](decisions/DR-056-prose-maintenance-tax.md) §Consequences and [DR-057](decisions/DR-057-diff-review-mcp-resource.md) §Consequences each committed to, and it does not scale: with three records PROPOSED at once the pointer sat three stale, still reading "DR-056" while DR-056 and DR-057 existed on disk, so a fresh session reading §20 would have minted DR-056 a second time. **A record gets its row and the pointer moves when the FILE LANDS, whatever its status** — which is what [DR-053](decisions/DR-053-agent-completed-error-mint-disclosure.md) (PROPOSED, indexed) already did, making the withhold the deviation rather than the rule. The status column carries PROPOSED-vs-ACCEPTED; the index answers "what exists and what number is free," which is a fact about the tree, not about ratification. Withholding still applies to what it was actually for: **amendment back-pointers stamped into OTHER records** stay withheld until the amending record is accepted (DR-052/DR-053's discipline, unchanged).

---

## Appendix A — dependency table

| Crate | Purpose | License | Confidence |
|---|---|---|---|
| tokio | async runtime, channels | MIT | high |
| serde / serde_json | envelope + payloads | MIT/Apache | high |
| rusqlite | event log, WAL | MIT | high |
| ulid | event ids | MIT | high |
| schemars | JSON Schema export → npm types | MIT | high |
| gix | git read paths | MIT/Apache | high (reads) |
| notify | FS watching | CC0/Artistic-2.0 family | moderate — verify license detail |
| blake3 | CAS + chain | CC0/Apache | high |
| tracing | observability | MIT | high |
| clap | CLI | MIT/Apache | high |
| rmcp | MCP server SDK | Apache (expected) | **moderate — verify at S3** |
| interprocess or tokio-net | UDS + named pipes | MIT-family | moderate — pick at S0 |
| portable-pty | Phase 3 PTY (incl. ConPTY) | MIT | high |
| libghostty-vt (FFI) or alacritty_terminal | Phase 3 VT kernel | MIT / Apache-2.0 | high / moderate — verify |
| ratatui | S5 TUI board | MIT | high |
| rustls | TLS termination (C3b+c egress MITM, DR-026) | MIT/Apache/ISC | high |
| rcgen | rezidnt CA + per-destination leaf certs (C3b+c, DR-026) | MIT/Apache | high |

## Appendix B — subject taxonomy v0 (excerpt; canonical copy lives in `spec/ontology.md`)

```
workspace.opened | workspace.closed | workspace.spec.applied
worktree.allocated | worktree.observed | worktree.conflict | worktree.released
session.created | session.attached | pane.spawned | pane.exited
agent.spawned | agent.status.changed | agent.blocked | agent.completed | agent.signaled
gate.entered | gate.passed | gate.failed | gate.inconclusive | gate.explained
artifact.captured                  # {ref, mime, bytes, provenance}
diff.ready | merge.completed | merge.rejected
adapter.health.changed | daemon.started | daemon.warning | daemon.error
badge.issued | badge.revoked
```

*End of the plan body. BINDING changes are recorded as decision records — see [§20](#20-decision-records) and [`docs/decisions/`](decisions/).*
