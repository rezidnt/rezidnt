---
description: Write failing tests from the current slice criteria
argument-hint: "[component]"
---
Delegate to the oracle agent. Convert the acceptance criteria for the current slice (or the component named in $ARGUMENTS) into failing tests, property tests, and golden fixtures before implementation. Confirm each new test fails. Return the list of failing tests as the work order for the implementer.
